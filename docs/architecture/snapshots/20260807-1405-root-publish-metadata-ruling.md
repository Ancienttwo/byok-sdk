# Architecture Snapshot: root publish-metadata ruling

> **Date**: 2026-08-07
> **Functional Block**: `root`
> **Capability ID**: `root`
> **Type**: ruling (adjudication record, not a boundary description)
> **Request Card**: `docs/architecture/requests/root.md`
> **Supersedes nothing**: the K0 events remain adjudicated by `20260807-1200-root-card-ruling.md`

## Event

Four new drift events landed on the root card on 2026-08-07, one per workspace package manifest, all from the `k4-publish-prep` slice:

| Event time | File |
| --- | --- |
| 2026-08-07T14:01:48+0800 | `packages/protocol/package.json` |
| 2026-08-07T14:01:50+0800 | `packages/client/package.json` |
| 2026-08-07T14:01:52+0800 | `packages/server/package.json` |
| 2026-08-07T14:02:03+0800 | `packages/keys/package.json` |

The hook classifies every `package.json` edit as `boundary-or-config`, so all four fired at `medium` severity regardless of content. The actual content is npm publish metadata:

- All four gained `publishConfig.access: "public"`, `repository` (with the per-package `directory`), `bugs`, and `homepage`.
- `packages/keys` additionally moved `version` `0.0.1` → `0.1.0` and gained `"prepublishOnly": "pnpm run build"`.

## Ruling

**No architecture change. Closed by adjudication.**

These are publish-registry fields only. Checked against the four things the card's follow-up asks about:

- **Module boundaries** — unchanged. No `exports`, `main`, `module`, `types`, or `files` field was touched, so every package's public surface is byte-identical. The `keys` ↔ `client`/`server`/`protocol` separation that canonical `docs/architecture/sdk-architecture.md` §7 records is untouched.
- **Entrypoints** — unchanged. `packages/client`'s `bin` block is untouched; no new binary, export path, or entry file exists.
- **Dependency rules** — unchanged. No `dependencies`, `devDependencies`, `optionalDependencies`, or `peerDependencies` entry was added, removed, or re-versioned in any of the four manifests. The added `prepublishOnly` script invokes the package's own existing `build`.
- **Runtime paths** — unchanged. `prepublishOnly` runs only under `npm publish`, which this slice does not perform; nothing in it executes at import or at test time.
- **Verification commands** — unchanged. The repo's required checks (`pnpm -r run typecheck`, `pnpm -r run test`, `pnpm -r run build`, `repo-harness run check-task-workflow --strict`) are the same commands, and all pass.

Evidence, all run in the primary worktree with these edits applied:

| Check | Result |
| --- | --- |
| `pnpm -r run typecheck` | exit 0 across all workspace packages |
| `pnpm --filter @byok/keys run test` | exit 0 — 15 files / 328 tests, identical to the pre-slice baseline recorded in `docs/researches/k4-aip-swap-dryrun.md` §0 |
| `pnpm -r run build` | exit 0 — tsup output unchanged in shape; the manifests carry no field tsup or `tsconfig.build.json` reads |
| `cd packages/keys && npm pack --dry-run` | exit 0 — tarball gains only `LICENSE`; the `dist/` entry list is unchanged (22 → 23 files) |

The version bump to `0.1.0` is a registry-visible change, not an architectural one: nothing in this repo resolves `@byok/keys` by version (the workspace links are `workspace:*` / `file:`), and no artifact has been published.

The module boundary therefore stands exactly as recorded in canonical `docs/architecture/sdk-architecture.md` §7 and in the first root ruling snapshot, `docs/architecture/snapshots/20260807-1200-root-card-ruling.md`. Neither is restated here; both remain the source of truth.

## Durability Note

The request card `docs/architecture/requests/root.md` is regenerated by the architecture-event hook on every matching drift event, which overwrites any prose written into it — the 2026-08-07 adjudication section merged in PR #16 was lost that way when this slice's four events fired. Ruling prose therefore lives in these snapshot files, which the hook never rewrites; the card carries only a thin pointer to them.

## Slice

- Plan: `plans/plan-20260807-1331-k4-publish-prep.md`
- Contract: `tasks/contracts/20260807-1331-k4-publish-prep.contract.md`
- Notes: `tasks/notes/20260807-1331-k4-publish-prep.notes.md`
