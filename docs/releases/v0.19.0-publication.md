# 0.19.0 publication record

Published 2026-09-22 under user authorization. Ten aligned SDK packages are
0.19.0; independently versioned keys is 0.6.0.

- Source and annotated tag target: `9408ed7b80ffb0f7ab21f5d82f50782f18074acb` / `v0.19.0`.
- [PR #221](https://github.com/Ancienttwo/byok-sdk/pull/221) merged that exact source; its CI was 46/46.
- [Main push CI 35711372146](https://github.com/Ancienttwo/byok-sdk/actions/runs/35711372146):
  completed, success.
- Frozen main artifact `release-pack-9408ed7b…` (id `10686497675`): eleven tarballs
  plus `release-manifest.json`, whose `sourceGitSha` is the release commit and
  every one of whose recorded SHA-256 digests re-hashes to the downloaded tarball.
- Manifest SHA-256: `074c331325ba47e49d7d4349693f7ebd0f8fa178b6d39dcf897f4599c212c072`.
- The eleven published packages: `@byok-sdk/core`, `@byok-sdk/implementation-identity`,
  `@byok-sdk/protocol`, `@byok-sdk/server`, `@byok-sdk/cloud`, `@byok-sdk/client`,
  `@byok-sdk/cloud-dataplane`, `@byok-sdk/ui-runtime`, `@byok-sdk/testkit` and the
  `byok-sdk` umbrella at 0.19.0, plus `@byok-sdk/keys` at 0.6.0.
- `@byok-sdk/implementation-identity` is a first publication; the package did not
  exist on npm before this train.
- Publication ran from a detached checkout of the release commit with interactive
  web 2FA: `node scripts/release/publish.mjs --artifacts <dir> --execute`, no
  `--otp`. All eleven `npm publish` calls returned PUT 202.
- The script's step-7 readback then failed with E404 on `@byok-sdk/server@0.19.0`
  — registry propagation, the same class as 0.17.0's ui-runtime E404. No package
  was republished. About 1.5 minutes later all eleven versions were visible and
  `node scripts/release/registry-readback.mjs --manifest …` passed.
- Canonical readback: exact closure of the eleven packages; `npm ls @byok-sdk/core`
  resolves only 0.19.0; a single `@byok-sdk/pi-coding-agent@0.86.1001` runtime with
  `byokFork.upstreamCommit` `13cbf77…`; every registry `dist.integrity` equal to
  the manifest's `sha512Integrity`. `latest` points at 0.19.0 on the ten aligned
  packages and at 0.6.0 on keys.
- The annotated `v0.19.0` tag was created locally at the release commit exactly as
  `publish.mjs` step 8 does, carrying the frozen `sourceGitSha`. Its push to origin
  is the Owner's step and is PENDING at the time this record was written.
- Local web 2FA publication has no GitHub Actions OIDC provenance attestation,
  as with 0.17.0.
- Prior evidence: the external tarball audit and its P2 fixes (`cf5feaef`), and the
  acceptance receipt (`user_waiver`) bound to `9408ed7b`.
- Ignored local evidence: `_ops/releases/0.19.0-*`. No authentication URLs or
  credentials are included in this record.

[Release notes](v0.19.0.md) document the two breaking cuts this train carries —
the input-preparation wire and the keys launcher argv — and the record-log caveat
with its exact path and manual disposition. See the
[release handoff](v0.19.0-handoff.md) for the preparation receipt.
Post-publication documentation does not change the frozen source or package bytes.
