# 0.18.0 / keys 0.5.0 release preparation

Status: stable source prepared; subject-bound CI/artifact/dry-run receipt required; no merge, npm publication, tag or GitHub Release.
PR181 remains Draft. This handoff does not authorize those actions.

## Scope and upgrade decision

The existing 0.18.0-rc.1 / keys0.5.0-rc.1 breaking train becomes stable identities,
without additional product logic or third-party dependency changes. Spec version
policy is authoritative. The old runbook patch-default paragraph conflicted with
the existing spec and selected RC; it now links to that single policy.

Release notes: [0.18.0](v0.18.0.md). Required custom-store methods, old keys SQLite
refusal and explicit Pi model/binding provisioning are upgrade requirements.
No automatic migration or live-store conversion is supplied or executed.

## Release Gate 2.0

| Surface | Evidence / remaining boundary |
|---|---|
| Base and scope | PR base bb3e1b19; last stable v0.17.0. Full PR reviewed separately from the version-only final delta. |
| RC source | f3138751; both native CI runs34519052311/34519058101 succeeded,46checks. |
| Stable source | Freeze the version/lock/docs commit after acceptance; record exact SHA in ignored release018 receipt and GitHub push CI. |
| Versions | Nine dispatch packages0.18.0, keys0.5.0; workspace root0.0.0 is private and not a release package. |
| Dependencies | Workspace edges preserved; lock delta must contain only ten workspace version changes. Pi stays0.85.1. |
| Generated API | Existing public declaration snapshots must match rebuilt declarations. |
| Installed packages | Download exact push-CI ten-package artifact, verify hashes/versions/dependency closure and installed smoke. RC tarballs cannot certify stable identities. |
| Registry | Last readback latest SDK0.17.0 / keys0.4.3; dry-run rechecks target-version availability. |
| Publish driver | Run scripts/release/publish.mjs --artifacts <exact-CI-directory> without --execute or --tag. |
| PR and main | Draft181; review/preparation does not merge. Main remains a separate integration boundary. |
| Account / tag / release | No account mutation, uploads, tags or Release writes. Account/2FA must be rechecked only for authorized publication. |
| Product distribution | Salesko pins, Profile/store migration, budgets/Summary and deployment are separate. aiphabee untouched. |

The generic skill release_gate.py reports private root package0.0.0 as a version
regression. This is an inapplicable single-package heuristic, not a product fault;
repo check:version-authority and check:release-graph own this monorepo's train.

## Publication boundary after preparation

1. Obtain explicit merge/publication authorization and resolve any acceptance
   blockers. Select the exact final main source before producing final evidence.
2. Reuse matching green push-CI artifacts only. A new merged source requires its
   own matching manifest; do not relabel the branch or older RC artifact SHA.
3. Run the official frozen-artifact dry run in a clean checkout of that source.
4. Under publication authorization, verify npm account/write-2FA and execute the
   existing driver. Stable releases omit --tag. Read all package integrities and
   exact dependency edges back before tag/Release creation.
5. Preserve original bytes and inspect registry state after partial failure;
   never blindly republish. Product updater/deployment remains separately owned.
