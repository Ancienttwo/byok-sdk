# 0.22.0 / keys 0.7.0 — Salesko and Owner handoff

> **Update, 2026-09-25.** 0.22.0 and keys 0.7.0 WERE published to npm on
> 2026-09-25 from `0962f14f` and are the registry's `latest`; the annotated
> `v0.22.0` tag was created by hand because `publish.mjs` did not reach step 8.
> Everything below was written BEFORE publication and is retained as the
> historical preparation receipt; the Salesko cutover steps still apply. The
> current publication state is the [0.22.0 publication record](v0.22.0-publication.md).

Status (as written, before publication): release preparation; publication, tag and Host deployment remain
separate. Source base is main `9fe732e6` (official Pi migration PR #233).
[Release notes](v0.22.0.md) define the shipped contract; this handoff is the
bounded downstream and publication checklist.

## Salesko cutover

1. Fence new admissions and drain queued/in-flight old preparations and
   Executions before upgrading. Preserve terminal instruction/audit bytes;
   do not replay history or rewrite old records into the new identity.
2. Pin the eight aligned SDK packages at 0.22.0 and the separately installed
   keys launcher at 0.7.0 after registry readback. The eight are core,
   implementation-identity, protocol, client, cloud, cloud-dataplane, server
   and ui-runtime. No byok-sdk umbrella or public testkit returns.
3. Assemble complete Host framing and product instructions into the required
   `prompt.systemPrompt`. The prepared model sees no Pi default prompt.
   Remove custom/append/cwd/skills/context/docs/tool-snippet renderer inputs;
   device-local files cannot supply missing prompt authority. Keep observed
   tools in the tools argument; no message tool enters D.
4. Switch once to wire/record 7 and `agent-input-preparation-v7`. Rebuild current
   preparation and runtime-installation records rather than reading v6.
   Preserve required egressPolicy, messageEgress and agentMessageContext in
   the prepared user-turn flow. Empty requiredToolsets is now admissible;
   prepared native-tool policy remains empty.
5. **Issue a new ruling revision**, even though C remains 1024. Bind the actual
   attested official runtime and compiler identity, envelope v4, `P(D)=D`,
   `ruledResidualKeys=[]` and **C=1024**. Never reuse a fork-bound revision.
   The measured identity is in the M5 evidence's `rulingRecommendation.runtime`:
   `@earendil-works/pi-coding-agent@0.87.1`, compilerVersion 4,
   closureDigest `33ee3e426437c4bfd9fb50c06252c1ccd8196aa97729f93f00e6a14a289e5ab2`.
   Copy/read back the complete validated tuple, including tarballIntegrity,
   upstreamCommit and provenanceDigest; name/version alone is insufficient.
6. Read back the lane's official identity, artifact envelope digest, byte
   accounting and usage handling. Verify continuation typed refusals reach
   task.fail and settle the Host execution. Reopen admissions only after the
   downstream gate; this document is not a deployment receipt.

Calibration authority: [M5 result](../../tasks/runs/20260925-official-pi-c-reprobe.json)
and [.sha256](../../tasks/runs/20260925-official-pi-c-reprobe.json.sha256).
Measured uncovered amount was zero; prepared requestBytes=17638 and prompt=6581
leave 12081 under bytes+1024. Identical-D repeat counts cached tokens in prompt.
The samples are synthetic calibration, not a universal proof or production
Salesko ruling. Current prompt/tool schemas still need Host approval.

Operational boundaries: OPENAI_LOG can produce local compilation logs when
the device owner opts in; the prepared child does not inherit the six OpenAI
variables. The official npm layout has 10 roots and isolated compat registries;
do not claim cross-instance registerApiProvider sharing. pi-tui carries the
Win32 native prebuild, so an arbitrary npm tree is not a portable sealed binary.
See release notes for the accepted continuation overflow boundary.

## Release preparation evidence

The local report is `/private/tmp/h5-salesko-prep-20260923/release-0-22-0-report.md`.
It records the exact preparation HEAD, fresh clone commands/results, final
sourceGitSha-bound pack artifacts and publication dry-run output. The notes
under `tasks/notes/20260925-1007-release-0-22-0.notes.md` record durable decisions.
Local artifacts prove preparation only. The Owner must use the accepted
**merged-main push-CI artifact** for actual publication; a PR merge-ref artifact
or preparation-branch artifact is not a substitute for that source identity.

keys is 0.7.0 because its unchanged launcher source consumes the changed strict
nativeProvenance contract through implementation-identity; both that security
authority change and the packed core 0.22.0 edge are part of its new closure.
Implementation-identity/core consolidation remains deferred in tasks/todos.md.

## Owner commands — Terminal.app, after separate publication approval

These are a command sheet, not commands run by this preparation. Use an exact
clean checkout of the merged release commit. Choose the successful **push** CI
run whose headSha equals that commit, not a pull_request merge-ref run.

```sh
git status --porcelain
git rev-parse HEAD
gh run list --branch main --workflow CI --event push --limit 5
```

Fill these values from that readback; do not use the preparation SHA after merge:

```sh
release_sha='<full merged release commit>'
release_run='<successful push-CI run id for that SHA>'
release_artifacts='/private/tmp/release-0.22.0-main-artifacts'
test "$(git rev-parse HEAD)" = "$release_sha"
gh run view "$release_run" --json headSha,status,conclusion
gh run download "$release_run" -n "release-pack-$release_sha" -D "$release_artifacts"
node scripts/release/publish.mjs --artifacts "$release_artifacts"
```

The dry run must show exactly eight 0.22.0 packages plus keys 0.7.0, no omitted
or already-published candidate, matching manifest sourceGitSha and tarball
hashes. Check the manifest/readback if the planned set differs; do not substitute
versions or bypass the guards. The following command is **Owner only** at a
real interactive TTY for npm write 2FA:

```sh
node scripts/release/publish.mjs --artifacts "$release_artifacts" --execute
```

It verifies account/write-2FA and tag absence, publishes in dependency order,
then performs registry readback and finally creates the annotated v0.22.0 tag.
Local Terminal.app publication does not obtain GitHub OIDC provenance. No
credential or OTP belongs in a report or checked-in file.

Readback commands after publication:

```sh
node scripts/release/registry-readback.mjs --manifest "$release_artifacts/release-manifest.json"
npm view @byok-sdk/core@0.22.0 version dist.integrity --json
npm view @byok-sdk/keys@0.7.0 version dist.integrity --json
npm view @byok-sdk/core dist-tags --json
npm view @byok-sdk/keys dist-tags --json
git show --no-patch v0.22.0
```

Canonical registry-readback checks all nine versions, tarball integrity,
installed closure and official Pi identity; the two npm view examples are
additional spot checks, not the full gate. Registry propagation may briefly
return E404 after successful PUTs. Preserve the publish log, rerun readback when
visible and never republish successful versions. If publication stopped before
tag creation, have the Owner reconcile exact registry/source evidence using
the existing recovery procedure rather than silently creating a tag here.
Only after the script-created annotated tag is verified:

```sh
git push origin v0.22.0
```

This work-package runs none of the execute/tag commands and opens no PR.
