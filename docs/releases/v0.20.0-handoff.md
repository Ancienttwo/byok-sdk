# 0.20.0 / keys 0.6.1 release preparation

> **Update, 2026-09-23.** 0.20.0 and keys 0.6.1 WERE published to npm on
> 2026-09-23 and are the registry's `latest`. Everything below was written
> BEFORE publication and is retained as the historical preparation receipt; it
> is no longer the current state. The current state is the
> [0.20.0 publication record](v0.20.0-publication.md).

Status (as written, before publication): prepared; NOT published. No npm publication, no tag, no GitHub Release,
no `scripts/release/publish.mjs --execute`. This handoff does not authorize any
of them; it states what the Owner must run when publication is authorized.

## Scope and upgrade decision

Source: `main` @ `c61615f8` (the PR #223 merge), plus this preparation branch.
The ten aligned dispatch packages — `@byok-sdk/core`, `@byok-sdk/protocol`,
`@byok-sdk/client`, `@byok-sdk/server`, `@byok-sdk/cloud`,
`@byok-sdk/cloud-dataplane`, `@byok-sdk/ui-runtime`, `@byok-sdk/testkit`,
`@byok-sdk/implementation-identity` and the `byok-sdk` umbrella — move
0.19.0 → 0.20.0, and `@byok-sdk/keys` moves 0.6.0 → 0.6.1. No product logic and
no dependency range changes in the preparation itself — the diff is version
literals plus release documentation.

Version selection follows the authoritative
[pre-1.0 package version policy](../spec.md#pre-10-package-version-policy):

- The dispatch train takes a MINOR, because MINOR covers "any pre-1.0 breaking
  cut" and PR #223 is one: input-preparation wire version 5, record schema
  version 6, capability token `agent-input-preparation-v5`, the
  `counted`→`prepared` / `not_counted`→`not_prepared` renames, `counter_missing`
  and the counter `kind` removed, no dual read.
- keys takes a PATCH. PATCH is limited to "corrections with no new public
  behavior, API, persistence, or security authority", and keys changes none of
  them: `packages/keys` source is unchanged since `v0.19.0` apart from the version literal, and so is the source of
  `packages/core` and `packages/implementation-identity`, its only workspace
  edges. It is re-released only because the same policy requires keys' packed
  and published `@byok-sdk/core` edge to be the exact current dispatch release,
  and the published keys 0.6.0 pins core 0.19.0.

Release notes: [0.20.0](v0.20.0.md). The upgrade requirements are: drain the
in-flight input-preparation rows and then upgrade the cloud and its devices as
a pair (a cross-version in-flight row gets a 422 and stalls that device's whole
mailbox until the other side upgrades); expect the unversioned
`agent-input-preparation` token to be refused everywhere; archive a stale
preparation record log by hand when a device reports
`input_preparation_record_log_unsupported` (exact path and steps are in the
release notes); and move a Host that reads preparation state to the renamed
wire. No automatic migration or live-store conversion is supplied or executed.

## Registry state this preparation rests on

0.19.0 and keys 0.6.0 are PUBLISHED — `latest` on npm since 2026-09-22, built
from the `v0.19.0` tag target `9408ed7b` (the PR #221 merge); see the
[0.19.0 publication record](v0.19.0-publication.md). On 2026-09-23 the registry
listed no `byok-sdk@0.20.0` and no `@byok-sdk/keys@0.6.1`. Reconfirm immediately
before the dry run:

```sh
npm view byok-sdk versions --json
npm view @byok-sdk/keys versions --json
```

## Evidence boundary

| Surface | Evidence / remaining boundary |
|---|---|
| Base and scope | `origin/main` @ `c61615f8`; last published train 0.19.0 / keys 0.6.0. This preparation is version literals + release documentation only. |
| Versions | Ten aligned dispatch manifests at 0.20.0, keys at 0.6.1; the workspace root stays private at 0.0.0 and `@byok-sdk/conformance` stays private at 0.0.0; neither is a release package. |
| Lockfile | The eleven `bun.lock` workspace records were hand-edited to match the manifests (an eleven-line diff); `bun install --frozen-lockfile` accepts it unchanged, and `check:release-graph`'s drift guard is what proves it. |
| Dependencies | Workspace edges unchanged; the Pi pin stays `@byok-sdk/pi-coding-agent@0.86.1001` / `@byok-sdk/pi-ai@0.86.1001`. |
| Generated API | `check:api-surface` matches the committed declaration snapshots; the surface files carry no version literal, so any diff would be a real finding rather than a bump artifact. |
| Local verification | Root required checks plus `check:release-graph` and `check:release-pack` (the last one packs the eleven real tarballs, installs them isolated and runs the real bins). Tails are recorded in the work-package notes. |
| Installed packages | Publication must use the exact push-CI `release-pack-<sha>` artifact for the merged release commit. Locally packed tarballs do not certify the published identities. |
| Registry | No registry mutation has occurred. The dry run rechecks target-version availability. |
| Account / tag / release | No account mutation, upload, tag or Release write. npm account and write-2FA are rechecked only under publication authorization. |
| Product distribution | Salesko pins, Host input-preparation consumers, deployment and the Local Agent binary channel are separate and downstream. |

## Publication boundary after preparation — Owner only

Obtain explicit publication authorization first. Then run
[`deploy/runbooks/release-responsibility.md`](../../deploy/runbooks/release-responsibility.md)
checklist item 5, filled in for this train, from a checkout of the exact merged
release commit:

1. Confirm the CI run for the exact merged release commit is green, including
   its `npm-release-pack` job. Use the `push` run, not a `pull_request` run — on
   pull-request events `github.sha` is the merge ref, so that run's artifact is
   named after a commit that is not `HEAD` and the dry run refuses it.

   ```sh
   gh run list --branch main --workflow CI --event push --limit 1
   ```

2. Download that run's accepted tarballs, with `<sha>` the full 40-character
   commit id:

   ```sh
   git rev-parse HEAD                       # this is <sha>
   gh run download <run-id> -n release-pack-<sha> -D <dir>
   ```

3. Dry run. It refuses unless the frozen `release-manifest.json` names 0.20.0
   and was packed from the current `HEAD`, and unless every tarball the publish
   set needs is present and re-hashes to its recorded sha256. It then prints the
   ordered publish plan with those digests. Nothing is published, read back or
   tagged.

   ```sh
   node scripts/release/publish.mjs --artifacts <dir>
   ```

4. Release — run this yourself, interactively, in Terminal.app. npm's write
   2FA needs a human at a real TTY, so this step cannot be delegated to an
   agent or a non-interactive shell. It refuses if the tag already exists, if
   `npm whoami` reports no account, or if `npm profile get --json` does not
   report `tfa.mode` `auth-and-writes` — there is no override. It publishes each
   tarball in dependency order (`--provenance` only under GitHub Actions OIDC; a
   local release logs that no attestation is attached), reads the registry
   back, and only then creates the annotated `v0.20.0` tag carrying the source
   commit.

   ```sh
   node scripts/release/publish.mjs --artifacts <dir> --execute
   ```

   Without `--otp`, npm prompts for its web 2FA confirmation in the terminal;
   open the URL it prints and approve in the browser whenever it asks. This is
   the route the 0.17.0 and 0.19.0 releases used. To use an authenticator code
   instead, pass it with `--otp`; the script hands the same code to every
   `npm publish`, so enter a fresh one right before starting:

   ```sh
   node scripts/release/publish.mjs --artifacts <dir> --execute --otp <code>
   ```

   If step 7's registry readback fails with E404 right after all eleven PUTs
   returned 202, that is registry propagation (it happened on 0.17.0 and
   0.19.0). Do NOT republish. Wait a minute or two, re-run the readback alone,
   and then create the tag exactly as step 8 would:

   ```sh
   node scripts/release/registry-readback.mjs --manifest <dir>/release-manifest.json
   git tag -a v0.20.0 -m "v0.20.0" -m "sourceGitSha <sha>"   # same message step 8 writes
   ```

5. Push the tag, only after the registry has confirmed the train.

   ```sh
   git push origin v0.20.0
   ```

CI artifacts expire after 30 days; past that, re-run CI on the same commit
rather than repacking locally. After a partial failure, preserve the original
bytes and inspect registry state before anything else; never blindly republish.
Record the outcome in `docs/releases/v0.20.0-publication.md` and update the
README release-status block and row to name 0.20.0 / keys 0.6.1 as published.
Product updater and deployment remain separately owned.
