# 0.17.0 / keys 0.4.3 publication handoff

Status: publication completed. See [verified publication record](v0.17.0-publication.md).
The following is the retained pre-publication handoff; its authentication blocker
and pending actions have been resolved.
The registry account returned E401 (invalid authentication token). Restore npm
login interactively; never retain tokens, passwords, OTPs or login URLs in evidence.

## Frozen source and evidence

- Release source: `f00646d7f82e518f02293923687bdf81f64076d5`.
- [PR #179](https://github.com/Ancienttwo/byok-sdk/pull/179): MERGED; main readback matches source.
- [Branch CI 34276397268](https://github.com/Ancienttwo/byok-sdk/actions/runs/34276397268): 23/23 passed.
- [Main push CI 34277170974](https://github.com/Ancienttwo/byok-sdk/actions/runs/34277170974): 23/23 passed.
- Main artifact: `10076185647`, `release-pack-f00646d7f82e518f02293923687bdf81f64076d5`.
- Manifest SHA-256: `7a4a005bc038a0026d98fc74c9f754b3391a57e3ccefae1a9fa7bdb01c427620`.
- Nine aligned packages at 0.17.0, independently versioned keys 0.4.3.
- Build/typecheck/API/version/release graph/workflow/root scripts passed locally.
  Workspace tests: 3942 passed / 135 skipped.
- Both branch and main frozen-artifact publish dry-runs passed under Node 22.22.0.
  Manifest and package digests are identical between these CI runs.
- Isolated npm consumer installed all ten branch tarballs and ran
  `docs/researches/2026-09-09-g1c-facade/public-consumer-probe.mjs`: passed.
  This evidence applies to the identical main artifacts. It covers host binding,
  selected close/reopen boundaries, immutable offer readback, retention, payload
  conflict refusal and cancellation. Fake-device events are not RuntimeAdapter.start,
  SIGKILL, provider work, AiphaBee wiring or consumer ACK evidence.

Ignored local evidence: `_ops/releases/0.17.0-main-pack/`,
`_ops/releases/0.17.0-branch-pack/`, `_ops/releases/0.17.0-consumer/`,
`_ops/release017-*.log`. Re-download the main artifact when continuing on another host.
These post-acceptance documentation commits do not change the frozen source.

## Exact resume boundary

1. Complete `npm login --registry=https://registry.npmjs.org/ --auth-type=web`.
   Verify account authentication and required `tfa.mode = auth-and-writes` without
   recording credential material. Existing approval authorizes the release.
2. Use a clean checkout at the frozen source above, Node >=22.22 and Bun 1.4.0.
   Download the main artifact into an empty directory:
   `gh run download 34277170974 -n release-pack-f00646d7f82e518f02293923687bdf81f64076d5 -D <artifact-dir>`.
3. Run `node scripts/release/publish.mjs --artifacts <artifact-dir>` again after
   registry authentication, then execute the same driver with `--execute` and
   interactive/fresh OTP as required by the canonical release runbook. Stable
   publication must omit `--tag`; do not repack or change artifact source identity.
4. If all ten uploads succeed but propagation causes readback failure, do not
   republish. Use `node scripts/release/registry-readback.mjs --manifest <artifact-dir>/release-manifest.json`
   after propagation and create the annotated source tag only after successful
   registry readback. For partial publication, the stable driver skips published
   packages; retain the same frozen artifacts and inspect registry state first.
5. Verify all ten identities/integrities/dependency closure/latest tags, push the
   annotated `v0.17.0` tag, create the stable GitHub Release and attach the manifest.
   Read back tag target, release body and asset. Record local 2FA publication honestly;
   no OIDC provenance attestation exists for a local publish.

No npm upload, release tag, GitHub Release, product dependency update, deployment
or real database migration was attempted in this acceptance slice. Product adoption
still requires the official updater to manage the server dependency and separate
host/consumer acceptance. See [release notes](v0.17.0.md) for migration refusal and
remaining composition limits.
