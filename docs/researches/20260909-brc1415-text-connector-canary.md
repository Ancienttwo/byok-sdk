# BRC14/15 text Connector canary — stopped before acquisition

The real canary used literal `@github connector`, current browser model settings, the pinned Oracle 0.20.0 fork and the existing Docker image. Formal observe-revision returned verified exact-revision evidence. Initial authoring updated original Issue177 and refuted the alleged Issue178 coverage gap because `scripts/release/pack-and-smoke.mjs` already exercises the packed umbrella SDK. The single authorized follow-up updated Issue178 metadata while preserving that negative finding. No replacement Issue was created.

The follow-up reply completed in ChatGPT but Oracle kept waiting. Read-only DOM inspection found the latest reply and its finished action controls. Earlier conversation turn1 was virtualized away; the visible turn list started at conversation-turn-2, while the extractor reported the newest conversation-turn-6 as list index4. Scrolling the existing conversation to the top restored earlier turns, and the original Oracle invocation completed with its normal captured evidence. No prompt was resent and no Oracle source or binary was changed.

Adoption and its challenge completed. The materialized two-slot candidate was fast-forwarded onto the dedicated canary target. Issue177 has a canonical planning job, a real bugfix preflight, and a new package-local README regression observed failing twice with the keys boundary passing. The README itself remains unfixed.

The execution offer was initially absent because the local active Sprint marker was absent and workflow routing still targeted main, which has no canary Sprint. Activating the correct Sprint and changing workflow routing to the already-authorized canary branch made the real Engineer offer appear. However, committing this workflow-policy correction after the planning job was frozen changed the protection fingerprint, and the next planning step correctly failed with `planning protection snapshot changed`. This was an operator preparation-order error. The old job and proof must not be relabeled for the new protected-input revision.

The campaign was stopped through the existing state-machine operation. The budget ledger records12 provider calls, zero successful acquisitions, no open reservations and no active step. There was no Docker worker/verifier execution, repair PR, repair merge, Issue closure/cleanup, fresh audit or group acceptance. BRC14/BRC15 are not complete.

Evidence is in the dedicated clone `.canary-scratch/text-connector-*`, `text-slot02-review.log`, `text177-preflight.json`, `text-plan-02-failure.json` and `text-connector-stopped.json`; the tracked pre-fix evidence is `tasks/evidence/brc177-pre-fix.log`. Git owns exact target/publication revisions. Preserve failed observations and the stopped grant.

Any later explicitly authorized continuation must freeze the corrected target routing and active Sprint before minting authority and before a planning job. Continue the same original Issue identities; do not redraw Issues, invent a second defect, rerun Docker infrastructure acceptance, or revive this stopped grant. Read-only preflight must prove that the canonical target, active Sprint, capability binding and intended task-offer inputs agree before spending another model call.
