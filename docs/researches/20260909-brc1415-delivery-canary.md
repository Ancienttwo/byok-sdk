# BRC14/15 delivery continuation: positive admission, failed provision

The continuation reused the original Issues 177 and 178 after verifying the stopped predecessor, unchanged published manifest ancestry, corrected canonical target routing, active Sprint and Engineer binding. Its frozen target was d5fc2da's ancestor 7487d329de893129ed1ce97b3006db7525d71e11. Formal revision observation, authoring, one slot-02 metadata follow-up, challenge and two-slot adoption completed through literal `@github connector` with current browser model settings.

Issue 177 remains a real README namespace-count/import defect. Issue 178 preserves the negative finding that existing packed-install smoke already covers the umbrella entrypoint, seven namespaces and keys exclusion. No replacement Issue or invented gap was introduced.

The new Issue 177 plan reused the unchanged pre-fix regression and failure log. Canonical planning returned `plan_ready`, a stable `execution_ready` offer, and no blockers. This is real positive admission evidence; it does not prove worker execution or delivery.

Acquisition then failed because fleet's default provisioner invoked the absent target-local `scripts/contract-worktree.sh`. Current installed repositories use packaged helpers. Fleet released its own claim; readback found the lease available. The exact failed acquisition was reconciled as observed no-progress, charging one attempted-acquisition turn and zero successful acquisitions. The campaign was stopped through its state machine with no open reservations. It consumed eight provider calls and nine turns. No worker, repair PR, repair merge, automatic cleanup or fresh audit completed.

The bounded blocker repair is tracked by repo-harness's `fleet-packaged-helper` work package. It must pass its own final checks and CI before another frozen execution version is used. Do not revive this stopped grant or relabel its evidence. Any continuation must retain these original Issue identities and the existing failed observations.

Runtime evidence: `.canary-scratch/delivery-{preflight,adoption-result,plan-01,admitted-01,acquired-01,stopped}.json`. Source contract: `tasks/contracts/20260909-1520-brc177-delivery.contract.md`. Git owns the exact adoption and planning revisions. BRC14/BRC15 remain incomplete until actual repair delivery, cleanup and the single group-end fresh audit.
