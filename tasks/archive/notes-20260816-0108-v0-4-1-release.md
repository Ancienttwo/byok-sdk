> **Archived**: 2026-08-16 01:08
> **Related Plan**: plans/archive/plan-20260816-0037-v0-4-1-release.md
> **Outcome**: Completed
> **Lifecycle**: notes
> **Parent Run ID**: run-20260816-0108

# Implementation Notes: v0-4-1-release

> **Status**: Completed
> **Plan**: plans/plan-20260816-0037-v0-4-1-release.md
> **Contract**: tasks/contracts/20260816-0037-v0-4-1-release.contract.md
> **Review**: tasks/reviews/20260816-0037-v0-4-1-release.review.md
> **Last Updated**: 2026-08-16 01:07
> **Lifecycle**: notes

## Design Decisions

- Release all eight aligned dispatch packages at 0.4.1 and keep `@byok-sdk/keys` at 0.1.0.
- Keep `task.complete.document` as the single terminal result authority; the release does not introduce the `truth.records` alternative path.
- Freeze final tarballs only after the release PR is merged, from the exact merged source SHA.

## Deviations From Plan Or Spec

- `bun install --lockfile-only` and `bun install --force --lockfile-only` under Bun 1.3.14 both report a saved lockfile but intentionally do not update the informational workspace `version` fields when the dependency resolution graph is unchanged. `bun.lock` therefore remains byte-for-byte unchanged; package manifests and frozen tarball manifests are the release version authorities.
- The first acceptance preparation correctly rejected the release contract's narrow allowed-path list because the PR also carries the previously accepted implementation commit `a7c7167` and its workflow closeout. The contract now explicitly includes those inherited, already-reviewed paths; no implementation bytes were changed by this correction.
- The second preparation passed every build/test gate but the repeated pack command correctly refused to overwrite the existing fixed rc directory. The contract now checks the fixed rc manifest as an artifact and runs each repeatable pack/install smoke in a fresh `mktemp` directory.

## Tradeoffs Considered

| Option | Decision | Reason |
|--------|----------|--------|
| Publish only changed packages | Reject | It would split the established aligned release train. |
| Publish eight aligned packages | Accept | It preserves one downstream pin and exact workspace dependency translation. |

## Open Questions

- None.

## Evidence Links

- Checks: `.ai/harness/checks/latest.json`
- Run snapshots: `.ai/harness/runs/`
- `bun run check:release-graph`: passed for seven scoped packages plus the `byok-sdk` umbrella at 0.4.1; keys remained 0.1.0.
- `bun run build`: passed.
- `bun run typecheck`: passed after the build completed. An earlier parallel invocation raced `dist` cleanup and was discarded as invalid evidence.
- `bun run test`: 2706 tests passed across the workspace (56 environment-dependent cloud-dataplane tests skipped by their existing contract).
- PR #70 merged as `06f157626a4e882a7f5f09d210918bc8ad0d8f38` after both push and pull-request CI runs passed all 21 cross-platform jobs.
- Final frozen manifest: `_ops/releases/v0.4.1/release-manifest.json`, `sourceGitSha=06f157626a4e882a7f5f09d210918bc8ad0d8f38`.
- Published `@byok-sdk/{core,protocol,server,cloud,client,cloud-dataplane,testkit}@0.4.1` and `byok-sdk@0.4.1`; all eight `latest` tags read back as 0.4.1. `@byok-sdk/keys` remained 0.1.0.
- `node scripts/release/registry-readback.mjs --manifest _ops/releases/v0.4.1/release-manifest.json` passed exact SHA-512 integrity, maintainer, dependency graph, and fresh exact-install/import verification.
- Annotated `v0.4.1` dereferences to `06f157626a4e882a7f5f09d210918bc8ad0d8f38`; GitHub Release is published, non-draft, and non-prerelease at `https://github.com/Ancienttwo/byok-sdk/releases/tag/v0.4.1`.

## Promotion Filter

Promote a candidate to `tasks/lessons.md`, `docs/researches/`, or harness asset files only when all three hold: hard to reverse, surprising without local context, and a real trade-off existed. If any one is missing, keep it in this notes file instead.

## Promotion Candidates

- Promote to `tasks/lessons.md` only after a repeated correction or failure pattern.
- Promote to `docs/researches/` only when it is durable repo knowledge with evidence.
- Promote to harness asset files only after verification across more than one task or fixture.
