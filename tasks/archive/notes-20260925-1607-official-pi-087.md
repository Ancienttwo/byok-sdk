> **Archived**: 2026-09-25 16:07
> **Related Plan**: plans/archive/plan-20260925-0300-official-pi-087.md
> **Outcome**: Completed
> **Lifecycle**: notes
> **Parent Run ID**: run-20260925-1607
> **Archive Projection V1**: `plans/plan-20260925-0300-official-pi-087.md` => `plans/archive/plan-20260925-0300-official-pi-087.md`
> **Archive Projection V1**: `tasks/notes/20260925-0300-official-pi-087.notes.md` => `tasks/archive/notes-20260925-1607-official-pi-087.md`
> **Archive Projection V1**: `tasks/contracts/20260925-0300-official-pi-087.contract.md` => `tasks/archive/contract-20260925-1607-official-pi-087.md`
> **Archive Projection V1**: `tasks/reviews/20260925-0300-official-pi-087.review.md` => `tasks/archive/review-20260925-1607-official-pi-087.md`

# Implementation Notes: official-pi-087

> **Status**: Active
> **Plan**: plans/archive/plan-20260925-0300-official-pi-087.md
> **Contract**: tasks/archive/contract-20260925-1607-official-pi-087.md
> **Review**: tasks/archive/review-20260925-1607-official-pi-087.md
> **Last Updated**: 2026-09-25 05:42
> **Lifecycle**: notes

## Design Decisions

- Sole plan is 0300; 0331 is superseded without deleting any commits. Sole writer Codex took over clean `claude/pi-087-migration@27d28c05`; checkpoint `37a2d0ee` is a transfer source only. The nine-item ruling and supplements are captured in `docs/researches/official-pi-migration/consolidation-ruling.md`.
- A1'' compiles against the real baseUrl, through official streamSimple with a placeholder key and no real credential/transport configuration. Capture fetch must throw and run exactly once; zero/multiple calls fail closed. This preserves upstream-owned compat and OpenAI cache inference. Residual risk accepted by Owner: only if upstream ignores injected fetch and conformance misses it could a placeholder-key request reach the intended provider; refusal/no-billing is the Owner's accepted assumption, not live-verified evidence.
- U2 cuts prompt to one required verbatim Host systemPrompt, including explicitly empty text. No session allocation or renderer copy during compile. No Pi default prompt, local cwd, skills, docs, snippets or guidelines enter D. Tools use only the observed declaration parameter. Salesko must later compose framing and product instructions; this WP does not edit Salesko.
- Envelope v4 uses complete captured D as P(D), residual=[] and no token claim. requestBytes+C remains Host-owned; runtime/compiler identities changed, so Salesko must reissue ruling (ruledResidualKeys=[]) and C after M5.
- Freeze only the first request. Tool-result continuations use actual accumulated context, retry disabled at both layers and at-most-once scoped fetch on every stream, never replaying first D. Accepted Errata 1 E4.4 risk is post-response overflow detection, appended event and alert; tighten only after SummaryJob if separately approved.
- Wire and durable record are 7 with one capability, no dual reads. G1 blank provider and G2 missing object-schema properties are typed refusals. Empty requiredToolsets is admitted on preparation only.
- Direct Pi dependencies are coding-agent, pi-ai and pi-agent-core. The five siblings are exact under lock integrity, runtime installed-file attestation and release/readback convergence. pi-tui's transitive Win32 addon remains part of the installed tree; SEA claims apply only to measured headless JS/resources, not arbitrary interactive Pi installations.

## Deviations From Plan Or Spec

- The compile purity contract was explicitly revised by w2:pB as recorded below; no unapproved guard reduction.

## Tradeoffs Considered

| Option | Decision | Reason |
|--------|----------|--------|
| Official public capture + scoped gate | Use | Preserves upstream serialization without copying it |
| Private import / patch / global env interception | Refuse | Violates approved migration boundary |

## Open Questions

- None.

## Evidence Links

- Checks: `.ai/harness/checks/latest.json`
- Run snapshots: `.ai/harness/runs/`

## Promotion Filter

Promote a candidate to `tasks/lessons.md`, `docs/researches/`, or harness asset files only when all three hold: hard to reverse, surprising without local context, and a real trade-off existed. If any one is missing, keep it in this notes file instead.

## Promotion Candidates

- Promote to `tasks/lessons.md` only after a repeated correction or failure pattern.
- Promote to `docs/researches/` only when it is durable repo knowledge with evidence.
- Promote to harness asset files only after verification across more than one task or fixture.

## 第 6 条编译 purity 修订（w2:pB 明确裁定）

INV-03 定义为 D 与环境无关，且读取集合精确受限。官方 OpenAI 构造器必读 OPENAI_ADMIN_KEY、OPENAI_ORG_ID、OPENAI_PROJECT_ID、OPENAI_WEBHOOK_SECRET、OPENAI_LOG、OPENAI_CUSTOM_HEADERS；读取集合必须恰好这六个，增减都阻止升级。不得全局 monkey-patch env。用污染后 D 不变、prepared 子进程不继承这些变量、header 门零发送共同锁定边界。设备 owner 主动设置 OPENAI_LOG 时，编译可能将请求写入本地日志；接受这个只在本地、不影响 D、不外发的副作用。

## M1–M3 integration evidence

Transferred U2/wire7, envelope v4 full D projection, approved exact OpenAI env-read contract and strict child/header boundaries. M3 now verifies six official package inventories including resolved nested siblings; direct purity guard itself is unchanged. The checkpoint's cryptographically verified tarball/provenance evidence was reused unchanged, not recollected from mutable installed files. Typed provider-id/schema refusals are real tests. Initial typecheck integration is green after rebuilding dependency declarations.

Measured official headers include x-stainless-timeout in actual AgentSession calls (timeoutMs is supplied by Pi); the one-off compile lacks it. Header allowlist records this measured difference rather than accepting arbitrary x-* prefixes.

## Validation discipline / setup correction

The first aggregate candidate run passed 3074 client tests but exposed old fixture expectations and timing failures. Corrected v4 residual and provenance fixtures, preserving raw official manifest bytes; the launch-cwd marker now passes its report path through the test-only argv seam, and asserts the private marker env is absent under the new child allowlist. No product env exception was added. The closure mutation test checks the real full closure, then copies only the mutated telemetry package to exercise the same package verifier; copying all six packages in a test had itself exceeded the unchanged 10 s deadline under load.

A shell workdir mistake briefly applied six fixture/guard edits to the disposable main comparison checkout and invalidated its first build. Those exact edits were restored; git status was clean, and main was rebuilt from untouched 3dd7ba6f before any comparison test. `consolidated-main-build.log` is invalid setup evidence; only `consolidated-main-build-clean.log` is the control build. No user worktree was changed by that setup mistake.

## P1 / P2 / P3

P1: protocol/implementation-identity own wire and record contracts; daemon preparation observes tools and freezes artifacts; prepared-request owns official capture; prepared-session owns context injection and final send; runtime-launch owns the child env; release helpers verify isolated install convergence. Host, fork and provider calls are out of scope.
P2: Host systemPrompt/history → device-observed tools → official capture D → durable v7 artifact → independent envelope verification → context_with_system → official streamSimple → URL/body/header verdict → transport → usage-before-egress. Refusals are recorded before upstream turns throws into generic connection errors.
P3: retain pF public-session/RPC implementation and transplant the approved contract cut plus stronger attestation. The result has one compiler/session path, no dual authority. At 10x, closure verification and full-body storage scale linearly; admission is not weakened to hide that cost.

## Timeout control and deferred test infrastructure

The six files containing pF's 14 timeout-class failures passed on clean TMPDIR with unchanged four-worker config and deadlines: main 3dd7ba6f 86/86 in 38.46s; candidate 86/86 in 40.96s. This supports environment/load sensitivity; it does not prove every possible flake absent. Raw logs are `consolidated-{main,candidate}-timeouts.log` under `/private/tmp/h5-salesko-prep-20260923/`.

pF bisect is LIKELY, not a production-source fix: the huge system TMPDIR delays the compiled fake Codex before boot; daemon.stop can mask the original smoke timeout. Use a new empty TMPDIR for test/release-pack; do not clean system TMPDIR. Deferred todos: test temp-directory lifecycle; preserve the primary smoke failure when stop also fails. Both remain outside this WP, unmodified.

## Consolidated verification before package smoke

All commands below exited 0; raw logs are in `/private/tmp/h5-salesko-prep-20260923/`. Production implementation was frozen before these final results.

- `bun run build`: `consolidated-build-verified.log`.
- `bun run typecheck`: `consolidated-typecheck-pass.log`, all 15 packages.
- Fresh-TMPDIR `bun run test`: `consolidated-test-final.log`; client 253 files / 3094 tests passed, 2 files / 11 tests skipped. Every workspace passed; existing configured skips remain skips. Earlier aggregate failures and corrections remain in `consolidated-test-{1,2}.log`.
- Functional conformance: `consolidated-focused-2.log` (119 passed) and `consolidated-final-focused.log` (67 passed, including exact capture count, empty tools, both retry layers, headers, env, closure mutations).
- `bun run test:scripts`: `consolidated-scripts-1.log`, 37 passed. Release identity units: `consolidated-release-unit.log`, 5 passed.
- `bun run check:api-surface`, `bun run check:version-authority`, `bun run check:release-graph`: respectively `consolidated-api-check.log`, `consolidated-version.log`, `consolidated-release-graph.log`.
- Frozen lock installation and exact integrity parity: `consolidated-frozen-install.log`, `consolidated-lock-attestation.log`.
- `repo-harness run check-task-workflow --strict`: `consolidated-workflow-final.log`, OK.

The first release-pack invocation stopped at the clean-worktree precondition before packing (preserved as `consolidated-release-pack-preflight.log`). Commit the implementation locally before final package evidence; do not bypass the sourceGitSha guard. Independent gate remains pending, M4/M5 and publishing remain unauthorized here.

### Package smoke integration correction

The first clean release-pack reached the isolated Pi launcher and failed with ERR_MODULE_NOT_FOUND: the smoke copied pi-runtime-identity.mjs alone, losing its new relative official-pi-installation/closure imports. The smoke staging now preserves those four oracle files' relative directory structure and resolves runtime imports exclusively from the isolated node_modules. No verifier bypass or product change. Raw failure: consolidated-release-pack-attempt1.log.

## M1–M3 executor completion

Implementation commit 76d5f969; isolated smoke staging correction 6ed5b9fe. Final clean-worktree release-pack on 6ed5b9fe exited 0 using a fresh empty TMPDIR. Raw output: `/private/tmp/h5-salesko-prep-20260923/consolidated-release-pack-final.log`. Official six-package closure and all nested copies matched exact version/integrity/file inventories; fork manifests=0. Actual Pi RPC plus packed root/adapters/CLI MCP probes passed, LLM requests=0. Whole suite passed 5515 tests (141 existing skips), 417 files (30 existing skips).

All M1–M3 executor checks are green. M4, M5, independent acceptance and publication remain pending. The completion commit changes plan/notes only; release-pack sourceGitSha identifies 6ed5b9fe, not the later documentation commit. No expensive full rerun is implied for that documentation-only delta. Temporary-directory leakage and smoke error masking remain deferred, and system TMPDIR was not cleaned.

Final report: `/private/tmp/h5-salesko-prep-20260923/official-pi-consolidated-report.md`. Next action: w2:pB independent gate of M1–M3, followed by a separately assigned M4.

## Independent gate repair round 1

P1: release fixture staging owns the isolated script import graph; root test:scripts owns routine release regression coverage; architecture outputs derive deterministically from the frozen source tree.
P2: beta fixture copies release drivers -> registry-readback imports pi-runtime-identity -> relative official-pi-installation -> official-pi-closure.json. The absent two files caused ERR_MODULE_NOT_FOUND before the missing/latest prerelease-tag refusal. Pre-fix reproduction: node --test scripts/release/beta-release.test.mjs, 5/6 passed, exit 1; raw log consolidated-fix1-beta-before.log.
P3: copy the exact two oracle files at their original relative paths, retain all tag/refusal assertions, and include beta-release plus pack-and-smoke units in test:scripts. No product fallback or guard change. Added explicit v6-only capability refusal before receipts/mailbox writes, corrected obsolete prompt comments, removed an orphan JSDoc. Architecture materialization follows source freeze.

M4 carryover (w2:pB ruling): production does not read gate.state.refusal to propagate continuation refusal reasons; defer implementation and regression to M4. This repair does not change that behavior.

Repair evidence (raw logs under /private/tmp/h5-salesko-prep-20260923/): test:scripts 48/48 passed; node --test scripts/release/*.test.mjs 24/24 passed; cloud input-preparations 13/13 passed; typecheck exit 0. Logs: consolidated-fix1-{test-scripts,release-tests,v6-test,typecheck}.log. The beta test includes the original pre-registry tag refusal oracle, now 6/6 green.

Architecture apply initially refused snapshot drift while the concurrently finishing cloud test wrote its ignored Vitest results cache (consolidated-fix1-projection-drift.log). With all tests finished, serial apply exited 0 and materialized only docs/architecture/.projection-manifest.json and docs/architecture/modules/sdk/sdk-root.md, both explicitly allowed by the contract. No projection guard override. Final check and strict workflow outputs: consolidated-fix1-{projection-check,workflow}.log.

## M4 start / P1–P3

w2:pB accepted fix1 and authorized M4 at f43608e4 (clean). No projection chasing per commit: materialize once at final freeze. No provider calls, push or INV-12 depth changes.
P1: prepared-session owns the gate; pi-prepared-host owns SDK RPC frames; PiSession/events owns runtime-neutral terminal control; TaskRunner projects RuntimeExecutionFailure into task.fail. Recursion/custody/workflow and vendored subagents are separate regression surfaces.
P2: a steer inserts a user tail, context projection rejects it, second fetch records prepared_context_drift but upstream turns its throw into a generic connection error. First-response callback is already consumed, so no typed continuation reason crosses stdout.
P3: emit one closed-code SDK refusal frame synchronously before throwing; map it to the existing non-retryable RuntimeExecutionFailure with the exact code as reason. This preserves current daemon task.fail contract without parsing provider text or adding a public error-field authority. Test actual official Session/adapter refusal and daemon terminal projection.

### M4 functional evidence

- Regression red: m4-refusal-before.log, real official prepared launcher accepted the steer but returned no typed failure (1 failed). After fix: m4-refusal-tests.log, 105/105 across real launcher, mapper, daemon prepared lane and runtime-failure tests. The real-session guard checks RuntimeExecutionFailure(message=prepared_context_drift); the daemon guard checks task.fail reason=prepared_context_drift/retryable=false and no completion. Malformed or unknown refusal frames fail closed without forwarding arbitrary text.
- m4-custody-tests.log: 6 files / 92 passed, including all five physical dispatcher edges, charge-once, runner entry, external CLI admission, runtime descendant plan and sealed workflow launch-schema restrictions. Children are physical capture fixtures, not live model requests. m4-descendant-tests.log: 53 root/parent/transition identity tests passed. INV-12 production code unchanged.
- Added official-pi-workflow-probe.mjs, run by official-pi-workflow.test.ts under Bun as a plain JS fixture (the vendored TS publishes no consumable type declarations; no tsconfig guard was relaxed). Actual scripted workflow Worker launches the real vendored arbiter using official Agent and pi-ai/compat against a loopback SSE server; repeated child key reuses a result, one-use permit is consumed, and root/child/agent/digest/context mismatch, unclaimed/forged/reused permits refuse. A first TS-fixture attempt pulled the upstream TS tree into client typecheck; moving this runtime fixture to plain JS keeps the same executable imports without altering vendor types.
- Separate isolated npm tree: coding-agent/ai/agent-core exact 0.87.1, typebox 1.3.7 and acorn 8.15.0, --ignore-scripts. Bundled the same fixture with packages external, ran Node with --experimental-import-meta-resolve and --expect-nested under a minimal env. m4-npm-probe-final.log passed: pi-ai AND pi-agent-core have two real paths (top-level and coding-agent nested). This is not physical-singleton evidence. The workflow/Agent/compat operation works across those exact instances; opaque permits stay in one SDK/vendor module graph. m4-npm-attestation.log verifies all ten resolved physical roots against the approved six-package file closure.
- First aggregate command accidentally forwarded a file argument through root test; Bun ran the full workspace suite instead of filtering. It passed, but test-fixture setup was still changing; m4-workflow-tests.log is preliminary only. Final code-frozen evidence is m4-test-final2.log with its fresh TMPDIR recorded separately; the first run and unrelated timing failure remain preserved below.


### M4 frozen validation and journal timing observation

Production code was frozen for m4-build.log (exit 0). Final typecheck m4-typecheck-final2.log passed all 15 packages. test:scripts m4-test-scripts.log passed 48/48. Targeted recursion/custody, descendant, refusal and workflow outputs are listed above.

The first final aggregate (m4-test-final.log) failed only journal-offer-family.test.ts:151: expected pending but transport confirmation had already advanced the exact same terminal row to confirmed. Client: 3099 passed / 1 failed / 11 skipped. The test queries the journal after waiting for task.fail, without fencing confirmation; this is an observed timing-sensitive failure, not a completed root-cause proof. No journal source/test or existing timeout was changed. An isolated new-TMPDIR rerun passed 10/10 (m4-journal-recheck.log); then a new empty-TMPDIR full run passed every workspace (m4-test-final2.log): 5522 tests passed, 141 existing skips, 0 failures; 418 files passed, 30 skipped. System TMPDIR was not cleaned. Report this infrastructure observation outside M4 rather than expanding the repair.

Final strict workflow output: /private/tmp/h5-salesko-prep-20260923/m4-strict-workflow.log. Executor report: /private/tmp/h5-salesko-prep-20260923/official-pi-m4-report.md. M4 independent gate is the next boundary; M5/provider probes remain separately authorized work. No release-pack repeat or per-commit projection materialization in this Owner-scoped slice.


## Final closeout after M4 gate / M5

w2:pB accepted M4 at 749d3dca and authorized sequential MEDIUM fixes, latest-main rebase, full verification and final projection/release-pack. P1: only test evidence/reporting and durable package/ruling boundaries change. P2: the daemon projection fixture injects a mapped failure into its stub session; it must assert exactly one task.fail, not imply a real Session-to-daemon end-to-end test. P3: strengthen that count/title; count executed permit refusals (including the workflow-level rejection), and remove the unobserved providerRequests field rather than introduce a global fetch patch. Historical M4 logs' providerRequests=0 was a literal, not a measured egress counter; withdraw any evidence claim based on that field. The loopback request counter remains measured.

npm scope correction: the official coding-agent shrinkwrap produces 10 measured physical roots: pi-ai, pi-agent-core, chord and pi-telemetry each twice; coding-agent and pi-tui once. All are attested, not deduplicated. The built-in workflow/Agent/compat probe does not prove module-registry sharing. pi-ai/compat's apiProviderRegistry is a module-local Map; a future registerApiProvider extension on one copy is invisible to another. Added a bounded todo: resolve and test that public-API boundary before admitting such an extension. No module singleton shim or private import was introduced.

M5: exactly 3 ordinary z.ai coding glm-5.3-flash inferences, no tokenizer, no tool execution, no retry. Bare: requestBytes=171, prompt=13. Prepared: 17638 bytes, prompt=6581. Identical-D repeat: prompt=6581, cached=6528, Pi input=53, cacheRead=6528, cacheWrite=0; cached is included in prompt. Observed max(0,prompt-requestBytes)=0 in all three samples. Prepared margin below bytes is 11057 and below bytes+1024 is 12081. Retain C=1024 under the approved empirical criterion; this is not a universal bound proof.

New binding: official @earendil-works/pi-coding-agent@0.87.1, closureDigest=33ee3e426437c4bfd9fb50c06252c1ccd8196aa97729f93f00e6a14a289e5ab2, compiler=4, wire=7, envelope=v4, C=1024, ruledResidualKeys=[]. Salesko must reissue its ruling revision even though C is unchanged. Toolset schema digest remains sha256:1f689f5c90c0e87f4bbfb7408560e43c6632fef83fe2b0f6722556a222c686c9. Host systemPrompt is the current product prompt (1529 bytes); probe-only executor bindings and synthetic user input are not a production Salesko receipt.

Owner amended the evidence requirement: persist M5 regardless of whether C changes. tasks/runs/20260925-official-pi-c-reprobe.json retains sanitized result, source checksums, runtime binding and shape differences; its .sha256 sidecar binds the exact archived bytes. Credential value and authorization-marker scans passed again before repository write; the credential was read only in memory, never printed or written. Original code/output remains /private/tmp/h5-salesko-prep-20260923/m5-probe/.

Archived M5 evidence SHA256: 264c09a8d7c775d93c12ae158cda0ac1c4ec5f5bd5d15e3e78a9d7ca2c22c68e.


### Latest-main integration and final freeze

w2:pB explicitly ruled that only docs/architecture/.projection-manifest.json conflicts take rebase --ours (already replayed main plus preceding commits); all other conflicts stop. Applied that ruling at f43608e4, then the remaining two commits replayed without conflict. Rebased implementation HEAD: 294c5c6bd40081099eac9a59bd89e3cfcc7b25e8; origin/main: 48cde45f65f29203dffb012463e6c05ee517f95c. Ahead 12 / behind 0; worktree clean and manifest diff versus origin/main empty before this notes update. Main's 11 commits are retained. M5 inference source remains its original recorded subject, with no new provider call after rebase.

Final sequential verification outputs use /private/tmp/h5-salesko-prep-20260923/final-*.log; final report is official-pi-final-report.md there. Build/test results are regenerated after integration freeze, not carried over from the old head. Final architecture apply is generated and committed separately; release-pack is last, from a clean final HEAD, with its sourceGitSha required to equal that HEAD. No push, provider calls or timeout changes in this closeout.


Final API guard caught one stale golden comment: fix1 deleted the orphan context-file JSDoc in client/src/input-preparation.ts, but api-surface/client.d.ts still retained it. The built declaration differs by exactly that one removed comment, no type/API change. Regenerated only the client golden through the existing --update command; no guard weakening. Earlier post-rebase build/typecheck/full test/scripts/release-tests evidence still covers identical production/test bytes; do not rerun that expensive suite for a comment-only golden update. Original failure: final-api-surface.log; corrected guard: final-api-surface-pass.log.


Post-rebase validation completed: build/typecheck pass; new-TMPDIR full suite 5522 passed / 141 existing skips / 0 failed (418 files passed, 30 skipped); test:scripts 48/48; all release tests 24/24; api-surface 9/9 after the one-comment golden correction; version-authority, release-graph and strict workflow all exit 0. Commands, subjects, fresh TMPDIRs, exit codes and raw log paths are retained in /private/tmp/h5-salesko-prep-20260923/final-checks.json. Production/test bytes are unchanged since tested subject 72d5fd75469aec5c2bf3a1d48c4174a5ea01439c; subsequent delta is only the API comment golden and these notes. Final architecture and clean-HEAD release evidence follow in the external final report so recording that evidence does not move the packed subject afterward.
