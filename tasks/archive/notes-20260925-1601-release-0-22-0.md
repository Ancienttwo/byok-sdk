> **Archived**: 2026-09-25 16:01
> **Related Plan**: plans/archive/plan-20260925-1007-release-0-22-0.md
> **Outcome**: Completed
> **Lifecycle**: notes
> **Parent Run ID**: run-20260925-1601
> **Archive Projection V1**: `plans/plan-20260925-1007-release-0-22-0.md` => `plans/archive/plan-20260925-1007-release-0-22-0.md`
> **Archive Projection V1**: `tasks/notes/20260925-1007-release-0-22-0.notes.md` => `tasks/archive/notes-20260925-1601-release-0-22-0.md`
> **Archive Projection V1**: `tasks/contracts/20260925-1007-release-0-22-0.contract.md` => `tasks/archive/contract-20260925-1601-release-0-22-0.md`
> **Archive Projection V1**: `tasks/reviews/20260925-1007-release-0-22-0.review.md` => `tasks/archive/review-20260925-1601-release-0-22-0.md`

# Release 0.22.0 preparation notes

> **Status**: Active
> **Plan**: plans/archive/plan-20260925-1007-release-0-22-0.md
> **Contract**: tasks/archive/contract-20260925-1601-release-0-22-0.md
> **Review**: tasks/archive/review-20260925-1601-release-0-22-0.md
> **Lifecycle**: notes

## Decisions and trace

P1: eight dispatch manifests align to core 0.22.0; independent keys is 0.7.0.
The nine-package public inventory is unchanged. Testkit/conformance remain
private 0.0.0; no umbrella package returns. Manifest authority is projected into
bun.lock, README/spec/runbook and release notes, never inferred from registry tags.

P2: keys -> pi-provider-launcher-core.ts parseImplementationSpawnBinding and
assertImplementationSpawnBinding -> implementation-identity strict identity
parser -> changed nativeProvenance keys. v0.21.0..9fe732e6 has no keys/core source
changes, but identity.ts replaces upstreamBase/forkBuild with tarballIntegrity,
provenanceDigest and closureDigest. Old bindings are refused before credential
use/spawn. Therefore this is a dependency security-authority cutover, not just a
version-string patch. bun pm pack resolves workspace edges from bun.lock;
release-pack must prove keys core/identity edges both exactly 0.22.0.

P3: dispatch and keys take MINOR under the pre-1.0 policy. Leaving keys 0.6.2
would retain old immutable dependencies; 0.6.3 would understate the changed
accepted security contract. At larger install scale stale lockfile or unverified
hoisting assumptions fail first; fresh clone/frozen install and isolated npm
pack checks cover those surfaces without changing topology or guards.

Implementation-identity/core merge stays deferred: it changes public imports,
Node-only security authority ownership, graph/purity guards and package release
closure; that needs a separately approved breaking topology WP. No such move
is required to ship the existing nine-package runtime migration.

## Migration errata

- Public API whitelist adds pi-ai root `.` (already used by the approved runtime).
- M5 shapeDiff.oldEvidence is now repository-relative:
  tasks/runs/20260923-bounded-admission-probe.json. Only this reference changes;
  probe observations, original result/checksum evidence and runtime binding stay
  unchanged. Regenerated archive SHA-256:
  9ed2a8a0fd498266bd42f8797a54c62803fb261aa67890a55a5be16c96349175.
  The previous notes' 264c09... hash identifies the earlier archive before this
  path-only erratum; it is historical, not the new file checksum.

## Boundaries and evidence

Owner authorized sole-writer version preparation, fresh-clone validation,
dry run and push of origin claude/release-0-22-0 only. No PR, publish, tag,
provider call, production deployment or alternate worktree writes.

Final external report: /private/tmp/h5-salesko-prep-20260923/release-0-22-0-report.md.
It will retain commands, exact source and artifact hashes, raw logs and results.
The final pack is sequenced after the last projection commit on clean HEAD;
results are external so recording them cannot invalidate sourceGitSha.

## Fresh-clone verification and Owner follow-up

Committed preparation subject ed1136cc15da736804ae59f589be3b2be89c42c9 was cloned
with git clone --no-local --single-branch --branch claude/release-0-22-0 into
/private/tmp/byok-release-022-clean.GxGenf. Frozen install installed 436 packages;
no root/client direct chord, telemetry or tui links existed. Each command used a
new empty TMPDIR. Build, typecheck, test:scripts (48/48), API (9 goldens), version
authority, release graph and strict workflow passed.

First root test failed only terminal-projection-selection.test.ts:56 (client
3099 pass / 1 fail / 11 skipped). Its two isolated file runs passed. The authorized
root rerun failed only pi-prepared-launcher.test.ts:458 (60s timeout; same client
counts), so execution stopped and raw evidence was reported before further action.
Owner then authorized two isolated launcher runs plus individual remaining-package
tests. Launcher passed 32/32 twice. All 10 non-client packages with test scripts
passed; both example workspaces with tests were also covered. These remaining
workspaces totaled 2422 passed / 130 skipped. Skips include unconfigured live
Postgres integration suites; no live-PG acceptance is claimed locally.

Per w2:pB's explicit ruling, the two aggregate failures are suspected load-sensitive
flakes, associated with the existing CI flake ledger. No source/test/timeout/worker
changes were made. Neither failed aggregate run is relabeled PASS. Final acceptance
still requires exact-head GitHub CI on the release PR.

CodeGraph absence initially changed flow-proof status to unavailable in this new
worktree. Owner confirmed existing authorization for codegraph init -i in release
worktrees. It indexed 1089 files / 21657 nodes / 90270 edges, entirely git-ignored.
The indexed projection check has status=planned, humanActions=[] and refreshSignals=[];
only generated manifest/module projections need updating. No accept was used.
Final apply and its dedicated commit follow this source/notes freeze.

Raw result authorities are release-022-checks.json and
release-022-supplement-checks.json under /private/tmp/h5-salesko-prep-20260923/,
with per-command raw logs, exact HEAD, TMPDIR and durations. That folder also holds
the initial failure report. The final report will supersede its STOPPED status
only after the authorized pack/dry-run/push actually succeeds. Final artifact
checks run after the projection commit; no new tracked report commit follows them,
so sourceGitSha continues to identify the tested clean HEAD.

## Publication closeout (2026-09-25)

The Owner published from the merged-main push-CI artifact, not the preparation
artifact: release commit `0962f14f` (PR #234 merge), run `36089419469`, artifact
id `10844194534`, manifest SHA-256 `ccb62b96…`. The standalone registry readback
passed after a brief propagation gap on client and ui-runtime (nothing was
republished). `publish.mjs` stopped before step 8, so the annotated `v0.22.0`
tag was created by hand with step 8's message (tag object `817b230d` → `0962f14f`).
Full receipt: `docs/releases/v0.22.0-publication.md`.

This contract's Verification Plan was written for preparation. Its
`publish-dry-run` check can no longer pass after publication, because
`publish.mjs` refuses a train that is already published for every package; its
`pack` check targets the preparation out-dir. Revising those two checks for the
post-publication state is an Owner/orchestrator decision and is not done here.

### Verification Plan revised for the post-publication state (2026-09-25)

The orchestrator revised the two checks so `verify-sprint` can pass after
publication:

- `publish-dry-run` is replaced by `registry-readback`:
  `node scripts/release/registry-readback.mjs --manifest /private/tmp/release-0.22.0-main-artifacts/release-manifest.json`.
  This is the canonical post-publication check the 0.21.0 and 0.22.0
  publication receipts already name (`docs/releases/v0.21.0-publication.md`,
  `docs/releases/v0.22.0-publication.md`). The manifest is the merged-main
  push-CI artifact the Owner published from (release commit `0962f14f`,
  SHA-256 `ccb62b96376c2ed4ef8492b8bd0c773a857ea4e2a7d9dfe4990cfc8f40274a75`).
  A dry run cannot pass any more because `publish.mjs` refuses a train whose
  every package is already on the registry.
- `pack` drops `--out-dir /private/tmp/h5-salesko-prep-20260923/release-022-artifacts`
  and runs plain `bun run check:release-pack`, as the archived 0.21.0 contract
  did. The script packs into its own ephemeral directory, so the check no longer
  depends on the preparation artifact dir (which is non-empty and would make
  the script refuse).

No acceptance receipt is recorded and no plan is archived here; both wait for
the Owner waiver.

