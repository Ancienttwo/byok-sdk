> **Archived**: 2026-09-25 13:39
> **Related Plan**: plans/archive/plan-20260925-0331-pi-087-official-migration.md
> **Outcome**: Superseded
> **Source Plan**: (none)
> **Parent Run ID**: run-20260925-1339

# Deferred Goal Ledger

> **Status**: Backlog
> **Updated**: 2026-09-25 (0.22.0 closeout)
> **Scope**: Medium/long-term goals deferred from active plan execution

Current plan tasks live in the active plan's `## Task Breakdown`.
Do not duplicate that execution checklist here. Record only work intentionally deferred beyond this slice, with the tradeoff and revisit trigger.

## Deferred Goals

| Goal | Why Deferred | Tradeoff | Revisit Trigger |
|------|--------------|----------|-----------------|
| 官方 Pi npm 双实例下的 compat provider registry 可见性 | 0.87.1 coding-agent 自带 shrinkwrap，pi-ai、pi-agent-core、chord、pi-telemetry 各两份，共 10 physical roots；M4 只证明当前 built-in workflow/Agent/compat 路径，不承诺跨实例扩展注册共享。 | pi-ai/compat 的 apiProviderRegistry 是模块级 Map；以后若扩展对一份调用 registerApiProvider，另一份不可见。不能以相同版本或 closure 校验冒充 singleton。 | 第一个调用 registerApiProvider 的扩展准入前；入口为 official pi-ai/compat、vendored llm-intent-arbiter 与隔离 npm workflow 探针，先复现注册可见性再裁公开 API 接线，禁止私有导入/全局共享状态补丁。 |
| **Salesko published SDK pin and production journal/recovery rollout** | Candidate composition and crash/restart acceptance now exist for exact SDK/Salesko sources (see `tasks/notes/20260906-pr152-closeout.md`); registry publication and production cutover are separate operator boundaries. | Candidate arm64/x64 recovery and Chat restart evidence does not establish deployed runtime state; external binary compaction remains PARTIAL and append-failure injection SKIP. | After authorized SDK 0.14.0 / keys 0.4.0 publication, switch the production exact pin, complete approved deployment/migration and verify the released Local Agent on the target environment. |
| **Residual downstream `ws/wss` cleanup** | Current reliability issues own long-poll transactions and liveness, not downstream stale URL/config cleanup. Mixing cleanup into this branch would enlarge rollback and acceptance surfaces without helping the durable invariants. | Stale downstream configuration may remain visible, but no WebSocket product authority is reintroduced into SDK source. | A dedicated downstream cleanup slice after #135–#144 acceptance; inventory exact `ws://`/`wss://` consumers and remove them as a one-authority cutover. |
| **context-fold compaction 隔离 PoC**：deterministic 外部 compaction 扩展能否在不破坏 BYOK task/session authority 下替代 Pi native compaction。plan：`plans/plan-20260826-1542-context-fold-compaction-poc.md` | Phase 0 falsifier gate 未过：无可廉价建立的 native-compaction 失败证据。Adapter 边界对 compaction 帧安全 by construction（`compaction_start/end`→`undefined`∈`ROUTINE_PI_EVENT_TYPES`，`events.ts:116`/`pi-adapter.ts:486`，inferred from code）；pi 模型侧 compaction 语义在 fake-pi stub 里结构上无法复现，真证伪须对活 BYOK provider 跑逼近上下文上限的长 session（plan 排除项）。已拍板生产默认永远 Pi native compaction，pi-vcc 淘汰。 | 无 falsifier 却引入新 compaction authority = 投机（anti-pattern #4）。接受"看不见 compaction 对 continuation 的潜在影响"，换不夹带第二套 context/存储/continuation 权威。 | 出现一个 BYOK 场景下 native compaction 的**可复现失败证据**（resume 后状态错乱 / continuation 断裂 / context 丢失 / token 膨胀），或有真实 downstream 消费者要求 deterministic reversible 折叠。触发后按 plan 从 Phase 1 起，context-fold 须 disabled-by-default + task-scoped + 无全局 config，且禁与其它 compact 扩展叠加（`session_before_compact` 单一所有权）。 |
| **P1 — long-lived connector supervisor 与本地 enable/disable authority**：裁定 daemon 是否拥有常驻 connector 进程、启动/崩溃/恢复探测，以及 host-only enable/disable | `connector-readonly-operability` 已交付 content-addressed registry、CAS reload、redacted status 与显式 host lifecycle observation；但 Claude 仍按 task 拥有 MCP 子进程，daemon 没有可诚实观测的常驻进程。 | 没有 host observation 时状态明确为 `unobserved`；SDK 不会谎报 ready/crashed，但也不会自动恢复一个它并不拥有的 connector。 | 第二个真实 long-running connector 或 Gmail 常驻 dogfood 明确要求 daemon supervision 时；先裁进程所有权与 crash receipt，再实现 enable/disable，禁止以 command presence 推断健康或引入 SaaS executable definition。权威边界见 `docs/researches/2026-08-21_raft-toolset-operability-application.md`。 |
| **P1 — MCP tool 级 policy / approval / audit**：给 task/toolset manifest 增加 exact tool allowlist、read-vs-mutation classification、本地 approval gate 与脱敏调用审计 | 当前选择粒度是整个 logical toolset；既有 runtime permission/approval seam 不能等价保证任意 MCP connector tool call 都被拦截。Browser、LinkedIn 发消息、邮件发送等 mutation 需要独立 authority。 | 在只有 read-only Gmail metadata 时可接受；一旦加入 mutation，toolset 全开会扩大本地账号权限与误操作半径。 | 第一个 write/mutation connector 上线前硬触发；同一 slice 交付拒绝路径、approval targeting、secret/body 不落日志守卫，不能只加声明文法。最小范围清单见 `docs/researches/2026-08-20_post-042-progress-and-sprint-audit.md` §7.3 Sprint B2。 |
| `packages/cloud/src/auth/bearer.ts` 与修前的 server 同形状：只查 row==claims，没有 instance-product 等值检查 | hosted 部署的「实例 product 权威」与 embedded 不同——一个 cloud 部署可能合法服务多个 product，没有 `createByokServer({ productId })` 那样的单值权威可比。照搬 server 的第四条检查会把一个尚未裁定的部署形态写死。 | 在裁定前，hosted 侧一个 product 的 token 在同部署内跨 product 面上仍只受 row==claims 约束；embedded 侧已闭合，两侧安全姿态暂不对齐，且这层不对齐只有读代码才看得出来。 | 下一把 cloud auth 刀，或第一个服务多于一个 product 的 hosted 部署——那时先裁「hosted 的实例 product 权威是什么」（单值？tenant→product 映射？），再决定检查落在哪；裁定约束（禁 dual-read/fallback、禁照搬 embedded 单 product 语义）见 `docs/researches/2026-08-20_post-042-progress-and-sprint-audit.md` §7.3 hosted-product-authority。 |
| 落实 review §4 的 P2 文档治理十条，以及 §8 记账的三项 CI 自动化检查：事实快照 SHA 检查（P0-1）、`verify-protocol-fingerprint`（P0-4）、文档引用符号存在性检查（P1-17） | 验收裁定（§8.4）判定 P2 为文档治理方向、不本轮执行；三项 CI 检查都要求扩展 harness checks 才有落点，而本轮修订授权范围是文档内容，不含 harness 能力。 | 文档事实漂移仍靠人工审查发现——这次 review 本身就是证据；P2 的拆分与状态字段缺失让跨章节追踪继续靠通读。接受一轮的代价，换取不在文档修订中夹带 harness 改动。 | harness checks 的下一次扩展（三项检查一并加入 `repo-harness run check-*`），或 canonical 架构文档再次超出可维护长度时启动 P2-2 的拆分。 |
| Journal-cover the `noteSkippedSeq` skip path: `connection-manager.ts:695-702` advances the persisted cursor for unparseable-but-task-classed envelopes without passing through `onEnvelope`, so a hosted daemon acks (and the mailbox retires) unknown `task.*` types from a newer cloud without any durable trace | Found by the S3b acceptance gate (2026-08-08). Pre-existing behavior; `connection-manager.ts` was an explicit contract red line for S3b (zero-diff), and the skip path only fires for envelope types this build cannot execute anyway | The architecture claim "ack hangs behind the journal commit" covers every envelope that reaches `onEnvelope`; the skip path acks without local evidence, which is honest today (nothing executable was lost) but silently discards forward-compat envelopes in hosted mode | A future protocol version bump adding new `task.*` types — that bump should either journal the skip as an explicit `skipped` record or route it through the handler chain via a contract amendment. The S4A branch of this trigger is consumed: S4A-b documents the evidence gap in `deploy/runbooks/mailbox-retention.md` rather than changing `connection-manager.ts`, per `docs/researches/s4a-dataplane-design.md` §10 |
| 裁定 tenant id 的字符集归属：`tenantId()`（`packages/core/src/tenant.ts:53-79`）不约束字符集，而 core 刻意不做正规化（避免与控制面争一源真相），所以 S4A-c 的防御落在 adapter 的 key 构造点（`packages/cloud-dataplane/src/stores/r2-blobs.ts` 的 `assertKeySegmentTenant`） | S4A-c 的授权面是 object plane 与 deploy skeleton；`packages/core/` 是本刀的冻结面（机检），而收紧 core 的 mint 点是 tenant identity 的契约修订，需要控制面一起裁 | 每个把 tenant id 用作路径/key 段的新 composition 都要自己再竖一道守卫，漏一个就是跨租户 alias | 下一个把 tenant id 编进命名空间的 composition（D1 adapter、KV、对象前缀变更），或任何一次 tenant identity 的契约修订 |
| 收敛 composition 混装：`ByokCloudOptions` 对 `cloud` stores 与 `blobContentProxy` 独立接收，呼叫方可把 Postgres/R2 stores 与另一个 proxy 混装并挂上 `/content`（Codex STYLE，S4A-c 双轨验收） | 官方 factory 本身诚实——`createPostgresCloudStores` 不产 proxy，`createInMemoryByokCloud` 两半同源——而把「谁能配谁」提到型别层是 composition API 的形状改动，不是本刀的 P1 修复面 | 「R2 无字节路径」是 factory 层的事实而非型别层的事实：一个手工组装的 host 仍可挂上两条 `/content` route 去代理它其实拿不到的字节 | 出现第二个真实 proxy 实作，或 composition API 下一次修订 |
| 补四条 `packages/conformance` 缺口：pairing code 默认 TTL；已铸 code 的 claims 不可经返回值被改；两个不同 claims 的 code 各自绑定；**删除 device 注册即丢弃其未用 nonce（§6.3，跨 port 不变量）**。来源：WP3B 2b 删除 `pairing.test.ts`/`nonce-store.test.ts` 时对照 `cloud/{pairing,nonces}.ts` 的逐条核对（notes `tasks/notes/20260903-1505-wp3b-step2-facade-fold.notes.md` 2b 节） | Step 2 contract 的 allowed_paths 不含 `packages/conformance`，且这四条是 kernel 行为不是 façade 行为，不该塞回 server 单测 | 在补上前，这四条只有 hub 时代的 server 单测覆盖过，现在无人钉；nonce 那条若回归会让被撤销 device 的挑战继续可用 | 下一把 conformance 刀或 WP3B Step 3（SQLite composition 跑 conformance 时一并补） |
| Wire 级 task history / append-only 审计事件流：capability-gated 的 task 事件契约（UUID id、正整数 seq、eventType、actor `user/agent/system`、payload、createdAt，projection 带 revision），参考 raft-study 1.0.18 delta 的 `taskHistory` client contract（`raft-study/docs/researches/2026-08-21_reverse-raft-computer-1.0.18-feature-delta-report.md` F-003） | 现有 task 状态机 + local journal transition 记录已覆盖执行侧；wire 级 history 是新 protocol surface，需要先裁定 SaaS 侧是否有审计消费者，无消费者不加抽象 | SaaS 端目前无法回放一个 task 的变更轨迹，跨端审计只能靠 daemon 本地 journal，托管场景不可达 | 第一个需要 task 审计回放的 SaaS 消费者出现，或 protocol 下一次 capability 扩版；落地时必须 capability-gated、append-only、server 端 enforcement 与 client 契约同刀交付 |
| WP3 charge-once Windows coverage：custody print preset entry 的 seam spawn 形态（vendor `getPiSpawnCommand` 直接 spawn `PI_SUBAGENT_PI_BINARY`）在 win32 无脚本关联，`custody-charge-once-double-charge.test.ts` 三条 spawn 驱动用例已显式 `it.skip`（win32），POSIX 全绿 | 本切片裁定 POSIX-only 不算弱化；win32 直接 spawn 脚本文件需要 shell/解释器关联，属于 WP4 helper 改道的产物形态问题 | （已闭合 2026-09-18）WP4 五边刀（4de4dcb0/67afe3b3）落地 helper direct-connect argv 形态，`custody-five-edge-dispatch.test.ts` 全部用例跨平台无 skip；seam 本身已从 vendored 树移除，npm 副本驱动的旧 seam 用例 skip 仅历史覆盖 | 已消费（WP4 五边闭合） |
| `BYOK_SDK_CUSTODY_PARENT_DEPTH` / `BYOK_SDK_CUSTODY_LAUNCH_RECORD` 注册进 `TOOL_IMPLEMENTATION_LAUNCH_ENV_LIFECYCLE_NAMES`（`packages/implementation-identity/src/identity.ts:663` 精确枚举，裁定 fail-closed 例外只允许 SDK 自铸名） | 本切片（contract 20260917-1628）allowed_paths 不含 `packages/client/src/__tests__/tool-implementation-identity.test.ts`，而 :810-814 的行为 pin 精确枚举了该数组全部成员，任何新键都会把它打红；运行时本切片无 attested spawn 边界消费这两个名字（exec env 由 entry 精确构造，不含 BYOK_*），注册不阻塞本刀绿 | WP4 前 attested spawn 门（runner 启动 env 复验）会把这两个 SDK 自铸名当成未知 BYOK_* fail-closed 拒掉 | 已消费（WP4 五边闭合刀已注册两键，`packages/implementation-identity/src/identity.ts:672-673`，行为 pin 同步于 `packages/client/src/__tests__/tool-implementation-identity.test.ts:818-820`；2026-09-18 核对） |
| WP4 五边收尾遗留（gate 非阻塞）：① un-pruned closure 使 external-CLI step lane（`runs/shared/{claude-code,codex-exec,cursor-agent}-adapter.ts` 经 `external-cli-runner.ts:330` spawn）首次在 bundle 内可执行，按冻结设计在 custody 链外——需 Owner 裁定产品可达性与未来 custody 处理；② five-edge driver 的 `claims: []` 列表读 `<budget>/claims`，而 fanout claim 实际在 `custody/<rootKey>/<launchId>`，该列表项空转（records/launches/slots 断言不受影响）；③ `pi-sealed-factory-closure.test.ts` 只遍历 manifest 行，无 vendor 文件→manifest 行的存在方向断言 | 三项均为 20260917-2155 契约 gatekeeper r1/r2 非阻塞发现，本刀不夹带 | ①external-CLI 子进程在 SDK bundle 内运行但不受 descendant custody 门控；②③为测试面精度缺口，不掩盖当前绿 | ①已消费（2026-09-18 Owner 裁决终态=B，admission 门见下一条 deferred goal）；② 下次扩展 five-edge 测试时顺手改路径；③ harness checks 下次扩展时加存在方向断言 |
| **B 终态：external-cli 第六条 custody edge 设计包**——charge 定价（adapter 面差异）、per-root cap、DescendantLaunchV1 扩展（新 template kind 与 attested identity 形态）、`external-cli-preflight` 注入缝核实；实现面还须覆盖 vendored append 链：append-request 文件由在跑 runner 直接消费（`chain-append.ts` enqueue/consume），不经 dispatcher admission，SDK 侧无拒绝缝 | Owner 裁决（2026-09-18，plan `plans/plan-20260918-2052-n1-external-cli-gate.md`）external-CLI lane 终态 = B；首刀 admission 门已落地（`custody/external-cli-admission.ts`，dispatcher admission + runner payload 两入口 typed 拒绝），B 设计包需独立契约与 vendored 改动授权 | 门关闭期间 external-cli kind 全域 typed 拒绝——readonly 三 adapter（codex-exec/claude-code/cursor-agent）暂不可用；对在跑 chain 的 append-step 仍可达 external-cli（vendored 内部路径，SDK 面无拦截点） | B 设计包契约开立时：第六边落地后移除 admission 门拒绝、同刀交付 append-lane 拒绝（须改 vendored `chain-append.ts` 或 runner 消费面）与 charge/cap/launch-record 全链 |
| `acceptance.ts:1198 runVerifyCommand`（vendored `runs/shared/acceptance.ts`，`shell: true` spawn 的 acceptance verify 命令面）独立定性待 Owner | 与 external-cli lane 同属链外进程执行面，但语义是 task acceptance 验证命令不是 agent delegation 边；本刀 contract 明确 out of scope（acceptance.ts 面），定性权在 Owner | verify 命令仍可在 custody 链外以 shell 执行任意命令串（受 acceptance 配置来源约束），未定性前维持现状 | Owner 会话裁定 acceptance verify 命令面的 custody 语义（禁用/gate/保留）时开新契约 |
| **桌面端参考前端（Bot 式连续对话）**：给下游 SaaS 二次开发的通用参考桌面 app 模板，放在 `examples/`/`templates/` 下，不作为 npm 包发布。用户按角色创建多个 Bot，每个 Bot 负责一类特定任务（Owner 2026-09-24）。每个 Bot 有自己的身份、Profile、toolsets、model 和 home/MEMORY，各自持有一条 canonical 连续对话；桌面端要提供 Bot 列表、按角色创建 Bot、编辑 Profile。进入某个 Bot 之后，首屏就是它的那条 canonical 连续对话，交互方式同 Muse/Grok bot：清空聊天推进 epoch，不新建会话。会话权威留在 Host，数据经 `GET /agents/:agentId/conversation` 和 `POST /agents/:agentId/inputs` 读写。工具调用、提案、审批以轻卡片嵌在对话流内，默认折叠；设备状态、配对、runtime 设置放侧栏，经 daemon control socket 读取。可复用组件沉到 `ui-runtime`（Owner 2026-09-24 确认保留为白标 UI）。壳：research 推荐 Electron 内嵌 `createDaemon()`（见 `docs/researches/2026-09-24-desktop-frontend-landscape.md`），若有体积上限或 daemon 需独立常驻再改走 Tauri。license 必须 MIT 或 Apache 一类，任何 GPL/AGPL 项目都不能 fork。 | Owner 2026-09-24 决定：本阶段先完成重构和发布（P0 → 官方 Pi 迁移 → SummaryJob），桌面端只落盘、不开工。 | 下游在此之前只能用 SaaS 的 web 前端；Salesko 的 C08 Bot 前端也还没跟上后端新协议。 | P0 与官方 Pi 迁移发布之后，由 Owner 重新排期（两者均已发布：P0 随 0.21.0，官方 Pi 迁移随 0.22.0，2026-09-25）。开工前先做 research 定壳（Tauri/Electron/ACP）和 license，再出设计文档（界面结构、`ui-runtime` 补哪些组件、daemon/Host 接口边界、下游怎么改），设计过了再实施。 |
| **SummaryJob（连续对话摘要任务）——下一刀** | Owner 2026-09-24 定的顺序是 P0 → 官方 Pi 迁移 → SummaryJob（`plans/plan-20260924-1600-p0-prepared-egress.md` Source Ref）；迁移 plan 把 SummaryJob 列为 out of scope，写明它在迁移之后开工并依赖迁移交付的空 toolsets（`plans/plan-20260925-0300-official-pi-087.md`）。 | 在 SummaryJob 落地前，连续对话缺少提前摘要这一环，上下文增长只受准入字节上界约束。 | 0.22.0 已发布（2026-09-25），前置两刀都已完成；由 Owner 开独立 plan，入口 `docs/researches/2026-09-23-continuous-conversation-admission-best-practice.md`。 |
| **P0b — prepared lane 的 native 文件/命令工具与 agent-memory 工具** | P0 的 prepared bot 只带受 ruling 约束的 Salesko MCP（C02 Amendment 2 B4）。Salesko 现用的 `{mode:'auto'}` 在 prepared host 里表达不出来。 | 在补回之前，bot 不能读写本地文件，也用不了记忆工具。 | 官方 Pi 0.87.1 迁移完成后开工，基于官方接口实现，不改 fork。迁移已随 0.22.0 发布（2026-09-25），触发条件已满足，等 Owner 排期。 |
| **P0c — subscription lane（Claude/Codex）的连续对话** | 这两个 CLI 会自己编译请求，没有 prepared lane。P0 在 admission 阶段 typed 拒绝它们（Amendment 2 B5）。 | 订阅用户暂时要切到 Pi 才能用 bot 连续对话。 | P0 之后。做法是 context-pack instruction 加每个 runtime 各自的 ruling 与 C 取证，再用 usage 做事后证伪，需要单独的 amendment。 |
| **Deferred: implementation-identity 并入 core 的包拓扑评估** | 0.21.0 已停发 byok-sdk 总入口包，testkit 已 private；0.22.0 保持九包发布集合。identity 为 Node-only 安全身份 authority，core/keys/client 等已有公开 import 与打包闭包。 | 合并会改变公开入口、消费者 import、包依赖图、纯度及发布 guard，不是版本化机械修改；需要独立破坏性 cutover，不能混入 0.22.0 dry-run。 | Owner 另行批准包拓扑 WP 后，从 packages/{core,implementation-identity,keys}/package.json、api-surface 和 scripts/release/check-package-graph.mjs 开始；本次明确 deferred。 |
| **CI 时序 flake（含待归因项）**：`pi-mcp-launch-cwd.test.ts` 的 10s 超时（通过时也要 7.4–7.5s）；Windows `daemon-auth.test.ts` 的 750ms 防护；Windows 凭据管理器 native smoke 的 20s 超时；Ubuntu strace credential-isolation audit 报 `ptrace(PTRACE_LISTEN): Input/output error`（0.21.0 的 PR #227 靠一次 rerun 转绿，属 runner 环境）；0.22.0 本地干净 clone 全量出现 `terminal-projection-selection.test.ts:56` 终态等待失败与 `pi-prepared-launcher.test.ts:458` 60s 超时，两文件分别独立跑两次均通过，其余包及 example 补跑通过 | 前四项保留原历史判定；新增两项仅归为疑似负载敏感 flake，尚未证明根因。Owner 裁定 report-only，不改测试/timeout；release PR 的 GitHub 全绿仍是最终 CI 边界。 | 会随机把 PR 的 CI 打红，干扰发版判断。 | 发版收尾后单独开一刀：先测量耗时花在哪，再决定放宽阈值还是提速。 |
| **过期测试 `scripts/release/pack-and-smoke.test.mjs`「the repo pins Pi to the published fork the release gates verify」**：断言 `PI_FORK_UPSTREAM_COMMIT` 等于旧 fork commit `d981de12`，而 `scripts/release/pi-runtime-identity.mjs` 现在钉的是 `13cbf77`；该文件不在任何 npm script（`test` / `test:scripts`）里，所以没有门在跑它 | 0.21.0 收尾时发现，只报告不修（report-only），不属于发版收尾范围。 | 手动跑 `node --test` 会失败，误导人以为 Pi pin 漂了；留着不跑等于死代码。 | 下次动 Pi fork pin 或 release 脚本测试时：改成从 `pi-runtime-identity.mjs` 读期望值或删掉该断言，并决定是否把该文件接进 `test:scripts`。 |
| MCP tool `inputSchema` 的准入错配：SDK observation（`packages/client/src/mcp/observation.ts:197` 附近）接受任意非 null object 的 `inputSchema`，而 native compile 要求 `type === "object"` 且 `properties` 是 object；于是一个回答 `{"type":"object"}` 的零参数 MCP tool 过得了 observation，却在 compile 被拒为 `unsupported_input` | 本刀（contract 20260921-0016）scope 明确排除 MCP tool-schema admission；修正准入点是 observation 契约改动，需要先裁定 SDK 是否复述 native 的 schema 要求还是原样透传 | 零参数或非 object-schema 的 MCP tool 在 prepared lane 上是「观测通过、编译失败」的晚拒绝，错误名（`unsupported_input`）不指向 tool schema | 第一个真实零参数 MCP tool 走 prepared lane，或 observation 契约下一次修订；同刀必须裁定单一 authority，不得在 SDK 侧新增第二套 schema 推断 |
| `packages/client/src/bin/pi-prepared-host.ts` 的 `parseModel` 没有 top-level unknown-key 门，而 `daemon/control-protocol.ts` 的 `parseModel` 有（`exactKeys`）；补齐这处不对称 | 该解析器的输入是 daemon 自己已校验过的记录，风险低；本刀优先闭合字段承载与 parity，不夹带解析器形状改动 | 两个手写解析器在「未知顶层键」上的行为不一致，只有读代码才看得出；parity 测试比对的是已知字段，不会打红 | 下一次动 prepared-host 解析器，或 parity 表扩展到未知键维度时 |
| Official-Pi migration 的能力证明必须包含：对一个不透明（projected）provider id 的 prepared compile + consume，以及一个能等于已组合 session model 的 wire model（`thinkingLevelMap`、`compat`） | 本刀选择在 fork 上修，不等迁移（plan trade-off 表已记）；迁移本身是独立 plan，能力清单只能在那里落地 | 若迁移 plan 不带这两条，迁到官方 Pi 后 BYOK profile 会再次在 compile 或 consume 处失败，且失败点与本刀相同 | Official-Pi migration plan 开立或修订能力证明清单时（`plans/Official_Pi_Migration_Execution_Plan_2026-09-19.md` 入口） |
| Fork 侧：在 auth result 里暴露本地配置的 provider `baseUrl`，让 native `prepared_endpoint_mismatch` 不再空转——今天它拿记录自己的 model 跟自己比，等于恒真；属 official-Pi 迁移能力清单 | plan amendment（2026-09-21）decision 5：SDK 侧 consent gate（`packages/client/src/bin/pi-prepared-host.ts` 的 `admitPreparedProviderProjection`）已把端点绑定闭合，而 fork 改动会触及每个 composed provider 的 auth result，且 fork 在退役路径上 | prepared lane 的端点绑定只有 SDK 一层权威；native 检查看起来在守端点，实际不守。迁移到官方 Pi 后若不带这条，这层不对齐会跟着过去 | official-Pi migration plan（`plans/Official_Pi_Migration_Execution_Plan_2026-09-19.md`）开立或修订能力证明清单时，与「不透明 provider id 的 prepared compile + consume」同条；或 fork 下一次动 `composeModelProvider` 的 auth 面 |
| 审计 Claude 与 Codex runtime adapter：凡从 runtime/CLI 名推断模型 vendor 的假设，逐处列出并裁定（用户会把别家 vendor 路由进这两个 CLI，例如 cc-switch） | plan amendment（2026-09-21）decision 7 记为常驻约束，本 work-package 的 allowed_paths 不含这两个 adapter 面；审计要先有清单再裁定单一 authority，不该夹带 | 在审计前，任何按 CLI 名推 vendor 的分支都可能对一个被路由过的 provider 给出错误的 header、计费归属或响应解析——与 prepared lane 曾按 provider 名 `zai` 设门是同一类错误 | 下一次动 Claude/Codex adapter 的 provider/model 解析面，或第一个被路由 vendor 的实际报障；审计结果须落成单一 authority 裁定，不得在 adapter 侧新增第二套 vendor 推断 |
| Fork 侧：带 `samplingParams` 的 `models.json` 条目永久不适用 prepared lane，且失败信息（`prepared_model_drift`）不指明原因；`github-copilot` / `opencode-go` 的字面 provider 名分支在不透明 id 下变为可达 | 两项都是 fork build 7 把 provider id 结构化后被发现的既有形状，不在本刀的修复面；fork 仓另有分支与发布边界 | `samplingParams` 用户只能得到一个不解释原因的 drift 拒绝；字面名分支只影响 headers 与响应解析、不进摘要体，但确实可被调用方以选定 id 触达 | fork 下一次改 `nativeModel()`/`composeModelProvider` 面时，或第一个 `samplingParams` 条目在 prepared lane 上报障时；官方 Pi 迁移落地则两项一并重新定性 |
| 把 `docsPaths` builder 参数与 `buildRequestPayload` 的导出推回上游 `earendil-works/pi` | 两者都是 fork 为 prepared seam 加的最小上游改动（0.86 上 `docsPaths` 改为必填参数、`buildRequestPayload` 在 `streamSimple` 边界导出），但本 work-package 的边界是本仓 + fork 分支，推上游是独立的沟通与发布流程 | fork 每次跟 0.87+ rebase 都要重放这两处；上游若自己动了同一处，冲突要人工裁 | fork 下一次跟随上游大版本 rebase 前，或上游出现同类需求（任何要在 session 之前复现 request body 的调用方）时 |
| Salesko 侧 prepared 请求构造跟进：`snapshot.prompt.formattedSkills` 已被移除，改为 `snapshot.prompt.skills`（`{name, description, filePath, disableModelInvocation}` 闭集）；新增 `snapshot.prompt.toolGuidelines`（`Record<string, string[]>`）；`selection.options.toolChoice` 不再接受 `required`；请求 `version` 为 `4` | 下游集成面不在本仓 allowed_paths，且这是 host 侧的构造改动，需要 Salesko 自己的发布节奏 | 未跟进前，Salesko 发出的 prepared 请求会被 daemon 以 `bad_request`（version）或 unknown-key 拒绝——fail-closed，不会被静默改写 | Salesko 下一次对接 SDK 0.18.x prepared lane 时；本行只记事实，不代替他们的契约 |
| `check:release-pack` 增加与显示无关的 load-time 探针：在模块图加载期间观测任何 `dlopen` 与任何裸 specifier 解析 | 现有的 sealed-host tripwire（`pi-s2-bundle-resolution.test.ts`）只在 darwin 且有 display 时才非零，headless CI 恒零，所以它证明不了「加载期没有原生/注册表逃逸」；而 0.86 把 `config.js`/`paths.js`/`child-process.js` 拉进了 prepared compile 的静态闭包，import-time 纯度现在只靠 fork 的 entry-graph 禁止清单守着 | 在探针存在前，加载期逃逸只有在一台有显示器的 darwin 上才可能被看见；CI 绿不构成证据 | S6 之后的第一把 release-pack 刀，或下一次 fork 跟随上游大版本 rebase（闭包一变就要重新证明）|
| 把 `sessionId` 升格为 Host 显式声明的 compile 输入，使 prepared 请求能携带 `prompt_cache_key` | 0.86 的 prepared option set 强制 `sessionId: undefined`——这同时是 `CacheWarmer` 不可达的机制，也是「没有任何 ambient 输入能移动 D 的字节」的一部分；加一个 Host 声明的 session id 要同时裁定它进不进 consent gate 的比较字段 | prepared launch 在 OpenAI-direct 端点上拿不到 prompt cache 命中，首请求按未缓存计价 | 当 `prompt_cache_key` 的缺失被实测证明有成本（对账数据，不是推测）时；同刀须裁定 session id 的权威来源与它在 `PREPARED_PROJECTION_COMPARED_MODEL_FIELDS` 之外的比较位置 |
| 给被取代的 preparation record log 一条 operator 显式调用的隔离/处置路径（quarantine 或明示丢弃），使跨 record-version 升级的设备不会卡在「daemon 起不来」 | `INPUT_PREPARATION_RECORD_VERSION` 由 `4` 升到 `5` 是本刀的既定 fail-closed 裁定，写迁移读旧记录等于把第二套 record 形状重新放活；而处置路径要定谁有权删用户的 preparation 历史、留不留审计痕迹，是独立的 operator 边界 | Owner 于 2026-09-22 裁定：0.19.0 接受 release notes 里的手工隔离流程（停 daemon → 移走或删除 `<storeDir>/input-preparation/records.jsonl` 与 `artifacts/` → 重启）作为本次的处置方式。适用对象只有跑过仓库自建版本的设备（Salesko 集成开发机）；已发布的 client 不含 store，无存量用户受影响；历史 record 不保留，由操作者自行决定移走还是删除。剩余风险：手工步骤依赖操作者读到 release notes。operator 显式调用的处置路径改为下一次 record-version 变更之前的门槛，不再是本次出仓的前置条件 | 第一台真实设备跨 record-version 升级时，或 preparation store 随任一已发布 client 出仓之前（今天没有任何已发布 client 带这个 store，所以窗口还在）；0.19.0 发布记录 `docs/releases/v0.19.0.md` |
| 对齐 `toolSnippets` / `toolGuidelines` 的 zod record schema 与 daemon 手写解析器对 `__proto__` 自有键的处置：zod 侧今天接受，手写侧拒绝 | 改的是 frozen golden 覆盖到的 wire schema，且 remote lane 只有 zod 一条路径，改法（拒绝 vs 规范化）要与 golden 重生一起裁，不在本刀的验收面内 | 同一份 snapshot 走 remote lane 与走本地 daemon 手写解析器会得到不同结论：一个接受一个拒绝，等于这条 record 有两个语义权威 | 下一次动 `agent.input.preparation` wire schema 或重生 `v1.frozen.json` 时同刀处理；若先出现 host 真的发来 `__proto__` 键的样本则立即提前 |
| 仓内文档与 npm 实况对账：`README.md` release-status 行、`docs/spec.md`、`docs/releases/v0.18.0-handoff.md` 仍称 0.18.0 / keys 0.5.0 为未发布候选，而 npm 上 `@byok-sdk/protocol@0.18.0` 与 `@byok-sdk/client@0.18.0` 自 2026-09-10 起就是 `latest` | README 的 release-status 行是 owner 自己维护的台账，本刀（0.86 prepared 契约）不越界改它；对账要连 handoff 文档的发布状态一并裁定 | 文档说未发布而 registry 说 latest，任何据文档判断「下一版号可以复用 0.18.0」的动作都会撞上已占用的版本号 | 已消费（2026-09-22，release-0.19.0 work-package）。`README.md` release status 段与 release-status 行、`docs/spec.md` 版本政策段、`deploy/runbooks/release-responsibility.md`、`packages/keys/README.md` 均已改为「0.18.0 / keys 0.5.0 于 2026-09-10 从 `v0.18.0`（`7b26ef5f`）发布、当前仍是 `latest`」，并给出 `npm view` 复核入口；`docs/releases/v0.18.0-handoff.md` 顶部加注日期更正、正文保留为历史凭据。下一个 SDK 版本号定为 0.19.0、keys 0.6.0 |
| fork 的 stage 工具在 `dist` 旧于 `src` 时拒绝打包（复用 fork 新纯度门里的陈旧性检查） | `publish-fork.mjs` 今天只校验 `HEAD == sourceHead`，不校验被打包的 `dist` 是否由该 HEAD 构建；`0.86.1001` 的 coding-agent tarball 就构建于 `4e6b207ac`（只改两段 JSDoc）之前，manifest 声称的 sourceHead 与实际字节不完全对应——`dist/bundle` 逐字节相同、行为零差异，所以不为注释重发，但这是 provenance 瑕疵 | 下一次 fork 发布之前 | `/Users/kito/Projects/pi-wt-086` `scripts/fork/publish-fork.mjs`；证据：在 sourceHead 重建后 coding-agent sha512 变为 `xL5X5opM…`，差异仅 `dist/core/system-prompt.{js,d.ts}` 及其 map |
| SDK 纯度门的 fork origin 由 `line:col` 改为文件级（与 fork 侧的门对齐） | `pi-compile-purity.test.ts` 把加载期 allowlist 钉到 `fork:dist/config.js:298:13` 这类行列号；已发布产物不可变所以今天稳定，但每次升 fork pin 都要跟着改，且注释改动就会移动行号 | 下一次升 fork pin 时 | `packages/client/src/__tests__/pi-compile-purity.test.ts`；fork 侧 `originFile()` 的做法可直接照搬 |
| ~~`node:crypto` 的 `getRandomValues` 在 Node 22/24 上是不可配置的 getter-only 属性，纯度门装不上监测~~ 原记载里「不受监测」是误判，已按证据结案 | 属性确实不可直接替换，但覆盖没丢。Node 自己的实现是转发函数 `function getRandomValues(array) { return lazyWebCrypto().crypto.getRandomValues(array); }`，调用时才去查 WebCrypto 方法，所以装在 `globalThis.crypto.getRandomValues` 上的监测覆盖三种入口形状——`crypto.getRandomValues(...)`、监测前捕获的 CJS 引用、监测前绑定的 ESM 具名 import。Node 24.18.0 实测 3 次调用 3 次命中且 forced 值生效，外部复核在 Node 22.16.0 同结果 | 证据是负控制 (e)（`control-getrandomvalues`）：具名 import 在模块顶层绑定并捕获后调用，事件记在 `globalThis.crypto.getRandomValues` 并归因 `fork:dist/core/system-prompt.js`，两个 skew 下分别取到 `0000000000000000` 与 `ffffffffffffffff`，两个 D 互不相同、也都不同于 clean；把该监测安装关掉，控制 (e) 零事件 | 已消费（2026-09-22）。`monitorsAbsent` 保留 `reason: 'unwritable'`，只表示直接 wrapper 装不上；`packages/client/src/__tests__/pi-compile-purity.test.ts` 与 `docs/researches/runtime-input-preparation-contract.md` §25 已改为「直接 wrapper 装不上；经 WebCrypto 转发路径间接覆盖，由定向负控制验证」 |
