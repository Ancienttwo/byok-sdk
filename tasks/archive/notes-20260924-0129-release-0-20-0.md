> **Archived**: 2026-09-24 01:29
> **Related Plan**: plans/archive/plan-20260923-1555-release-0-20-0.md
> **Outcome**: Completed
> **Lifecycle**: notes
> **Parent Run ID**: run-20260924-0129
> **Archive Projection V1**: `plans/plan-20260923-1555-release-0-20-0.md` => `plans/archive/plan-20260923-1555-release-0-20-0.md`
> **Archive Projection V1**: `tasks/notes/20260923-1555-release-0-20-0.notes.md` => `tasks/archive/notes-20260924-0129-release-0-20-0.md`
> **Archive Projection V1**: `tasks/contracts/20260923-1555-release-0-20-0.contract.md` => `tasks/archive/contract-20260924-0129-release-0-20-0.md`
> **Archive Projection V1**: `tasks/reviews/20260923-1555-release-0-20-0.review.md` => `tasks/archive/review-20260924-0129-release-0-20-0.md`

# Implementation Notes: release-0-20-0

> **Status**: Active
> **Plan**: plans/archive/plan-20260923-1555-release-0-20-0.md
> **Contract**: tasks/archive/contract-20260924-0129-release-0-20-0.md
> **Review**: tasks/archive/review-20260924-0129-release-0-20-0.md
> **Last Updated**: 2026-09-23 (publication entry appended)
> **Lifecycle**: notes

## Design Decisions

- **Same two authorities, same three physical copies as 0.19.0.** `packages/core/package.json` and `packages/keys/package.json` moved first; the ten aligned manifests, the eleven `bun.lock` workspace records, and `README.md` + `docs/spec.md` were edited to agree. The `bun.lock` diff is exactly eleven lines, and `bun install --frozen-lockfile` accepted it without rewriting anything.
- **keys takes a PATCH, by the policy.** `docs/spec.md#pre-10-package-version-policy` limits PATCH to "corrections with no new public behavior, API, persistence, or security authority". `git diff --stat v0.19.0 c61615f8 -- packages/keys packages/core packages/implementation-identity` is empty (HEAD differs only by the version literals), so keys' source and the code behind both of its workspace edges are unchanged. The same policy paragraph requires keys' packed and published `@byok-sdk/core` edge to equal the current dispatch release, which the published keys 0.6.0 (core 0.19.0) cannot satisfy. `check:release-pack` shows `byok-sdk-keys-0.6.1.tgz internal @byok-sdk edges all pin 0.20.0`.
- **The runbook pointer was stale before this work-package.** `deploy/runbooks/release-responsibility.md` still called 0.19.0 "prepared" and 0.18.0 `latest` after the 0.19.0 closeout. It now points at the 0.20.0 handoff and names 0.19.0 / keys 0.6.0 as `latest`, with its publication record.
- **The handoff makes web 2FA the primary publish command.** Both 0.17.0 and 0.19.0 published with `--execute` and no `--otp`, answering npm's web 2FA prompt. `--otp <code>` is documented as the alternative. Both routes run interactively in Terminal.app, and the E404 readback-propagation recovery (readback alone, then the step-8 tag) is spelled out because it happened on both previous local publishes.
- **CHANGELOG gets a new top section.** PR #223 added no Unreleased section, so the `0.20.0 / @byok-sdk/keys 0.6.1` section summarizes the release notes and is headed "unpublished until the registry confirms", as the 0.19.0 cut was.

## Deviations From Plan Or Spec

- None recorded.

## Tradeoffs Considered

| Option | Decision | Reason |
|--------|----------|--------|
| keys `0.7.0` minor | Rejected | no keys API, behaviour, persistence or security authority moves; the policy's MINOR clause does not apply |
| Leave keys at `0.6.0` | Rejected | the spec requires keys' core edge to equal the current dispatch release |
| Regenerate `api-surface/` | Not done | `check:api-surface` passed unchanged (10 goldens match) |

## Open Questions

- None.

## Evidence Links

- Checks: `.ai/harness/checks/latest.json`
- Run snapshots: `.ai/harness/runs/`
- Registry at preparation time (2026-09-23): `npm view byok-sdk versions` ends at `0.19.0`; `npm view @byok-sdk/keys versions` ends at `0.6.0`. So `0.20.0` / `0.6.1` are free.
- Verification on the final tree (`70a0098f` plus this notes commit), all exit 0: `bun run build` (`BUILD_EXIT=0`); `bun run typecheck` (`TYPECHECK_EXIT=0`); `bun run test` (`TEST_EXIT=0`; client 3027 passed / 11 skipped, cloud 401, cloud-dataplane 72 passed / 107 skipped, conformance 161, core 304, implementation-identity 110, keys 521 passed / 4 skipped, protocol 402, server 373 passed / 19 skipped, testkit 4, ui-runtime 20, sdk 4, examples 21 + 25; zero failures); `bun run check:api-surface` ("10 package golden(s) match the built declarations"); `bun run check:version-authority` ("agree with byok-sdk@0.20.0 and @byok-sdk/keys@0.6.1"); `bun run check:release-graph` ("10 aligned manifests at 0.20.0, keys at 0.6.1"); `bun run check:release-pack` (eleven tarballs, every internal edge pins 0.20.0 including keys 0.6.1, isolated install closes to exact versions, `npm ls @byok-sdk/core` resolves only 0.20.0, real bins passed; `sourceGitSha 70a0098f`); `repo-harness run check-task-workflow --strict` ("[workflow] OK").
- Known flakes: `pi-mcp-launch-cwd.test.ts` passed inside the full suite, so no isolated rerun was needed. The Windows-only `daemon-auth.test.ts` 750ms guard does not run on this darwin host.

## Publication — 2026-09-23

- Published 2026-09-23 from `main` @ `48605554bc09bfc31ca560ba198e0caa3e637820` (the PR #224 merge). Full receipt: `docs/releases/v0.20.0-publication.md`.
- Artifact provenance: main push CI run `35837426556` (completed / success); artifact `release-pack-48605554…` (id `10739348210`) holding 11 tarballs + `release-manifest.json`; `sourceGitSha` equals the release commit and every tarball re-hashed to its recorded sha256. Manifest SHA-256 `eb8f2de2692b7d1dc3a4ff1c6d109f9a85ddea297be994b662c4c1d444c744f3`.
- Execution: `node scripts/release/publish.mjs --artifacts /private/tmp/release-0.20.0-artifacts --execute` from a detached checkout of the release commit, interactively in Terminal.app with web 2FA, no `--otp`. All 11 PUTs were accepted.
- The script's step-7 readback failed with E404 on `byok-sdk@0.20.0` and `@byok-sdk/client@0.20.0` — registry propagation, the same class as 0.19.0's server E404. Nothing was republished. About 1 minute later the standalone `registry-readback.mjs --manifest …` passed: exact 11-version install closure, `npm ls @byok-sdk/core` resolving only 0.20.0, a single `@byok-sdk/pi-coding-agent@0.86.1001` runtime.
- Registry integrity read back live 2026-09-23: every `npm view <pkg>@<ver> dist.integrity` equals the manifest's `sha512Integrity` and the local tarball's SHA-512; `latest` is 0.20.0 on the ten aligned packages and 0.6.1 on keys. The per-package table is in the publication record.
- The annotated `v0.20.0` tag was created by hand at `48605554` with step 8's message (`v0.20.0` / `sourceGitSha 48605554…`), because the fail-closed step 7 stopped the script before step 8, and pushed. Origin tag object `fcefc54633f41e7b8de34089bf27dfe5aad40cca` → `48605554`.
- Local web 2FA publication carries no GitHub Actions OIDC provenance attestation, as with 0.19.0 and 0.17.0.
- Local evidence: `/private/tmp/release-0.20.0-artifacts` (the downloaded artifact).

## Promotion Filter

Promote a candidate to `tasks/lessons.md`, `docs/researches/`, or harness asset files only when all three hold: hard to reverse, surprising without local context, and a real trade-off existed. If any one is missing, keep it in this notes file instead.

## Promotion Candidates

- Promote to `tasks/lessons.md` only after a repeated correction or failure pattern.
- Promote to `docs/researches/` only when it is durable repo knowledge with evidence.
- Promote to harness asset files only after verification across more than one task or fixture.
