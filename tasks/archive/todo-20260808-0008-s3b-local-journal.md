> **Archived**: 2026-08-08 00:08
> **Related Plan**: plans/archive/plan-20260807-2242-s3b-local-journal.md
> **Outcome**: Completed
> **Source Plan**: (none)
> **Parent Run ID**: run-20260808-0008

# Deferred Goal Ledger

> **Status**: Backlog
> **Updated**: 2026-08-07 22:44
> **Scope**: Medium/long-term goals deferred from active plan execution

Current plan tasks live in the active plan's `## Task Breakdown`.
Do not duplicate that execution checklist here. Record only work intentionally deferred beyond this slice, with the tradeoff and revisit trigger.

## Deferred Goals

| Goal | Why Deferred | Tradeoff | Revisit Trigger |
|------|--------------|----------|-----------------|
| Port the platform-selecting default secret-store factory (`createDefaultLocalAgentSecretStore`, `index.ts:712-740`) including its release-channel service-prefix selection | Named by neither the plan's K1 nor K2 entry. It also carries a branding decision — the source picks the prefix from a vendor-specific environment variable — that needs an owner before it can be ported. | Callers construct `MacOsKeychainSecretStore` / `WindowsCredentialManagerSecretStore` and branch on platform themselves. Nothing is blocked: `ProviderRegistry` takes its `SecretStore` by injection. | K4's aip-main-open swap, which needs a drop-in replacement for the source factory, or any consumer asking for one-call setup. |
| Port the scope data-directory manifest (`prepareLocalAccountDataScope`, `local-data-scope.ts:50-98`): the `0o700` profile directory plus its `scope.json` collision check | Named by neither the plan's K1 nor K2 entry. K1 ported the secret-side scope envelope; this is the filesystem-side partition, which only matters once a caller keeps per-tenant profile databases. | `SqliteProviderProfileStore` takes a path, so a caller partitions by passing a per-tenant path and forgoes the manifest's cross-tenant collision check. | A consumer running more than one tenant on one machine, or K4 if aip's existing profile directories must be honored. |
| Wire `@byok/keys` profile persistence to the `@byok/core` `TruthStore` contract (P5 in `ARCHITECTURE-PROPOSAL-byok-platform.md:698`) | Moved out of `plans/sprints/20260807-byok-platform-raft-aligned.sprint.md` (was story K-501 in S7). It sequences after K4, and K4/K4.1 belong to the sealed K-line active plan, so a sprint proposal cannot append to it. `@byok/core` and its `TruthStore` do not exist yet either, so there is no contract to wire against. | Profile metadata/hash/revision stay local: `SqliteProviderProfileStore` remains the only persistence path, and no profile state is observable across machines. The keys dependency graph stays free of both `@byok/protocol` and `@byok/core`, which is the property this deferral preserves at zero cost. | K4/K4.1 closing out, **and** `@byok/core` `TruthStore` landing (sprint S2). Both are required; either alone leaves nothing to wire. Take it as a new plan, not as an amendment to the K-line plan. |
| Add a generic `ProviderRegistry.testConnection()` (test-before-save at the registry level) | K3 dropped the settings-page server, and with it the only in-package caller. aip's equivalent routes through `createNarrativeProvider().testConnection()`, a §4.5 symbol that stays in aip, so this would be new design rather than a port — and K3 was authorized as a docs-only slice, so adding a registry API was out of scope. | Hosts verify a key today by resolving a client and calling `testConnection()` on it, which is two calls instead of one and requires the host to know the client types. | K4.1's aip-side adapter, or the first host UI that needs test-before-save without reaching for the client. |
| Close the long-poll auth/validation asymmetry: WS upgrade validates `protocolVersions`/`productId`/`deviceId` via the `conn.hello` gate (`ws-server.ts`), while the long-poll HTTP endpoints (`http.ts:206/255`) only run `authenticateBearer` | Discovered during S0's D-4 investigation (deep-reasoner track, 2026-08-07). Independent of GAP-002: the steer-capability fix moved to `task.claim.capabilities`, so S0 does not touch transport validation. Folding it into S0 would widen a bounded amendment. | Long-poll-only devices skip protocol-version and product equality checks that WS devices must pass; bearer-token claims still bind device identity, so the gap is version/product skew tolerance, not an auth bypass. | S1's tenant cut (T-005 already adds `conn.hello.productId` equality) — decide there whether long-poll needs an equivalent registration/validation surface, or an explicit documented waiver. |
| Add `packages/core/README.md` and `packages/core/LICENSE` so the package's files match the publish metadata already in `packages/core/package.json` (`license`, `publishConfig`) | The S2 contract is the contract layer plus the InMemory reference; it explicitly does not include publishing `@byok/core`. The metadata was written with the manifest, the files that metadata promises were not. | `npm pack` would ship a package declaring a license it does not carry and no readme, so the inconsistency stays invisible until the first publish attempt rather than failing now. Nothing in-repo depends on either file. | `@byok/core`'s first publish, or S7 release preparation — whichever reaches the registry first. Pair it with the same check for any other package carrying `publishConfig` without a LICENSE. |
| Tenant-scope the operator surface: `ByokServer.machines.list()` (`index.ts:213-214`, `hub.ts:1768`) enumerates all tenants' devices and `MachineInfo` (`types.ts:159-166`) carries no `tenantId` | **Ruled 2026-08-07 (S2): the embedded operator surface stays host-global by design.** In embedded mode the hosting SaaS *is* the control plane and owns every tenant, so a flat `machines.list()` is the correct authority shape — there is no second principal for it to over-expose to. Tenant-scoped operator surfaces belong to `@byok/cloud` (S3+), built on the tenant-first ports `@byok/core` landed in S2; `@byok/server` is not widened for them. Original deferral: found by the S1 acceptance gate (2026-08-07); not a wire exposure — no HTTP route serves machines. | The embedder cannot build a per-tenant dashboard or filter machines by tenant; a multi-tenant host sees one flat list and filters client-side. Accepted as the embedded-mode cost. | **Closed — no revisit.** Hosted multi-tenant operator views arrive with `@byok/cloud` on the core ports (S3+), not by changing `ByokServer.machines.list()`. Reopen only if embedded mode ever serves a principal that is not the host itself. |
| S3b first-commit fixes from the S3a gate (P2-1/P2-3/P2-4): correct the "verbatim, as the device sent it" comment at the receipt seam (`packages/cloud/src/cloud.ts:105`, `inbound.ts:39-42` — it is a canonical re-encode under the frozen v1 codec, and S3b's journal will share this idempotency seam), add an unwrapped-fetch guard (`expect(cloud.fetch).toBe(registry.fetch)`), and fix the GAP-015 landing label `S3` → `S3b` in `docs/architecture/sdk-architecture.md` | Found by the S3a acceptance gate (2026-08-07); none affect wire semantics, tenant isolation, or the statelessness red line, and S3a was already merge-clean | One-line-level corrections ride along instead of blocking a green slice | S3b's opening commit (the journal slice touches the same files and docs) |
| Journal-cover the `noteSkippedSeq` skip path: `connection-manager.ts:695-702` advances the persisted cursor for unparseable-but-task-classed envelopes without passing through `onEnvelope`, so a hosted daemon acks (and the mailbox retires) unknown `task.*` types from a newer cloud without any durable trace | Found by the S3b acceptance gate (2026-08-08). Pre-existing behavior; `connection-manager.ts` was an explicit contract red line for S3b (zero-diff), and the skip path only fires for envelope types this build cannot execute anyway | The architecture claim "ack hangs behind the journal commit" covers every envelope that reaches `onEnvelope`; the skip path acks without local evidence, which is honest today (nothing executable was lost) but silently discards forward-compat envelopes in hosted mode | A future protocol version bump adding new `task.*` types, or S4A's mailbox retention work — whichever lands first should either journal the skip as an explicit `skipped` record or route it through the handler chain via a contract amendment |
