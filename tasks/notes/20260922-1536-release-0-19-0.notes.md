# Implementation Notes: release-0-19-0

> **Status**: Active
> **Plan**: plans/plan-20260922-1536-release-0-19-0.md
> **Contract**: tasks/contracts/20260922-1536-release-0-19-0.contract.md
> **Review**: tasks/reviews/20260922-1536-release-0-19-0.review.md
> **Last Updated**: 2026-09-22 (publication entry appended)
> **Lifecycle**: notes

## Design Decisions

- **Two authorities, three physical copies.** `packages/core/package.json` and `packages/keys/package.json` are the only version authorities; the ten aligned manifests, the eleven `bun.lock` workspace records, and `README.md` + `docs/spec.md` are all projections of them. The bump therefore moved the authorities and then made every projection agree, rather than editing any document to a number of its own.
- **`bun.lock` is hand-edited.** `bun install` does not rewrite the workspace `version` records on a version-only bump, and `bun pm pack` resolves `workspace:*` from those records rather than from the manifests. The eleven records were edited directly; `bun install --frozen-lockfile` afterwards reported "no changes", and `check:release-pack` proved the packed edges: every tarball's internal `@byok-sdk` edges pin `0.19.0`, including `byok-sdk-keys-0.6.0.tgz`.
- **`v0.18.0-handoff.md` gets a correction note, not a rewrite.** It is the historical preparation receipt for a train that has since been published; rewriting its body would erase the evidence that the repo drifted from the registry. A dated block at the top states the current fact and links the 0.19.0 handoff; the body keeps its original wording with the status line marked "as written, 2026-09-11".
- **The `CHANGELOG.md` 0.18.0 heading was corrected too.** It read "unpublished release candidate" for a train that is `latest` on npm. The section body is untouched.
- **No fresh empty `## Unreleased` heading.** The file's own precedent is the 0.18.0 cut (`aa049307`), which renamed the top section in place and left no empty Unreleased behind; this cut follows it.
- **keys takes a MINOR by the policy, not by preference.** `docs/spec.md#pre-10-package-version-policy` makes MINOR cover "any pre-1.0 breaking cut", and `--runtime-entry {pi-rpc, pi-prepared}` is now required at the launcher parser. `check:version-authority` and `check:release-graph` do not force a specific number — they only require keys ≠ the train version — so the number is a human decision and is flagged for the Owner in the handoff and the PR body.

## Deviations From Plan Or Spec

- The plan's File Changes table did not list `CHANGELOG.md`'s 0.18.0 section heading as an edit. It was corrected in the same commit as the release cut: it asserted "unpublished release candidate" about a published train, which is the same drift the work-package exists to close.

## Tradeoffs Considered

| Option | Decision | Reason |
|--------|----------|--------|
| Regenerate `api-surface/` with `-- --update` | Not done | `check:api-surface` passed unchanged (10 goldens match), so there was no diff to justify |
| Rewrite `v0.18.0-handoff.md` | Rejected | dated correction note preserves the receipt |
| Leave an empty `## Unreleased` heading | Rejected | the file's own 0.18.0 cut did not leave one |
| keys `0.5.1` patch | Rejected | the authoritative policy classifies a breaking argv as MINOR |

## Open Questions

- The keys `0.6.0` minor needs the Owner's explicit confirmation; the approval of 2026-09-22 named the SDK train. Nothing else in the preparation depends on the answer.

## Evidence Links

- Checks: `.ai/harness/checks/latest.json`
- Run snapshots: `.ai/harness/runs/`
- Verification, all on the final tree, all exit 0: `bun run build`; `bun run typecheck`; `bun run test` (`TEST_EXIT=0`; client 2999 passed / 11 skipped, cloud 397, cloud-dataplane 72 passed / 107 skipped, conformance 161, core 304, implementation-identity 110, keys 521 passed / 4 skipped, protocol 397, plus server/testkit/ui-runtime/sdk/examples — zero failures); `bun run check:api-surface` ("10 package golden(s) match the built declarations"); `bun run check:version-authority` ("agree with byok-sdk@0.19.0 and @byok-sdk/keys@0.6.0"); `bun run check:release-graph` ("10 aligned manifests at 0.19.0, keys at 0.6.0"); `bun run check:release-pack` (eleven tarballs packed, isolated install, real bins; `sourceGitSha 2e9c95bc`); `repo-harness run check-task-workflow --strict` ("[workflow] OK").
- Known load-order flakes `pi-mcp-launch-cwd`, `sdk-reserved-helper-host`, `pi-rpc-host`: green inside the full suite AND green re-run alone — `bunx vitest run` on the three files, 3 passed / 30 tests, `ISOLATED_EXIT=0`.

## Publication — 2026-09-22

- Published 2026-09-22 from `main` @ `9408ed7b80ffb0f7ab21f5d82f50782f18074acb` (the PR #221 merge). Full receipt: `docs/releases/v0.19.0-publication.md`.
- Artifact provenance: main push CI run `35711372146` (completed / success); artifact `release-pack-9408ed7b…` (id `10686497675`) holding 11 tarballs + `release-manifest.json`; `sourceGitSha` equals the release commit and every tarball re-hashed to its recorded sha256. Manifest SHA-256 `074c331325ba47e49d7d4349693f7ebd0f8fa178b6d39dcf897f4599c212c072`.
- Execution: `node scripts/release/publish.mjs --artifacts <dir> --execute` from a detached checkout of the release commit, interactive web 2FA, no `--otp`. All 11 `npm publish` calls returned PUT 202.
- The script's step-7 readback failed with E404 on `@byok-sdk/server@0.19.0` — registry propagation, the same class as 0.17.0's ui-runtime E404. Nothing was republished. After ~1.5 min all 11 versions were visible and `registry-readback.mjs --manifest …` passed: exact 11-package closure, `npm ls @byok-sdk/core` resolving only 0.19.0, a single `@byok-sdk/pi-coding-agent@0.86.1001` with `byokFork.upstreamCommit=13cbf77…`, every `dist.integrity` equal to the manifest's `sha512Integrity`.
- `latest` now points at 0.19.0 on the ten aligned packages and 0.6.0 on keys. `@byok-sdk/implementation-identity` is a first publication; the package did not exist on npm before.
- The annotated `v0.19.0` tag was created locally at `9408ed7b` exactly as `publish.mjs` step 8 does; `git push origin v0.19.0` is the Owner's step and is pending.
- Local web 2FA publication carries no GitHub Actions OIDC provenance attestation, as with 0.17.0.
- Ignored local evidence: `_ops/releases/0.19.0-*`.

## Promotion Filter

Promote a candidate to `tasks/lessons.md`, `docs/researches/`, or harness asset files only when all three hold: hard to reverse, surprising without local context, and a real trade-off existed. If any one is missing, keep it in this notes file instead.

## Promotion Candidates

- Promote to `tasks/lessons.md` only after a repeated correction or failure pattern.
- Promote to `docs/researches/` only when it is durable repo knowledge with evidence.
- Promote to harness asset files only after verification across more than one task or fixture.
