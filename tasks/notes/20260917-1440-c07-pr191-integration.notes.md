# PR191 integration notes

PR190 candidate114fdbd3 merged without text conflicts. Final base will be its main merge before canonical verification. Direct integration gap: remote MCP probe lacks cwd binding and executor launch argument; regression-first repair follows the existing resolver.

## Root Cause Evidence
- root_cause: Remote observation callback inherited daemon cwd and its returned authority omitted mandatory launch attestation.
- repro: Actual daemon callback drives a real stdio fixture: configured trusted cwd not used; writable configured cwd still spawns. 2 failed /6 passed. Root build also identified missing required ToolExecutorsRequest.launch.
- regression_guard: packages/client/src/__tests__/create-daemon-mcp-launch-cwd.test.ts
- pre_fix_failure_artifact: _ops/pr191-integration/cwd-pre-fix-evidence.log, raw log and SHA retained.

Repair resolves configured trusted cwd once before probes, passes it to real spawn and returns direct-spawn attestation. Remote fingerprint requires and consumes that exact observation binding. No fallback path or inferred launcher. Unknown/unavailable observation still follows existing toolsets_unobservable completion behavior. Synthetic remote compiler fixtures explicitly supply their synthetic launch authority; separate real daemon child readback proves production wiring.
