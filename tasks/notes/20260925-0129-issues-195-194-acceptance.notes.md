# Implementation notes

## Subject and scope

Base: `8b7a2121` (latest fetched main); branch `codex/issues-195-194-acceptance` in an isolated worktree. Issues read in descending order, #195 then #194. Existing other worktrees and open PRs were preserved. No relevant Obsidian project index exists; the repository is authority. No Salesko, aiphabee, production or paid-provider actions were taken.

## P1 / P2 / P3

P1: SDK cloud/server owns offers and receipt read-back; TaskRunner owns fresh sessions and home exclusion. Host owns transcript, ContextPack, SummaryJob and CAS. Provider owns token semantics.

P2: The existing successful Summary test began at internal execution and injected fixture text into the dependent turn. It now includes the preceding real SDK user execution, obtains the accepted reply and Summary through public read-back, submits frozen next input, verifies same-home/fresh sessions and reopens SQLite to compare complete original instruction bytes and both user messages.

P3: No product implementation or wire/API changes. The fixture deliberately does not implement a Host store/coverage/CAS or assert native tool isolation. Research found #194's mandatory-counter wording superseded by the approved bounded-admission work; current spec/code already own optional-counter readiness and post-hoc usage. Updated the stale research entrypoint and mapped remaining Host obligations instead of duplicating accounting.

## Verification

- `bun install --frozen-lockfile`: PASS.
- `bun run build`: PASS for all workspaces. Built code is unchanged by this test/documentation diff.
- Focused `vitest run` for recurring integration, input preparation, prepared offer lane and Pi events: 145 PASS.
- Final recurring integration rerun after the input type guard and fixture/assertion adjustments: 4 PASS.
- `bun run typecheck`: PASS. The first run caught the offer union's prepared variant lacking `instruction`; the final test explicitly checks for a string before comparing bytes.
- `bun run check:api-surface`: PASS, 9 goldens match.
- `bun run check:version-authority`: PASS, SDK 0.21.0 / keys 0.6.2.
- `repo-harness run check-task-workflow --strict`: PASS using the same installed regular package through `REPO_HARNESS_SOURCE_ROOT`. The inherited root points at helper symlinks rejected by the runtime; no global installation was changed.
- Full `bun run test`: PASS, 5460 passed / 141 skipped. Skipped platform/substrate cases are not acceptance. It started before the final recurring-only assertion change; the final focused rerun covers that delta.
- `git diff --check`: PASS.

Raw command logs are local `/tmp/byok-issues-195-194-*.log`; they are not shipped as provider/Host acceptance artifacts. Build/API/version evidence may be reused because production inputs and declarations are unchanged. Native provider and installed Host acceptance were not run. No issue is auto-closed.
