# Implementation notes

## Subject and scope

Base: `8b7a2121` (latest fetched main); branch `codex/issues-195-194-acceptance` in an isolated worktree. Issues read in descending order, #195 then #194. Existing other worktrees and open PRs were preserved. No relevant Obsidian project index exists; the repository is authority. The initial SDK slice used no Host checkout. The continuation validates one isolated Salesko candidate read-only; no aiphabee, production or paid-provider actions were taken.

## P1 / P2 / P3

P1: SDK cloud/server owns offers and receipt read-back; TaskRunner owns fresh sessions and home exclusion. Host owns transcript, ContextPack, SummaryJob and CAS. Provider owns token semantics.

P2: The existing successful Summary test began at internal execution and injected fixture text into the dependent turn. It now includes the preceding real SDK user execution, obtains the accepted reply and Summary through public read-back, submits frozen next input, verifies same-home/fresh sessions and reopens SQLite to compare complete original instruction bytes and both user messages.

P3: No product implementation or wire/API changes. The fixture deliberately does not implement a Host store/coverage/CAS or assert native tool isolation. Research found #194's mandatory-counter wording superseded by the approved bounded-admission work; current spec/code already own optional-counter readiness and post-hoc usage. Updated the stale research entrypoint and mapped remaining Host obligations instead of duplicating accounting.

## Verification

- `bun install --frozen-lockfile`: PASS.
- `bun run build`: PASS for all workspaces. Built code is unchanged by this test/documentation diff.
- Focused `vitest run` for recurring integration, input preparation, prepared offer lane and Pi events: 145 PASS.
- Final recurring integration rerun after the input type guard and fixture/assertion adjustments: 4 PASS.
- `bun run typecheck`: PASS. The first run caught the offer union's prepared variant lacking `instruction`; the final test explicitly checks for a string before comparing bytes.
- `bun run check:api-surface`: PASS, 9 goldens match.
- `bun run check:version-authority`: PASS, SDK 0.21.0 / keys 0.6.2.
- `repo-harness run check-task-workflow --strict`: PASS using the same installed regular package through `REPO_HARNESS_SOURCE_ROOT`. The inherited root points at helper symlinks rejected by the runtime; no global installation was changed.
- Full `bun run test`: PASS, 5460 passed / 141 skipped. Skipped platform/substrate cases are not acceptance. It started before the final recurring-only assertion change; the final focused rerun covers that delta.
- `git diff --check`: PASS.

Raw command logs are local `/tmp/byok-issues-195-194-*.log`; they are not shipped as provider/Host acceptance artifacts. Build/API/version evidence may be reused because production inputs and declarations are unchanged. Native provider acceptance was not run. Installed Host candidate verification is recorded in the follow-up below. No issue is auto-closed.

## Follow-up: exact Host candidate

Validated Salesko `3d48da3702e8833962b6d6a42df3f76b4d7dff11` in a clean isolated checkout. Frozen install, package build and API/control/local-agent/contracts typechecks passed. Registry SDK resolution: client/cloud/protocol 0.21.0, keys 0.6.2; lock SHA-256 `69503df38f82368619301ec245fa88b7dfcc3c19f341a48f3b26da1a55778454`.

Focused contract, ContextPack, preparation, preparation-lane, budget-observation, control preparation/chat/tools suites: 168 passed / 748 assertions. Installed-SDK + real-PG arbitration/recovery rehearsal passed with zero provider starts. Execution migration rehearsal: 53 checks passed. D2 tools rehearsal: 10 tests / 64 assertions passed. Logs: `/tmp/salesko-conversation-{build,focused,arbitration,migration,tools}.log`. Initial narrow attempt could not locate `initdb`; final commands prepend `/opt/homebrew/opt/postgresql@18/bin` and use disposable fixture-owned PostgreSQL 18.4 clusters.

This candidate already implements main accounting/fit/falsification and prepared execution. It is not mainline/deployment or native-provider proof. The current Host contract explicitly excludes a production Summary writer/job; no unapproved budget defaults or writer were added. The user was asked to choose the next fail-closed configured local slice or acceptance-only boundary.

## Follow-up: CI substrate root cause

- Symptom: both PR/push dataplane jobs failed at compose startup before any tests; the other 44 checks succeeded on `d1cdb10d`.
- Cause: the existing Quay MinIO release returned unauthorized; one failed-job rerun reproduced it. Anonymous Quay and Docker Hub manifests return 401; the official binary archive returns 410. This is independent of the conformance test change.
- Pre-fix evidence: GitHub run `36035767781`, attempts 1 and 2 (jobs `107755462367`, `107763667352`); same compose on main had previously passed. No failure was relabeled as test acceptance.
- Change and regression proof: build the same MinIO release from official commit `0d7408fc9969caf07de6a8c3a84f9fbb10a6739e`, SHA-256-pinned codeload archive and Go 1.24.2; retain the shared compose substrate. Readiness uses the server's HTTP endpoint. No alternate S3 implementation or registry credentials. Local image build and startup passed, runtime version/commit read-back matches. Real dataplane verification follows below.

Local first build compiled in about 140 seconds after dependency fetch; hosted builds have no persistent cache and incur compilation each time. The image retains the upstream license and source/revision labels. Only the owned `byok-issues-195-194` compose project is used; other Docker containers are untouched.

Local substrate verification: `cloud-dataplane` **338 tests / 35 files passed**, with `BYOK_REQUIRE_DATAPLANE=1`, real Postgres/S3 endpoints and both Worker flags enabled. In-memory `conformance`: **161 tests / 4 files passed**. Local runtime is Node 22.22.0 (supported); hosted CI selects repository-pinned 22.22.3. Logs: `/tmp/byok-issues-{minio-build,substrate,dataplane,conformance}.log`. Packed restart smoke correctly refused an uncommitted worktree; it will run after the source commit, without weakening its exact-SHA guard.

Final local packed verification: `check:release-pack` **PASS** on clean commit `a04c6a8f9cc6eefe95e5b04cf91ab789ba53093a`, with the same required Postgres/S3 environment. The driver runs recurring smoke from the installed tarballs and inherits those environment gates, so its durable restart/COMMIT-crash replay leg cannot silently skip. The provider is synthetic; no model request was made. Manifest and nine tarballs are retained under `/tmp/byok-issues-release-pack`, with source SHA, versions and per-tarball SHA-256/integrity; log `/tmp/byok-issues-release-pack.log`. The subsequent handoff commit changes only evidence documents. Strict task workflow and diff whitespace checks passed. Hosted CI status is recorded on PR #228 rather than inferred from local success.

## Mainline reconciliation and installed no-tool blocker

Latest fetched main `3dd7ba6f` includes release closeout PR #229 and the S3 substrate repair PR #230. This branch takes that canonical SeaweedFS compose definition and removes its superseded MinIO Dockerfile; no second substrate authority remains. Earlier MinIO command evidence describes the earlier commit only. Final hosted checks must bind the reconciled head.

The approved follow-up local SummaryJob encountered an actual prerequisite: installed SDK 0.21.0 rejects `requiredToolsets: []` in `AgentInputPreparationPayloadSchema`, and the installed Pi fork's `createPreparedAgentSession` rejects `options.tools.length === 0`. SDK spec lines 555–557 and release notes explicitly defer this to the separate official Pi migration while freezing fork 0.86.1001. The older Host amendment's intended empty-tool contract is not implementation evidence. No workaround, fork modification, package release or production activation was performed. The user was asked whether to expand to the official migration work-package.

Owner follow-up: explicitly approved advancing the official Pi migration, keeping production disabled. Work is isolated on `codex/pi-official-migration` from main `3dd7ba6f`; no release/deployment authorization was added.

## PR acceptance and merge scope (2026-09-25)

Owner authorized reviewing all open PRs and merging completed, accepted slices, then explicitly authorized a CodeGraph index for this isolated acceptance worktree. The bounded SDK test/documentation slice is the merge unit; T6 and complete #194/#195 Host/provider acceptance remain open.

Current head `67a4709c` passed all 46 hosted checks. A fresh local run passed build, typecheck, full tests (5461 passed / 141 skipped), API surface and version authority. Strict task workflow passed using the installed helper package's regular-file source root; the global helper symlinks are rejected by the runtime. Skipped tests are not acceptance.

Formal acceptance initially refused an unavailable CodeGraph flow proof. The authorized isolated index restored the proof, and automatic architecture materialization updated only the generated sdk-root size signal and projection provenance. Those two generated paths are included in this contract for acceptance sealing; no architecture model, product code, dependencies, wire or public API changed.
