# Notes: S6-b Atomic Truth

- Worktree: `/Users/ancienttwo/Projects/byok-sdk-wt-s6b-atomic-truth`
- Branch: `codex/s6b-atomic-truth`
- Base: `d8e7802` (S6-a, Draft PR #34)
- Claude review: paused; do not invoke.

## Evidence

- `@byok/cloud`：`truth.records` 为 composition-bound capability；三条 route 归类为 `proof`，只接受 `x-byok-device-proof` request-bound envelope，无 bearer fallback。manifest metadata-only；GET inline 或 content-hash keyed object download grant；PUT raw bytes 先验签后 parse。
- `@byok/cloud-postgres`：`PostgresTruthCommitter` 在单一 transaction 内完成 receipt replay、missing-row advisory locks、terminal/snapshot precondition、committed manifest lock、object reference replacement/recount、tenant/hash inline logical accounting 与 receipt-last commit。
- 标准 InMemory composition 默认不声明 `truth.records`；只有同时供应 `TruthCommitter` 与 `TruthObjectDownloads` 才可显式启用。route tests 的 fake 不作为 atomicity evidence。
- real Postgres targeted suite：11 tests，覆盖 exact/mismatched replay、terminal immutable、tenant isolation、batch、quota、receipt-last injected rollback、committed-only object/ref replacement、missing-row CAS race、same-request race、same-hash accounting，以及 concurrent A/B object swap 的 deterministic manifest lock order。
- hard env/full workspace gates：core 112、keys 330、protocol 189、cloud 130、server 217、client 935、conformance 117、cloud-postgres 200；全仓 typecheck/build、deploy SQL order、architecture sync、task sync 通过，`packages/protocol/**` 与 `deploy/sql/**` 相对 base 零 diff。
- fresh worktree 首次直接 typecheck 因 workspace packages 的 ignored `dist/` 尚未 bootstrap，example 无法解析 `@byok/server`；先执行一次全仓 build 后，按 contract 原顺序重新运行 typecheck → test → build 全绿。该 bootstrap 行为是既有 workspace packaging shape，不是 S6-b 回归。
- Claude review 仍暂停，independent Codex acceptance 尚未执行。
- Delivery：commit `2de841d`，stacked Draft PR #35（base `codex/s6-device-proof-memory` / PR #34）。合并前必须补 independent security/transaction acceptance；S6-a 合并后再 retarget `main`。
