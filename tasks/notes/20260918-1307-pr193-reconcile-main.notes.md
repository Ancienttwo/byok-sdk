# Notes: pr193-reconcile-main

Owner 批准三段合流顺序（2026-09-18）。Stage 1 = #193 in-branch reconcile。冲突裁决与逐文件决定记录在本文件。

## Per-file resolution decisions
(pending — worker fills)

## Per-file resolution decisions

Side key: HEAD = #193 (d4dcf961, pi-runtime-launch); `a688b1c0` = main. Binding ruling applied: main's #185–#190 evolution is authority on overlap; #193's runtime-launch core retained; integrate where interlocked.

1. **api-surface/client.d.ts** — Resolved the two conflicted export lines as the union of both lines' public input-preparation surface (#193's retired-keys constants + compiled-snapshot/message/readiness types AND main's `InputPreparationSourceAuthority{Request,Outcome}V1`), then regenerated the golden with the project's own `check-api-surface.mjs --update` after build. The generator additionally recorded the merged store's `gc(nowMs?, preserveInFlight?)` signature (auto-merged source truth) and, later in this merge, the new `mcp/authority-error.d.ts` section (see the interlock notes below). Rationale: the golden must equal the built closure; hand-writing it would guarantee drift. Verified by `bun run check:api-surface` (10/10 goldens match).

2. **docs/researches/runtime-input-preparation-contract.md** — Conflict was end-of-file: #193's unique runtime-launch sections (§21–§24, §18b fork-build-5) vs main's "PR187 review closure contract — 2026-09-17". Kept BOTH in sequence (#193's sections, then main's closure). No duplicated fact: §24 covers implementation-identity gaps; PR187 closure covers source-authority admission, `counter_interrupted` ownership, startup GC, and `runtime_identity_unavailable` — complementary records from the two lines. §20 (G4-remote) already carried main's §19 text via auto-merge; nothing renumbered.

3. **packages/client/src/__tests__/input-preparation-remote.test.ts** — Kept #193's tests (mixed-history verbatim, forged-provenance refusal, declared-permission-mode-to-binding); dropped main's `it.each` colliding-MCP-identities test because it drives the removed `deps.observeToolsets` seam that the merged `RemoteInputPreparationDeps` no longer has (auto-merged interface; the merged v2 wire contract retired caller-stated tools — `INPUT_PREPARATION_RETIRED_REQUEST_KEYS`/`_SNAPSHOT_KEYS` in the merged `input-preparation.ts`). No accepted behavior lost: a collision still ends as a terminal `unsupported_input` rejection with zero compile/counter/socket, now detected inside the ONE prepared-tool-surface assembler (`prepared-tool-surface.ts` refusal code `unsupported_input`, "or its servers collide"), and projection-level collision coverage lives in main's own auto-merged `mcp-projection.test.ts` ("MCP qualified-name collisions", lines ~709+).

4. **packages/client/src/__tests__/input-preparation.test.ts** — Import conflict resolved as union (`verifyCompiledPreparedInput` from #193 + `InputPreparationRuntimeIdentityError` from main; both used in the auto-merged body). One auto-merged main test then needed interlock integration: "isolates the request and verified grant from resolver mutation" asserted the compiler received the caller's snapshot byte-identically (main's service behavior), while the merged service — #193's runtime-launch core — hands the compiler the daemon-derived projection (derived `selectedTools`/`tools`, required by the native prepared contract). Adapted the assertion intent-preservingly: caller-stated prompt fields must reach the compiler verbatim (`toMatchObject`), messages must be exactly equal, and the mutated `evil` values must appear nowhere in the compile call. The isolation property is asserted, not weakened; the late-resolve counter fixture also gained the merged counter contract's required `providerEvidence` (same `fixtureEvidence(counterRequest)` idiom the file already uses at 6 other sites).

5. **packages/client/src/daemon/create-daemon.ts** — Both startup invariants kept, in dependency order: `await initializeInputPreparation()` (#193: one instance promise, no half-initialized service exposed to any control endpoint, including enrollment-only) followed by `await inputPreparationService?.open()` (main: retention owned by daemon lifecycle, store opened under the already-held lease before endpoints; `?.` preserves #193's legitimately-absent-service path). The merged service's public `open()` is `ensureOpen` + GC scheduling, so the second call is exactly main's accepted startup behavior.

6. **packages/client/src/daemon/input-preparation-remote.ts** — Took the #193 shape for both hunks (no inline toolset observation in the remote lane; the request carries only caller-authorized data and the service assembles through `prepared-tool-surface.ts`). Grounds: the merged `RemoteInputPreparationDeps` has no `observeToolsets`; the merged v2 request type has no `tools`/`toolExecutors` fields and REFUSES them by name as retired keys; main's inline-observation block would not compile and would state caller-stated tools the merged contract forbids. Main's behavioral surface survives structurally: toolsets still observed on-device (inside the assembler, after durable reserve), unobservable toolsets still answer the typed `toolsets_unobservable` wire reason, and every business refusal still reports as a terminal completion.

7. **packages/client/src/daemon/input-preparation-service.ts** — Two additive integrations. (a) Compile catch: main's accepted `InputPreparationRuntimeIdentityError` → `runtime_identity_unavailable` branch (typed rethrow) kept verbatim, followed by #193's accepted specific-detail recording (`cause.detail` for compile refusals that name a broken contract) instead of main's bare `compile_rejected`. The auto-merged `adapters/pi/input-preparation.ts` keeps #193's `InputPreparationCompileError.detail` field whose doc promises exactly this durable-record behavior, so the recording is the interlocking consumer. (b) Reserve call: main's `buildBinding(verifiedRequest, ...)` (binds the SOURCE-AUTHORITY-VERIFIED pair — `resolveSourceAuthority` is main's accepted #187 admission) kept, plus #193's `model: request.selection.model` which the merged store's record-v3 contract REQUIRES ("a prepared launch must re-present the counted model identity"). Neither side's semantics dropped; no fork.

8. **scripts/release/pi-launcher-smoke.mjs** — Took #193's `mcpEnv` line; did not take main's `permissionMode: 'auto'`. The merged `parseTaskScopedMcpConfig` requires both `mcpEnv` and a `permissionMode`; the merged config already declares `permissionMode: 'readonly'` (self-consistent with its `readOnly: true` fixture classification and surrounding text). Keeping main's line would have produced a duplicate object key shadowed by `readonly` — drift, not behavior. `mcpEnv` is the runtime-launch requirement (the pool spawns servers with it). File-URL/observer fixes were NOT imported (WP1 territory per the ruling).

**tasks/todos.md** — not conflicted; auto-merged cleanly (both lines' rows kept by git). No ledger merge needed.

### Interlock integrations outside the 8 conflict files (flagged for review)

The merged tree typecheck/gate revealed three interlocks whose fixes required touching auto-merged test files and the MCP error module. All are test-fixture/import-edge integrations; no accepted behavior on either side was dropped.

- **`packages/client/src/__tests__/mcp-projection.test.ts` (~line 753)** — main's accepted "refuses a prepared executor map" test called `buildToolExecutorsFromObservation` with the pre-launch 5-field shape; the merged adapter (runtime-launch) requires `launch` + `implementations`. Added fixture values (`{ launchCwd: '/', launcher: null }`, `{}`) that the tested rejection path provably never reads (the duplicate-name throw happens in projection, before both fields are consumed). Assertion unchanged.
- **`packages/client/src/__tests__/prepared-tool-surface.test.ts` (line ~157)** — the `ALWAYS_AUTHORIZED` stub lacked main's accepted `resolveSource` seam; added the exact idiom the other merged suites use (`async resolveSource({ source }) { return { authorized: true, source }; }`).
- **`McpAuthorityError` module extraction (gate interlock)** — main's accepted collision catch in `adapters/mcp-tool-grants.ts` imported the class from `mcp/client.ts`, which value-imports `@modelcontextprotocol/client`; with that edge inside the guarded adapters graph, esbuild retained `mcp/client.ts`'s top-level code and the emitted `dist/adapters/index.js` re-gained the two `@modelcontextprotocol/client` imports, tripping #193's accepted `dist-subpath-closure.test.ts` gate (verified: #193's own parent build, built in a scratch worktree, has ZERO such occurrences — the gate passed there). Integration: extracted the class verbatim into new zero-import `src/mcp/authority-error.ts`; `mcp/client.ts` imports and re-exports it (all existing specifiers — `mcp/index.ts`, daemon files, `mcp-server-pool.ts` — unchanged); `adapters/mcp-tool-grants.ts` and `mcp/projection.ts` import the class from the leaf module (projection's `McpStdioServerSpec` stays a type-only import, erased at emit). Result: one class definition (exact `instanceof`, no alias, no second authority), main's refusal semantics byte-identical, and the emitted bundle is clean again (bundle module list now differs from #193's clean build only by the new leaf module). The api-surface golden picked up the new `authority-error.d.ts` section via the sanctioned `--update` pass.

## Test-only flake hardening (2026-09-18, follow-up slice 1507)

CI 判定后附刀：`input-preparation.test.ts` 三处固定真实 sleep（原 :1226 450ms、:1230 150ms、:1252 450ms）改为 `vi.waitFor` 轮询（timeout 5s）；等待方式变化，断言逐字不变；`scheduleGc` 与产品面零改动。负向断言（原 :1275/:1296"确认不发生"类等待）保持原样。验证：全文件 63/63；两改造用例各 ×3 绿。验收面在 20260918-1507 contract（protocol 1, reviewer Claude——单文件机械改动按 CLAUDE.md 门槛免 gatekeeper 派发，orchestrator 直验）。
