# Integration Notes: PR188

> **Status**: Active
> **Plan**: plans/plan-20260917-1330-c07-pr188-integration.md
> **Contract**: tasks/contracts/20260917-1330-c07-pr188-integration.contract.md

## Evidence
PR187 merged as 601a1991da6c0c0353d4d059cdc851f457e79d93, source 635aeebc, 46 CI jobs passed. PR188 b93ef14a merged current main without conflicts. Read-only merge preflight verified source-authority fixtures and runtime consumers need no compatibility adaptation. Existing stacked CI is historical only.

## Boundaries
Shared MCP core, per-tool projection and fork pin 0.85.1002 retain the original PR188 scope. Prepared launch, Execution pin binding and remote preparation remain later PRs.

## Confirmed integration gate finding

The shared qualified-name format permits `a`/`b__c` and `a__b`/`c` to both produce `mcp__a__b__c`. Real observeMcpServer fixtures accept both pairs; preparation previously overwrote the first executor fingerprint. Two regression tests failed before the production change (`_ops/pr188-integration/collision-pre-fix.log`, PRE_FIX_EXIT=1). The minimal repair rejects duplicate qualified names in the existing shared projection, before registration or fingerprint generation, preserving accepted individual names and the public naming format. No winner-selection or name translation is introduced.

The first canonical run passed full tests (135028ms), build/type/API/version and focused checks. Workflow alone rejected three omitted metadata sections in the new integration plan; these are now supplied. Source changed for the confirmed collision, so final required evidence must follow the new source freeze.

The repair shares one collision validator between projectMcpTools and mcpToolsetToolNames. resolveMcpToolsetGrants converts its typed authority rejection into the existing {ok:false, reason} result, covering Claude/Codex/Pi admission as well as preparation. A second real-server pair a_/b versus a/_b proves banning only double underscores would be insufficient; no global name grammar restriction was added. Focused projection/grant suites passed after the repair.
