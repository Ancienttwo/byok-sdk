# 0.13.0 stable release execution

Authorization: user explicitly requested merge and publication on 2026-09-05.

## P1: boundary

Release the accepted SDK source from PR #145, merged as
`ab47f4eda6af2f90d6e3d2db2b27e352ad08abd7`, through the existing ten-package
npm train. Nine aligned packages use 0.13.0; independently versioned keys uses
0.3.10 to publish the exact core 0.13.0 edge. Preserve all concurrent main
worktree changes. Hosted migrations, deployment, and Salesko cutover remain
outside this release action.

## P2: concrete trace

Package manifests -> matching bun.lock workspace records -> canonical build ->
pack-and-smoke frozen tarballs/manifest -> dependency-order npm publication ->
registry integrity and fresh-install closure -> annotated remote tag and GitHub
Release. The publication driver must finish artifact validation before npm writes.

## P3: decision

The spec requires a MINOR for new public contracts, migrations, and pre-1.0
breaking cuts. Reusing 0.12.0 or keys 0.3.9 would collide with immutable registry
versions and preserve an incorrect keys dependency edge. Change only version
metadata and release documentation; reuse accepted source evidence and let the
version PR CI verify the frozen metadata revision. Registry partial publication
is the principal release failure boundary: retain the same source and tarballs
and resume only vacant immutable versions if necessary.

## Task Breakdown

1. Merge exact accepted PR #145: complete; 44 checks successful before merge.
2. Prepare version metadata, lockfile records, and release notes: complete.
3. Verify version PR, merge it, and freeze the release source: pending.
4. Run canonical build/pack/install/publish/registry readback: pending.
5. Push annotated tag and publish/read back GitHub Release: pending.

## Initial verification

- Frozen dependency install passed.
- Release graph: nine aligned 0.13.0 manifests and keys 0.3.10 passed.
- README/spec version authority and git diff whitespace checks passed.
- PR #145 source acceptance, real Postgres checks, and exact-head CI remain
  recorded in the reliability review; this metadata-only change does not replace
  or relabel that source receipt.

Final mutable release evidence belongs in the canonical local release manifest,
registry readback, remote tag, and GitHub Release.
