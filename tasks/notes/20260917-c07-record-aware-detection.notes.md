# Notes: C07 record-aware detection design

- Authorization: supervisor accepted public consumption packet2a692dc and explicitly released API-D+API-R docs-only design registration; implementation/API shape remains review-gated.
- SDK basef5258d8f clean, existing pi-launch branch reused. Salesko2a692dc packet followed by98016c5 acceptance-only docs; producteb5b6cc unchanged. No push.
- Current observations are readonly source inspection, not new runtime probes. Both installed forms lack authority-aware detect; configured prepare has its own existing authority path. Default daemon factory passes only byokLauncher; local display projection omits extra refusal information. The design includes these consumers rather than patching only PiAdapter.
- docs/spec.md remains product truth and unmodified. Q1-Q4 require SDK review; matrix is semantic specification, NOT RUN. No API spelling, new helper kind, record schema or actual package version selected.
- .ai/context/context-map.json read. Referenced capabilities.json is absent in this worktree; no scoped agent context added or repaired. No CodeGraph directory, so source reads used explicit known paths.
- Existing source/notes evidence and accepted gates are unchanged. Row4 LOW is retained report-only; future citations use exact measured interval, not whole-function claims.
- Validation receipts will be stored in _ops/c07-record-aware-detection-design; no build/typecheck/full/pack repetition on unchanged products.

## Design acceptance receipt

Supervisor accepted a4dab4a4 and independently checked freeze8e9d8416. `reviewPending` is false for this docs-only design subject. Product ABI and write scope are still pending their own review. Q1-Q4 directional decisions are recorded in the review file; no implementation or executable-vector acceptance is inferred. P4 gate remains accepted/unrepeated; old row4 evidence and notes remain unchanged.

## Exact product candidate preparation

- Read-only explorer plus root confirmed all current detect consumers and defaults/injected arrays. Product remains a4dab4a4's product base; no build/test/pack or executable vectors. Root is sole documentation writer.
- New pressure points: identity.ts resolver includes launch env digests; trusted-launch-cwd non-writability uses wx; generic detect runs before Pi resources.kind. Candidate does not fabricate env/launch identity, write-probe from detection, or a default pi-rpc target.
- Proposed optional installed method and finite refused arm remain candidates. Shared measured-installation has a distinct top-level tag and lacks env fields; type/parser exclusion from spawn is an acceptance requirement. Read-only cwd facts do not prove ACL non-writability.
- Q5 complete-top-level observation is recommended for review; exact aggregate shape/precedence and release-commonality reuse remain open. This does not silently activate the proposed source/test/spec/golden inventory. Pre-fix drivers follow accepted exact product registration, not this document.
- New evidence directory `_ops/c07-record-aware-detection-product-candidate/`; original freeze8e9d8416 preserved. Separate acceptance90d598cc retained. No P4 gate, old notes/row4 or native decision packet altered.

## Product scope activation

Product candidate af77aee2 accepted. Q5-A deterministic fixed-order complete set, first exact refusal, no aggregate metadata beyond verified equal native version; Q5-B is historical only, no fallback. Root owns all writes. Exact contract activated before pre-fix drivers; no product bytes changed in this registration.

## Implemented candidate (supersedes initial docs-only state)

- Registration1137ae33 → driver8c9f7a26. Pre-fix log `_ops/c07-installed-observation-product/pre-fix.log`:5 failed/1 passed, actual strict decoder/CLI/real daemon behavior, no compilation/import failure. Original73 driver lines retain all assertions; new cases append task selection/status/doctor/malformed controls.
- Shared measurement extracts one private physical core. Separate measured-installation discriminant/no env digests; old identity/spawn parsers and TypeScript assignment reject it. Old seal serializer is byte-identical. `_ops/c07-installed-observation-product/physical-parity.json` compares old/new serialized results or throws and ordered physical/env traces for4 frozen M0 locator vectors ×7 conditions =28/28. Synthetic probes are not installer proof.
- Native manifest body is mechanically extracted. Existing host retains export-assets check after it. Common record helper excludes only launchArgv; existing commonBinding body after this exact helper expansion is byte-identical. Physical reverify body and subsequent exports unchanged. `extraction-equivalence.json` records4 units +8 unchanged goldens.
- WIP verification: shared110/110; new routing/installation31/31; expanded focused7 files128/128 incl host-binding/launch-binding/plan/cwd-dependent launch/closure. Client/shared typecheck and client build passed. Only client/implementation-identity goldens changed; other8 byte-identical. These are pre-freeze evidence, not complete-gate acceptance.
- Historical failures retained: identity-r1 fixture replaced a digest with its existing value (corrected fixture only); typecheck-r1 included wrong fixture import/nested discriminant typing plus stale dist after public type change. No assertion/budget relaxation or runtime fallback.
- Read-only cwd success is NOT an ACL/non-writability proof. Unit files use real disposable bytes with uid-only seam and explicit synthetic record; no real Host/native release proof. D13 installed-child arm N/A (selected route creates no child); existing ordinary probe OS/deadline tests remain coverage.
- Executor matrix follows supervisor allocation: freeze first, one complete14-package test matrix, no repeated full. Gate independently does build/typecheck/focused10-golden/strict/diff/attribution/real pack. Public package publication, Salesko consumption, native closure and real installation are NOT RUN here.

Product frozen9f697036 (26 source/test/golden files). Additional read-only cwd27/27 covers observable-yet-writable versus full checker rejection and exact readonly invalid shapes. Spec/architecture/CHANGELOG registered separately; frozen executor matrix pending, no PASS inferred.

## Frozen discovery and fixture correction registration

d5949148 executor matrix completed all14 packages once: client24 failed/2778 passed/11 skipped; other13 passed. Existing S2 accounts for1; prepared-offer-lane22 + process-restart1 are missing-method fixture wiring, not timing failures. `_ops/c07-installed-observation-product/frozen/failure-classification.{md,json}` records exact P1/P2/P3 and source hashes. Full raw SHA2566d5f4b71f4cc9bd51ab54acda69e153c8a84b6164ec3a1108ea02d3b1c2d8ef7. No product gate acceptance yet.

Supervisor accepts the classification and only two local fake-adapter paths: check exact context/same authority then return explicit synthetic availability. Global stub/old assertions/budgets/product unchanged. New frozen client full once authorized; remaining13 results retain validity by bytes. Findings evidence remains archived.
