# Notes: Issue177 campaign completion

> **Plan**: plans/plan-20260910-2138-brc177-completion.md
> **Status**: Active

## Decisions

- The user's explicit extension on2026-09-10 lifts the earlier worker/verifier SKIP and authorizes finishing the original BRC chain. New campaign is byok-brc1415-20260910-completion; the existing Issues177/178 remain fixed.
- Fresh worker uses only the two product blobs from PR182, whose source/CI evidence remains independently attributable. It does not inherit a failed historical worker final or claim old CI as current execution.
- The old canonical regression is frozen for planning/acquisition. It may be corrected inside the acquired worktree as an explicitly allowed change; closeout does not revalidate that pre-acquisition guard hash after merge.
- The parent pins host runtime9cc12bac and image sha256:46726c2a1b8629a320dfa0d4cca95c4e46cca11f40af14a2d62f36559e956e0e built from it. Worker and verifier share a45minute budget inside the extended grant; provider and merge semantics remain unchanged.
- Required target checks run once through canonical preparation after implementation freeze. A real independent verifier consumes those exact artifacts; parent source checks are not final campaign acceptance.
