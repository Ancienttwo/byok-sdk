# Notes: wp1-win-ci-warmup (2026-09-17)

## Round-4 evidence (run 35200434840, job 105133623925)

- [ps51-diag] inherited PSModulePath includes runneradmin/packer profile dirs (inaccessible to lowpriv token) -- hypothesis "PSModulePath is the cause" REFUTED: explicit Import-Module + Get-Command Get-Acl succeeds with the inherited path (as-inherited: Get-Acl) and with a sanitized path (sanitized: Get-Acl).
- Keys suite after warm-up: 3/4 green including the previously failing positive ACL case; module auto-load resolved.
- Remaining failure is a different, pre-existing product bug: owner-mismatch test (queries SystemRoot) hits MethodInvocationException "Some or all identity references could not be translated" at the rules loop ($_.IdentityReference.Translate), i.e. pi-provider-launcher-core.ts WINDOWS_PROJECTION_ACL_SCRIPT eager per-ACE translate. Admin token translates those ACEs (test green in #193 gate); lowpriv token cannot. Expected typed refusal pi_projection_owner_mismatch never reached.

## Deliverable state

- eb1a5eca: keys file restored to d4dcf961 bytes (probes 5dd0056a/2bb441ba net-rolled-back), [ps51-diag] replaced by permanent one-line warm-up with rationale comment. Verified: KEYS_BASELINE_OK, DIAG_GONE, WARMUP_OK, git diff --check clean.
- Not pushed: lane stays red on the Translate bug until the Owner rules packages/keys ownership (second out-of-scope product finding; probes already consumed the single exception).

## Mechanism confidence

- Cold module-analysis cache -> first -EncodedCommand spawn under fresh account fails auto-load: empirical (3 rounds fail without warm-up, 1 round works with). Mechanistic pin (why analysis fails in-product but succeeds in a plain -Command spawn) not established; next green-seeking CI round after the product ruling validates the warm-up end to end.
