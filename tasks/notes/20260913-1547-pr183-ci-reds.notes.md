# PR #183 CI repair evidence

## P1: Map
Host task assertion capability owns BYOK_HOST_TOOLSET_CONTEXT. SDK Agent memory has independent admission and BYOK_AGENT_MEMORY_CONTEXT. The release migration smoke uses the current installed candidate API against old SQL fixtures. Compose is the single disposable CI substrate definition.

## P2: Traces and pre-fix evidence
- `create-daemon.ts:1213` resolves Agent memory when Agent home is configured; `task-runner.ts:2002,2086` admits the reserved memory server independently of host task assertion discovery. On macOS without helper it is absent; Linux CI supplies secure descriptor operations. Explicitly mocking availability to true reproduces the CI assertion, while false passes: `_ops/pr183-ci/client-pre-fix.log`, PRE_FIX_EXIT=1.
- `pg-migrate-smoke.mjs` constructs replayInput without schema and calls the current installed Postgres replay authority; undefined binds to SQL NULL. This is not an old runtime. Pre-fix smoke reproduced NOT NULL rejection on disposable PostgreSQL 18.4: `_ops/pr183-ci/packed-pre-fix.log`, PRE_FIX_EXIT=1. Reused artifact source 90cf5de6; `git diff 90cf5de6..dc2e0530 -- packages/core packages/cloud-dataplane deploy/sql` is empty, so relevant runtime/migration bytes are unchanged.
- PR diff contains no prior compose/workflow change. Docker Hub manifest access also fails locally (denied/unauthorized). Official MinIO release README specifies Quay: https://github.com/minio/minio/blob/RELEASE.2025-04-22T22-12-26Z/README.md . Anonymous Quay manifest GET returns HTTP 200, digest sha256:a1ea29fa28355559ef137d71fc570e508a214ec84ff8083e39bc5428980b015e, including linux/amd64 sha256:3f97c5651cb6662b880c787a232b6b34fec8d8922e08d6617b25d241a21164bb.

## P3: Decision and actual changes
- Parameterize the undeclared-capability assertion across both secure-memory availability states, require the exact appropriate MCP inventory, preserve memory's independent context, and assert no host nonce on any server. Production gating remains unchanged.
- Pass exported DEVICE_ASSERTION_SCHEMA_ID in the installed candidate smoke and exercise 64 competing consumers independently on each schema with the same JTI; read back both ledger rows. Migration 0022 already backfills pre-existing rows with device schema and drops the temporary default. A permanent fallback would weaken the explicit schema contract and would not fix an explicitly bound NULL anyway.
- Switch MinIO from Docker Hub to official Quay with the identical release tag. No credentials, secret configuration, billing or version change.

## Verification
- Focused task assertion broker: 30/30 pass, including availability true and false (`_ops/pr183-ci/client-fixed.log`).
- At source freeze 98fe017f: release pack (including `bun run build`), root typecheck, root test, API surface, version authority, and strict workflow all passed. Logs: `_ops/pr183-ci/{release-pack,typecheck,test}.log`. Root test runs with the standard service-free configuration; real dataplane tests are not claimed by that local pass.
- Exact tarballs at `_ops/pr183-ci/artifacts-98fe017f` passed installed-package empty-install and tag-bound v0.4.2 upgrade smoke on PostgreSQL 18.4. All 22 migrations applied; unrelated seeded facts remained unchanged; each schema independently admitted exactly 1/64 concurrent consumers for the same JTI. `_ops/pr183-ci/packed-fixed.log`, EXIT=0.
- Direct transactional backfill check seeded the pre-0022 table, applied 0022, verified device schema backfill, two rows for one JTI in distinct schemas, NOT NULL retained and no steady-state default (`_ops/pr183-ci/backfill.log`). Disposable cluster stopped afterward.
- CI 34746245946 at 98fe017f: MinIO substrate startup passed and packed migration job passed. Full dataplane reached 335 passes and three failures, all the same missing-schema seed in tenant-erasure.test.ts:258. Log `_ops/pr183-ci/dataplane-ci.log`. This D2 regression was previously masked by image-pull failure.
- Follow-up changes only that seed to pass exported DEVICE_ASSERTION_SCHEMA_ID explicitly; cloud-dataplane typecheck and diff whitespace check pass. All other replay INSERT sites were inspected: the sole remaining schema omission is an intentional rejection test. CI will validate the real tenant-erasure flows; no local MinIO runtime claim.
- The follow-up test fixture is excluded from shipped artifacts; package runtime, migrations, and packed smoke source are identical to 98fe017f, so local artifact generation is not repeated. Final CI produces its own exact-head tarballs.
- Local Docker daemon unavailable; container startup and full dataplane evidence must come from CI. Quay manifest availability alone is not container runtime acceptance.

## Scope and remaining evidence
Stay on codex/byok-018-d2-task-assertion; push only PR #183; no merge, release, other repository, TanStack or production mutation. Existing global unmanaged-hook warnings and advisory architecture queue are outside this slice. Configured brainRoot still differs from the user-provided Obsidian vault; no global configuration repair or vault synchronization is part of the CI fix.
