# Notes: team-pi-relay

User selected RPC/GUI interaction and exact Pi 0.85.1. Work is isolated at
codex/team-pi-relay based on 5af5c5c; the concurrent main/event-spill lane was not
modified. This task does not authorize merge, push, global install or release.

P1/P2/P3 and the accepted design are in the linked research and review. Pi's
new public UI spans are first observed by the guard, projected over native
setStatus RPC, and awaited at input/provider admission. Direct extension stdout
was experimentally found redirected to stderr and is not used as transport.

Known/unknown RPC failure both stop without retry. GUI IDs never expire by local
guess. 30s RPC and 120s settlement limits are documented host budgets. The same
native run proved confirmation hold and both members' read/post/ack. Its initial
smoke oracle incorrectly required Pi to ack its own reply sequence. The failure
is preserved; corrected evaluation uses the exact same durable receipts and
source/artifact hashes, without another provider call. Bob ack=1, Alice ack=2.
The original timeout path did not capture relay exit code; settlement, empty
stderr, owned-process cleanup and deleted native test thread are recorded.

Validation: root build/typecheck/test/API/version checks passed, 312 test files
and 3668 tests on the final full rerun. One known unrelated Wrangler cold
packaging 5s timeout occurred; unchanged rerun passed. No unrelated source fixes.
Focused Pi/relay/RPC tests: 52 passed. Existing tmux Claude returned safety PASS;
final delta/evidence closure recorded in the review.
