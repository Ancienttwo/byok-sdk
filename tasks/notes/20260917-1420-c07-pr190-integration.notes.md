# PR190 integration notes

Current main PR187/188 merged with grants/test conflicts resolved preserving both accepted collision rejection and new permission filtering. Final acceptance base will include PR189 once its CI/merge completes.

## Root Cause Evidence
- root_cause: Raw read/mutation qualified-name collisions were hidden by policy narrowing before identity validation.
- repro: Two real observed server/tool collision pairs, each with an extra safe read tool so no-empty-policy rejection cannot mask the defect; 8 failing cases across auto/confirm/readonly/plan, 38 existing cases pass.
- regression_guard: packages/client/src/__tests__/mcp-projection.test.ts
- pre_fix_failure_artifact: _ops/pr190-integration/collision-pre-fix-evidence.log with retained raw SHA/output.

Fix validates raw identity in the shared policy entry before narrowing and returns the existing typed refusal. Grants retain main McpAuthorityError handling. Release smoke initial fixture explicitly uses auto, preserving its previous complete-tool exposure without inventing a readOnly classification. The later actual readonly launch config remains generated by the adapter from operator-authorized fields.
