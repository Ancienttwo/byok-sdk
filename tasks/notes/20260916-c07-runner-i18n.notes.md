# Notes: C07 runner/i18n implementation entry

## Evidence base

Read supervisor original `scratchpad/gates/p3d-6696c211-gate.md`; copied unchanged to `_ops/c07-identity-workspace/runner-i18n-entry/p3d-terminal-gate.md`. Client2673pass/11skip/one S2 :305 failure36 (clipboard/jiti/i18n12each), both lanes get_state; release-pack0 sourceGitSha6696c211, installed operator usage78; API unchanged, attribution0. Keys reused unchanged prior evidence, not a new keys run. No full/pack rerun here.

P4 designacb29c9/31e8321 accepted, no runtime record author implemented. This packet records Host multi-kind table→single-prefix records, not multi-prefix SDK bindings. Default compiler identity discovery at input-preparation.ts:398–400 remains in frozen product; explicit required parameter/test-supplied runtime belongs futureM1.

## Entry readiness

Design directions approved; literal schema/version/encoding vectors, recursive runner→Pi custody and exact vendor file inventory remain M0 gates. Proposed format/version/layout values are review candidates, not deployed facts. `@juicesharp/rpiv-i18n@2.8.0` metadata read from existing verified tarball only; no install/network re-probe. Missing `.ai/context/capabilities.json` is existing report-only, context-map read; no scoped context added.

## Verification

Pending docs-only diff/path/workflow checks and supervisor review. No product acceptance claimed by this document.

Author static checks: exact nine docs-only paths (five successor + four parent tracking), product/scripts/lock/golden/workflow bytes unchanged from6696c211; diff-check0; workflow strict0. Evidence `_ops/c07-identity-workspace/runner-i18n-entry/docs-check.json` and `workflow.log`. No full/pack rerun. Supervisor review remains independent.

Supervisor design review accepted. Questions now separate M0 exact test-only vectors / M1–M3 SDK / Salesko P4; clipboard12 remaining RED is written into implementation approval text. Current scope stays nine docs only, no vectors authored yet. Compiler default removal remains M1 product scope; M0 freezes its explicit-argument contract. Canonical vault pattern local-cli-auth-environment updated with durable initialization/operator/descendant boundaries, no release-state claim.
