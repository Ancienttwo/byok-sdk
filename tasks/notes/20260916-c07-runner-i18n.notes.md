# Notes: C07 runner/i18n implementation entry

## Accepted base and scope

Product6696c211 passed the supervisor helper-lookup gate, not complete containment: client2673pass/11skip/one S2 tripwire36; ordinary/prepared get_state; release-pack0; API unchanged; attribution0. Gate original copied to `_ops/c07-identity-workspace/runner-i18n-entry/p3d-terminal-gate.md`. f9130440 is the accepted docs-only entry packet; no later product change.

Owner approved M0 two inert vectors + docs, conditional SDK M1–M3 and separate P4 implementation. Both product stages wait for supervisor M0 PASS and exact path registration. P4 product base must be previously pushed32cfcd49 after live check, or explicit followups disposition; docs-only375b5316 ancestry is not product authority. No P4 branch/product created.

## M0 draft delivery

Five successor docs and two JSON files only. Four kinds: pi-rpc/pi-prepared/pi-subagent-runner/pi-subagent-print. Five explicit edges include foreground and nested print routes; prepared has no descendant. Runner->print has depth cost0 because it executes the already-admitted async delegation. No in-process print equivalence assumed: global stdout/signal ownership conflicts with isolated concurrent process semantics.

Supervisor selected composition C for the next freeze: unchanged V1 template + templateDigest + Host descendantPolicy + typed perLaunch, with verified-parent exact optional-name selection. Existing byok.implementation-spawn/v1 remains unchanged; declaring exact names is not permission to edit its identity.launchEnvNamesDigest. Keep controlled directory roots and loader-values measurement; per-task session native option remains under the inherited session root. No empty-value shim, online daemon IPC or arbitrary code selector.

Record four finite upper bounds are explicit; vector3/7/2/11 is synthetic. Effective user configuration/grants must fit, no clamp. Explicit sessionCap is a product tightening over upstream unlimited; no production numerical limits chosen. Preserve root grant action with post-grant cap enforcement. Atomic fanout claims, session/parallel lifetimes and recursive parent authority still need the M0 custody gate.

Credential inheritance is explicit and keeps parent custody; no new lookup/provider authority. CLAIM C applies on every descendant: MCP gets projected env without provider credentials/Pi directory selectors. Secret values never enter fixtures/config/digest/logs; negative examples are clearly synthetic.

## Inventory and validation

Ignored evidence directory: `_ops/c07-identity-workspace/runner-i18n-m0/`.

- `vendor-inventory.json`:227 literal-relative source paths with bytes/hash,290 installed regular files counted,MIT license,package metadata,7 computed/dynamic edges. Not full runtime/transitive-license closure. Earlier explorer PARTIAL text is superseded only for exact inventory completion, not live custody proof.
- `locale-source-inventory.json`:9 exact local todo2.8.0 source files; English13 keys, others14,all strings. Record example hashes remain synthetic and distinct from source inventory hashes.
- `per-launch-env-source-inventory.json`:52 finite candidate names,48 buildPiArgs names +2 getSubagentDepthEnv outputs +2 proposed limit projections with consumer provenance. Correct package-root literal is PI_SUBAGENTS_PI_CODING_AGENT_PACKAGE_ROOT (utils.ts:12), not the constant identifier shorthand. Unknown executable selector PI_SUBAGENT_PI_BINARY remains forbidden.
- `build-draft.py`:local authoring of inert fixtures only, not a product serializer or second acceptance authority.
- `verify-vectors.mjs` / `four-kind-byte-check.log`:independent Node serialization/hash vs Python generation,5 record vectors and5 context encoding examples;4kind/5edge/depth arithmetic;227 source files + package/LICENSE +9 locale bytes rechecked.
-40 record/raw/SDK-consumer refusal designs +15 context refusal designs +5 arithmetic/transition examples. No product validator execution. Two required C negatives are exact-outside-allowlist and actual!=exact. Context examples contain no measured binding/real MCP config and do not prove executable readiness.

M0 remains NOT PASS until supervisor accepts the frozen composition schema/dual preimages, env delegation and recursive budget custody obligations. No install/full/pack/product test rerun; accepted6696c211 evidence is reused only for unchanged product facts. `git diff --check` and workflow strict apply to this draft. Missing .ai/context/capabilities.json remains existing report-only; no scoped context added.


## C composition follow-up to 44a2a7fa

Fable independently verified all5 prior Host hashes. Revised unpublished record field to descendantPolicy (finite allowlist + four required bounds). Added unchanged V1 templates with fractional mtimeMs, three-layer composition and9 negatives. Independent Python/Node bytes agree; unchanged V1 parser4/4 and existing shared env digest5/5. Existing V1 env-name rejection is not relaxed; new assertDescendantSpawn explicitly delegates optional presence and shares one extracted private physical reverify (future M1). Policy must compare against already verified parent declaration; candidate config digest is not policy authentication. Depth reset within cap is separately rejected via parent transition.

Evidence composition-byte-check.log and composition-existing-v1-check.log; no physical attestation/new-validator/live recursive claim. Old shape and prior ignored generators are historical authoring evidence, not compatibility product paths. New vectors are the sole candidate M0 interface once accepted. Product still6696c211; five docs+two inert JSON only.


## M0 232d4440 gate FAIL / r1 freeze repair

Gate substance recomputed successfully, but completeness failed: malformed parent exit_criteria YAML (strict missed it), missing descendant digest-usage vectors, missing Host resolve wrapper/four-kind interface, and uncited native print globals. Repair scope is six files: three research/notes/review documents, two inert JSON vectors, and parent contract indentation under its existing contract. Direct YAML negative/positive evidence added; no harness product fix. Runtime success wrapper now frozen (original V1 record + descendantPolicy + edges); MCP bytes unchanged, no runtime old-shape fallback. Add4 response vectors/5 response refusals and3 digest families(10 argv variants), expected78/prefix/no stack. Native1005 file:line and digest inventory recorded. context hash example vs real config preimage separated; sourceBase232d4440.

Ignored runner-trace now repository-relative,227 inventory complete, foreground call at actual:566 and explicit MCP grandchild projection cited. Zero-cost runner->print needs actual parent transition, not strict depth descent. Atomic claims/session/parallel lifetimes/charge-once/concurrent cancel-retry remain PARTIAL obligations M1 cannot bypass. No M0 PASS or product implementation claimed by this repair; request one r2 read-only gate after freeze.

Local repair checks: direct PyYAML parsed all7 contract blocks; independent bytes covered5 Host records/4 templates/5 launches/all12 composition cases; existing extractor10/10 refusal messages, V1 parser9/9, shared digests5/5, and wrapper data4/4 plus5 non-vacuous refusal mutations. EX_CONFIG78/prefix remain vector expectations, not live descendant-host evidence. `git diff --check` and workflow strict passed. Evidence: `r1-yaml-check.log`, `r1-byte-check.log`, `r1-existing-code-check.log`, `r1-workflow.log` in the ignored M0 directory.

## M0 accepted / M1a registered
Supervisor r2 PASS0df88a7f: existing parser21/21, digests5/5, all frozen families independently agree. Original gate `scratchpad/gates/m0-0df88a7f-gate.md`. Exact M1a paths activated before product writes. New runtime resolve result must retain immutable policy/edges; shared physical measurement remains singular, MCP unchanged. All five recursive custody obligations stay stop gates. repo-harness YAML validation gap remains report-only in another repository.

## M1a targeted result and i18n atomicity
First targeted run:207 pass/10 fail, every failure in sdk-reserved-helper-host. Adding the approved optional peer activates rpiv-todo index.ts104-105 registerLocalesFromDir(import.meta.url), which resolves nine files at dist/bin/locales instead of frozen release layout and writes fallback warnings before usage refusal. Product regression from the dependency addition, not fixture/timeout. Supervisor split M1b to own dependency+todo integration+layout+preverify; M1a manifest/lock restored byte-identically. Bun frozen install retained three introduced links, removed exactly as recorded in install-rollback-links.json, no cache deletion. Original log client-focused-r1.log preserved. Existing706 package tuples had been unchanged in lock-proof.json; that records the withdrawn candidate only.

## M1a frozen product6eec0ab6 / supervisor full gate requested
Product commit6eec0ab6aee6accdd6317568d326df68115f77c8:23 files +483/-85. Registration commitsca5ba62f/02825ce7 preceded writes. Product and final documentation commits separate, attribution0. Manifests/lock byte-identical to0df88a7f after withdrawing the non-atomic i18n addition.

Local evidence `_ops/c07-identity-workspace/m1a/`: client-focused-r2.log219/219 across11 files including unchanged helper usage refusals/cwd/control/closure20; identity-tests-final.log45/45; identity/client build and typecheck logs; api-check.log10 goldens; version/graph/workflow logs all0. scope-and-authority.json proves identical physical measurement body SHA2566ae6f9a6..., unchanged INSTALL_RECORD_KEYS/Omit, old11 root names retained/+5 Host types, exact registered scope. M0 negatives including runtime-legacy-bare-record and MCP byte pair execute in new shared tests. Synthetic physical probe is not installer evidence.

Not run locally on freeze: full client/keys/all package suite and real release-pack. Supervisor gate owns those runs. Remaining S2 RED expected from native/extension graph; no assertion relaxed. No recursive dispatcher, descendant config wire, budget lifecycle or new credential custody claim. P4 untouched. i18n dependency+single locale layout/preverify now M1b per supervisor ruling; no warning-suppression or extra layout.


## M1b registration during M1a independent gate
Supervisor confirmed same relative layout/two mutually exclusive roots, both with nine digest checks, and lazy initialization of the whole todo+i18n module after usage/config/binding/assets. unconfigured uses only package-relative import.meta.url, attested never calls it. Read-only source trace additionally proves i18n's top-level applyLocale reads config, so deferring registration alone is insufficient.27 upstream files hashed (85201 bytes,16TS/9locale/package/LICENSE); nine hashes match accepted M0 inventory. Exact source/packaging/test paths registered; root sole writer. This commit is docs-only; M1a remains d9e55a4e and supervisor owns the only full/pack. No dependency install or product copy in this checkpoint.


## M1a supervisor terminal PASS received
Independent gate d9e55a4e PASS: client2678pass with only S2 tripwire36 (:306), keys494,identity45,api10goldens,release-pack0,17dependencies unchanged. Original scratchpad gates/m1a-d9e55a4e-gate.md; authoritative handoff updated by supervisor. M1b may implement after this registration. Report-only, not repaired here: ToolImplementationResolutionV1 union at identity.ts507 is not subject-indexed at type level (runtime strict); compiler JS missing-runtime guard at input-preparation401 is intentionally unreachable for valid typed callers; runtimeLocator helper at test:72 ignores its command argument. M1b does not silently absorb these unrelated changes.


## Four pre-write confirmations
1. Sole todo author becomes the licensed private source; npm dependency/noExternal/lock edge removed atomically, not retained as a second runtime source.
2. i18n uses exact public2.8.0 dependency, because the library remains upstream-maintained; only todo's integration seam is private. Verified locale anchor is passed explicitly; neither bridge may discover locales from its own import.meta.url.
3.27 source paths carry upstream and maintained hashes, license/tarball provenance and a finite changed-file list; unit tests check all actual bytes and reject unexpected/missing files.
4. Existing JS+d.ts factories are proven by50b64f45/source. New vendor TS and private JS entry will be checked under root strict options, via separate tsconfig.vendor with checkJs, as part of normal client typecheck. This is not a TS-check bypass.

Read-only upstream preflight (realpath include) found two expected missing-i18n modules and array-index strict errors in state-reducer/todo-overlay; no source edited. Initial logical symlink include also produced artificial peer-resolution failures, so only upstream-typecheck-realpath.log describes actual upstream type gaps. Original todo resolutions were pi-ai fork1005,pi-tui0.85.1,rpiv-config2.8.0,typebox1.3.7. Direct dependency promotion and proven type-only index annotations submitted to supervisor before writes.

Supervisor accepted direct promotions of config2.8.0/typebox1.3.7/pi-ai fork1005/pi-tui0.85.1, with exact existing resolution/integrity and alias-version drift guard. Also accepted type-only proven-index annotations with erased-JS equality. Client dependency count will be17-1todo+4promotions+1i18n=21; config is one of the four, not counted twice. Product begins after a5afe9f1 scope registration.


## Pi TUI direct-edge blocker and selected bounded correction
Initial implementation: build/typecheck/strict vendor/API/version pass,56 focused pass; S2 real child paths/get_state proceed and i18n12 disappears, leaving the unchanged hard tripwire RED24 (clipboard12+jiti12). release-graph alone rejects direct pi-tui0.85.1 native/win32/prebuilds/win32-x64/win32-console-mode.node. No gate exception added. Supervisor selected an investigation with conditional(b)/(c).

Quantified pure runtime subtree: Text from dist/components/text.js -> dist/utils.js -> get-east-asian-width1.6.0 index/lookup/lookup-data/utilities,6 dependencies plus probe. Bundle31447bytes, no native/builtins/runtime import/require. Because a third-party exists, choose(b): exact build-only pi-tui dependency and isolated pure-subpath inlining. Evidence tui-pure-probe.ts/.mjs/.log. A dedicated sequential tsup.todo.config prevents noExternal from broadening the existing web/subagents TUI graph. Register path before change. No overlay removal or width-algorithm copy. Initial runtime21-direct candidate superseded by20runtime+1build-time edge; original failure log retained.

Supervisor accepted(b) with retained full licenses/file hashes in vendor/THIRD-PARTY.md and third-party-manifest.json, registered here before writes. Main tsup precedes the private todo-only clean:false build; the adapters checker must compare every prior dist file digest around that build. Apply the unchanged closure checker to the private graph, plus no pi-tui runtime edge/native-modifiers/createRequire/.node. Compare actual bundled Text/truncate behavior with installed dev pi-tui across CJK/emoji/boundaries. Exact dev0.85.1,20 runtime edges; all previous consumer resolutions/integrities remain unchanged. No complete gate claimed until frozen supervisor run.

Supervisor additionally accepted pure StringEnum public subpath + typebox construction/interpreted-value subtree inlining, conditional on fork1005 exports, zero codegen/TypeCompiler/new Function/eval in final probe, unchanged scanner, licenses/version/file hashes. tool/types.ts already registered; THIRD-PARTY scope covers these exact locked sources. No source pruning, no import whitelist. Check actual bundle before source change.

## M1b stopped at approved minify experiment
Sequential build now preserves all34 preexisting main dist hashes. pi-tui exact0.85.1 moved to dev, runtime target20; frozen install accepts unchanged package tuples. Fork1005 exports ./utils/* already covers StringEnum helper; pure Type/Value/helper probe658 inputs/374889bytes has zero new Function/eval/TypeCompiler/codegen. These are local WIP results, not a gate.

Supervisor authorized one standard minifySyntax+minifyWhitespace attempt with sourcemap/legalComments retained, then stop if the forbidden label remains. Exact tracked scanBundle/staticImportSpecifiers/disallowedSpecifiers functions applied once to each saved output: before508930bytes has8 substring occurrences on4 lines; after378000bytes has4 occurrences in2 __esm debug method labels. Both import only fs/os/path/url; no disallowed runtime specifier/codegen hit. Presence guard remains RED and was not changed. Evidence minify-closure-results.json/log, todo-before-minify.js, todo-minify.log under the ignored M1b directory. Earlier "four before" referred to lines, not occurrences; this result supersedes it.

Product changes/build-option trials STOPPED as instructed, reported directly to supervisor. No additional tuning or scanner exception. This is not a frozen product candidate: source-manifest/delta updates, complete THIRD-PARTY license inventory, equivalence vectors, final lock proof20, docs dependency counts and full/pack remain incomplete. Existing source hashes now intentionally need regeneration for approved import deltas before any future freeze. Preserve WIP; do not run final acceptance on this interrupted subject. No push/merge/publish or P4 writes.

## Authorized six-file vendoring after initialization trace
Supervisor revoked the pi-ai move and accepted path(i): preserve dynamic overlay; vendor two pi-tui0.85.1 JS files plus all four get-east-asian-width1.6.0 JS files, each with full license/provenance/upstream+maintained hashes. No third-party dependency remains outside this finite UI subtree. Exact13 paths registered before writes. ports.d.ts declares only the used requestRender port and derives shortcut key type from the already direct native ExtensionAPI contract, not an accidental pi-tui install. Remove the pi-tui dev edge. New JS included explicitly in strict checkJs; any required JSDoc/type-only changes must be finite provenance deltas with erased-JS equivalence. No blanket exemptions or hidden declaration shadowing.

Supervisor revised the six-JS typing rule after strict JSDoc candidate reached0 diagnostics: restore all six original JS bytes; use sibling declaration files with explicit provenance/deltas, checkJs only SDK-owned JS entries. This supersedes earlier six-JS checkJs requirement. All transient JSDoc/index casts removed. Six exact declaration paths registered before writes; upstream raw hashes must equal maintained hashes. Vendor noEmit target ES2024 is approved (existing /v syntax), main output target unchanged. The retained bare width import requires a single exact build-time mapping to the vendored width entry, submitted to supervisor; never resolve a hidden npm copy.

Supervisor withdrew additional strict checkJs on the existing pi-extension-factories.js: its static exports enter two third-party TS source trees, causing1290+ diagnostics unrelated to this UI slice (vendor-declarations-typecheck.log). Keep its established .d.ts/build/pack boundary. New todo JS plus16vendorTS/UI declarations remain strict. Historical vendor-js-annotations-r3.log0 belongs to the withdrawn six-JS annotation candidate; no annotations retained in upstream JS.

## M1b frozen56f1e569 / unique complete gate pending

Product72 files +7433/-15, registered before writes. Runtime20 direct edges (17-todo+i18n+config+typebox+pi-ai), no pi-tui dev/runtime edge. Remaining705 lock package tuples and other workspaces unchanged; typebox wrong-version negative rejected. All exact dependencies retain original resolution/integrity. Alias versions checked at build.

Final UI author: six raw upstream JS bytes with identical upstream/maintained hashes; sibling declarations/ports carry separate source/delta records. Dynamic overlay unchanged. Dedicated todo build aliases only get-east-asian-width to vendor; metafile asserts six vendor inputs and no npm UI copy. esbuild alias belongs inside esbuildOptions; initial top-level tsup alias was ignored and failed closed (build-vendor-final-r1.log). Correct wiring passed; no conditions/mainFields/minifier retuning. Main34 dist hashes unchanged around second build. Original presence/codegen/import scanner sees0 todo hits. 661 additional third-party build input hashes (including tree-shaken inputs) and full MIT notices ship in dist/assets. rpiv-config omits LICENSE in npm package; its MIT declaration and same-repository/same2.8.0 sibling i18n LICENSE are explicitly distinguished in licenseSource.

Local evidence _ops/c07-identity-workspace/m1b/: build-vendor-final-r2.log (continues completed main build after alias fix), typecheck-vendor-final-r2.log0 (strict vendor/own JS), focused-vendor-r2.log59/60 with one oracle failure then ui-vectors-r2.log2/2 after test-only correction. Oracle originally truncated an already-corner-rewritten line; upstream rewrites full tree marker AFTER truncation. Corrected expected order, no product change:56 Text +56 real lazy-overlay truncation vectors (CJK/emoji/ANSI/zero/boundaries), StringEnum output equals root-export reference, live German/Chinese translations and stderr empty. Other5 focused files passed unchanged, including helper usage, nine assets, source hashes and closure guards. No claim of a fresh combined60/60 run; supervisor full gate owns final result.

api-final.log10goldens/version-final.log/graph-final.log/workflow-final.log all0; lock-proof.json705tuple semantic proof; s2-vendor-final.log real ordinary/prepared get_state, no escape/cache/provider call, tripwire24 = clipboard12+jiti12, i18n0; original zero-attempt assertion remains RED at314. Final full client/keys/identity/real release-pack not run locally. Windows remains not run for this delta. No P4/runner/native1006/push/merge/publish. Historical discarded six-JS JSDoc check0 and minify4-hit evidence remain labelled withdrawn, not final proof.


## M1b d754cf4a gate: packaging/docs repair
Supervisor complete gate FAIL only for three packaging/documentation findings; technical checks1–8 passed independently, including tarball byte provenance, lock resolutions, minified equivalence vectors and real release-pack. S2 remains24 attempts (clipboard12+jiti12), i18n0; not complete containment. Gate original: scratchpad gates/m1b-d754cf4a-gate.md.

P1: dedicated tsup writes dist/bin/metafile-esm.json; check-adapters-entry validates its six vendor inputs, then build-todo-assets validates the full third-party inventory; package files include dist. P2: this temporary556776-byte file therefore entered the npm tarball after both consumers had finished. P3: remove that exact file only at successful completion of build-todo-assets, preserving both validators and their existing build-time path. No runtime/source/dependency/lock/guard change; errors still fail the build. CHANGELOG records36→24 and THIRD-PARTY repeats the manifest's exact rpiv-config licenseSource.

Report-only: earlier contract subsection describing a pi-tui devDependency is superseded by accepted option(i), reflected in subsequent registration/notes and actual manifests (no pi-tui edge); no retroactive rewrite. legalComments:inline retains zero source legal comments, so shipped THIRD-PARTY/LICENSE files carry notices. pi-rpc-host.test depends on built dist. UI oracle correction is test-only and independently matches truncate-before-corner-replacement order.

Local narrow verification: node syntax check, successful assets builder consumption/removal, npm pack --dry-run --ignore-scripts file list excluding metafile, diff/workflow. Complete frozen build and actual release-pack listing belong to the supervisor's bounded re-gate; no second full suite. Evidence: _ops/c07-identity-workspace/m1b/packaging-r1-*.


## M2a registration after M1b acceptance
M1b c8645877 accepted; supervisor original scratchpad gates/m1b-c8645877-gate.md. Root read the real self-resolution/config/child verification chain and froze M2a-only paths, while M2b vendor inventory remains inactive. Old227 source hashes agree; three-root runtime scan233 adds runner and6 supporting modules, union234. This is literal closure only. M2a cannot enable recursive dispatch or manufacture budget permits. M2b loaders and five custody execution points are being audited read-only. SDK product remains c8645877 at this checkpoint.


## M2a implementation and targeted verification

Two-file worker handed back strict plan validator after28/28 and client tsc0; root then wired production. Configv2 requires explicit plan/null with no old read. Original raw binding goes into plan parser before projected V1 field order can change. Daemon resolves own records for rpc/print/runner finite closure, checks policy/edges, and refuses missing record without fallback; prepared/maxDepth0 self-only. Helpers remain disabled. Shared parser uses the sole Host policy parser, original-order digest, independent parent transition/exact-env checks and shared private physical reverify. Root owns all manifests/goldens/docs, unchanged manifests/lock; no third-party source copied.

Local evidence in _ops/c07-identity-workspace/m2/: shared-tests-complete.log74/74, shared-typecheck-complete.log0, shared-build-complete.log0; client-build-final.log0 (34 main dist digests preserved), client-typecheck-final.log0, client-focused-final.log119/119 across8 files including closure and both cwd/control. Earlier reported117 was a commentary count error; final original says119. M0 nine composition refusals execute against new assertion; remaining three digest families/10 argv variants execute the existing extractor in pi-runtime-host-binding. Future descendant-host EX_CONFIG renderers are not claimed; helper not-enabled remains tested. Existing shared runtime-resolution/MCP byte-pair vectors still pass in the74 suite.

physical-and-distribution-parity.json: extracted2145-byte physical body exactly equals16abd539 (SHA256b28926a5109f223c36b2e353ce436c085daf5e8d68617b03e538ac9c39d103b5); six fixed stat/digest failure cases preserve subject/reason and probe order. Client/identity manifests and lock byte-identical. API goldens only client plan-linked declarations+required field, shared new composition/policy/assert exports; no old export removal. api/version/graph all0. Full client/keys/other packages and release-pack not rerun locally; supervisor owns the frozen full gate.

Targeted iteration evidence retained: shared initial loader negative encountered invalid policy shape before loader; hard loader deny moved first in the new descendant validator only. New test TS union narrowing corrected via explicit runtimeEntry membership (runtime test behavior already passed). Metadata rejects non-JSON/cyclic values; existing MCP parser retains semantic ownership. No old gate or timeout weakened. Plan-stage authority mismatches are non-retryable RuntimeExecutionFailure. M2b source classification/custody table in the design remains PARTIAL/MISSING; one-shot workflow permit is not a concurrency slot, issuer caller not found. Same-UID constraint unchanged.


## M2a frozen product 0208fbc6 / supervisor full gate pending

Product16 files +765/-63, manifests/lock unchanged, attribution0. Scope registration16abd539 preceded writes; worker2-file ownership handed back before root integration. Local119 client/74 shared passed (original logs under _ops/c07-identity-workspace/m2), builds/typechecks/API10goldens/version/graph/workflow0, physical2145-byte body exact old bytes. Runtime configv2 one-shot; template plan required, null only unconfigured, closed reachable set, independent declaration comparison. No recursive dispatch/permit/credential-origin change or product vendor copy. Public API delta additive shared composition+policy APIs and client resources plan-linked declarations.

Complete client/keys/all packages/real pack on the frozen subject are deliberately not duplicated locally; request supervisor's single full gate. S2 baseline24 unresolved, expected unchanged, fresh frozen outcome not yet claimed. M0 composition9 structural/env cases and3 digest families(10 argv) are tested through real assertion/extractor; descendant EX_CONFIG renderer is pending M2b, current helpers remain not-enabled. M2b packet explicitly distinguishes consumable permits from concurrency slots and identifies absent issuer/actual-parent custody, session exclusion, loader feature boundaries.


## M2b packet disposition while M2a gate runs
Supervisor classified export-html resources/static import as engineering; codegen-to-Value interpretation as engineering-first equivalence; sealed scripted workflow as Owner product decision. Design packet updated without SDK product writes. Native export-html source confirms five resources read as UTF-8 and inserted into HTML, not evaluated/imported by the host. §101 external handoff ledger updated accordingly. M2a gate subject remainsbe4a7c47; no full/build/pack during supervisor run.


## M2a accepted / M2b-0 registered before repair

Supervisor gate m2a-be4a7c47-gate.md PASS: identity74, client2724pass with only S2 tripwire24, real release-pack0 with two installed v2+plan lanes. Actual combined diff count supervisor+1114/-67 supersedes author estimates for its gate scope. Downstream residual: maxDepth>0 with missing runner/print record fails closed by contract, not self-only degradation.

Two MEDIUM fixes now bounded as M2b-0: frozen transition reason completion and15 context vectors including true env-depth projection hole. Choose frozen implementation reasons, not M0 byte changes. Root owns two existing registered shared files; pre-fix tests first, product/docs separately frozen. Full recursive custody and loaders remain blocked, P4 E-chain stop unchanged.


## M2b-0 frozen 7cabc200 / targeted supervisor gate pending

P1: shared descendant parser/validator owns context and transition consistency; assertDescendantSpawn calls it before the unchanged private physical reverify. Host policy and independently verified actual parent remain authority. No dispatch is enabled. P2: frozen env-depth-disagrees supplies PI_SUBAGENT_DEPTH=0 in both envValues and actual.env while typed depth=1; old pairwise equality accepted it. P3: preserve M0 bytes and complete implementation refusal vocabulary, then compare supplied nonnull projection with String(typed depth/maxDepth). Missing/null stays absent; no numeric parsing/defaults. Work is bounded by the finite context names, not task count; at10x recursive load this validator still does not provide atomic fanout/session/permit custody.

Before any product change,20 frozen rows ran as it.each. pre-fix.log is the first driver run11failed/32passed, including one driver-only mistake (old V1 subject expected launch rather than actual launch-env). Corrected driver, unchanged product: pre-fix-corrected-driver.log10failed/33passed. One true accept hole env-depth-disagrees; other failures are refusal category/ordering gaps. Raw logs retained, not overwritten. post-fix-vectors.log43/43; then10 additional boundary tests yield identity-all.log104/104 across6files. typecheck.log/build.log both0. Exact reason assertions use error.reason, not substring matching for new context/transition cases.

Depth partition is after finite limits and independent edge/parent consistency. Charged edge with actual parent.depth >= effectiveLimits.maxDepth rejects exhausted before examining an over-cap candidate; otherwise an over-cap candidate rejects exceeded. Limit declarations above record/parent caps retain their distinct limit_exceeded/parent_limit_exceeded reasons from frozen vectors. Zero-charge runner→print at cap is legal. cap0/1/3 tests use the same over-cap candidate to prove parent-budget precedence and exact mutually exclusive reasons. Additional DEPTH/MAX_DEPTH forged-pair tests include noncanonical decimal strings; absent/null round trips preserve bytes and env absence.

Frozen unchanged-authorities.json proves both M0 fixture hashes, both package manifests, bun.lock, identity.ts and spawn-binding.ts byte-identical tobe4a7c47. Old physical/refusal-order suite passes unchanged. No public type/API shape change, third-party/scanner change, helper enabling, product vendor copy or Salesko product writes. Product commit contains exactly2shared files +130/-12, attribution0. Client descendant/plan/host/closure/cwd verification belongs to supervisor targeted gate; no full/pack rerun. S2 residual24 is carried from accepted M2a, not rerun or claimed fixed.


## M2b-0 accepted / three follow-on slices registered

Supervisor original scratchpad gates/m2b0-7cabc200-gate.md accepts7cabc200: identity104/104, client targeted183, goldens unchanged, seven authority hashes independently equal. Both MEDIUM gaps closed; full/pack deliberately not repeated. f0220e29 records the same product. Report-only reason:string observation retained for real runner integration; no union/API edit now.

Registration splits export-html/static resources as M2b-1 and structured-output interpreted equivalence as M2b-2, each targeted gate and no dispatch. M2b-3 alone may enable dispatch after all five execution proofs, accepted engineering prerequisites and Owner scripted-workflow disposition; full gate then. Exact source/provenance/resource/build/test path activation precedes each product write,234-source inventory stays inactive. Native1006 export policy and P4 E-chain disposition stay explicit unmet dependencies. This docs-only change grants no external action and changes no product bytes.


M2b-1 exact9-file scope registered before product edits. Native1005 lacks public export-html; supervisor narrowed to required asset declaration/preverify and single layout/source inventory. Native1006 item11 public subpath is now a prerequisite for M2b-3; no partial runner vendoring or private native inlining. Resource originals remain the exact pinned native package's assets; SDK build ships verified layout/inventory only. P4 installation and computed share import remain open. Evidence readback _ops/c07-identity-workspace/m2b-1/native1005-resource-inventory.json.


## M2b-1 asset-only freeze 1ec9ffce / targeted gate pending

P1: native1005 owns export implementation; package.exports and root index do not expose it. P2: runner1173–1184 computes a physical import, native config313 uses PI_PACKAGE_DIR,344 selects compiled export-html or interpreted src/dist; export implementation90–95 reads five UTF-8 files then embeds data in output HTML. Actual literal ./export-html import rejects ERR_PACKAGE_PATH_NOT_EXPORTED (native-static-subpath-negative.log). P3: supervisor narrowed scope to sealed data prerequisites; do not vendor/in-line private native implementation, retarget exports, copy234 source graph or claim a dead runner copy closes loading. Native1006 item11 and output policy are now prerequisites for M2b-3.

Registrationd27be82e precedes product. Product1ec9ffce exactly9files +233/-5. One pi-export-asset-layout.json defines five names and native form bases; source manifest freezes actual1005 package/fork/resource hashes. Build checks installed pin/name/fork/five byte lengths+hashes, joins source rows with layout, and emits layout assetsByForm hashes + source manifest under dist/assets/pi-export-html. No five resource originals/native implementation or extra dependencies are shipped. Marked18.0.5 MIT and highlight11.9.0 BSD-3-Clause are header facts, not a claim that the native MIT license relicenses them. Future resource distribution requires Owner approval and exact third-party license sources.

Attested verifyPiHostBinding preserves invocation→physical/provenance ordering, then verifies all5 required asset declarations/bytes before session startup. PI_PACKAGE_DIR must equal assetRoot; interpreted src existence refuses with explicit PI_EXPORT_SOURCE_LAYOUT_FORBIDDEN = pi_export_source_layout_forbidden, never another root. Compiled uses export-html, interpreted uses dist/core/export-html; unconfigured branch returns before new validator, retaining native behavior. Record.assets author remains Host; build hashes describe source provenance, not a replacement Host identity. Five fixed resources total266300bytes; rechecking required content adds bounded linear I/O per launch, not per session history. At10x launch rate this repeated I/O is the first local cost; no caching that would skip the required measurement was added.

Tests use real pinned resource bytes in both form layouts, each missing/tampered/undeclared/duplicate row, root drift/src redirection, strict unavailable rejection. Isolated builder positive reproduces both generated form hash lists; one-byte same-length mutation fails digest assertion (no installed-package mutation). Real measured host fixture adds five synthetic declared data rows, retains prior assertions, and new omitted-row case refuses before session. S2 fixture already copied these five real resources; it now consumes the one layout, no acceptance assertion changes. Neither S2 full scenario nor native export execution was rerun here; no new S2 PASS claim.

Evidence _ops/c07-identity-workspace/m2b-1/: native1005-resource-inventory.json; assets-host-final.log32/32; closure-host-cwd.log51/51 across4 files; build.log0 (34main dist digests preserved across todo build, new export inventory emitted); typecheck-final.log0 including vendor; api.log10 goldens match; unchanged-authorities.json (M0, lock, identity/dispatch unchanged; client manifest only build script). Earlier assets-host-tests.log31/31 precedes isolated builder test, not final. Windows not run. Supervisor targeted identity/closure/hosts/cwd/API/strict gate requested, no full/pack. P4 E-chain hard stop, native1006 and scripted-workflow Owner decision remain open.


Read-only explorer independently matched frozen1005 tarball exports, all five resource bytes and native src/compiled path branches. AgentSession live export includes state/tool rendering while exportFromFile opens arbitrary JSONL; substituting the live API is not a semantics-preserving public fallback. Additional report-only: installed pi-subagents pi-spawn.ts5–28/utils.ts10–40 metadata lookup matches upstream @earendil-works package name while fork manifest names @byok-sdk; explicit root may avoid that lookup. This remains part of future M2b-3 source integration, not an asset-only fix. Only SDK runner/print helper dispatch is asserted not-enabled; this is not a claim that every existing third-party npm execution path was disabled or proved safe.


## M2b-2 guard-semantics equivalence: counterexample packet (design only)

**Goal / invariant.** Replace structured-output codegen only if the interpreted runtime guard retains the old runtime acceptance boundary: every schema/value accepted at the current entry must produce the same pass/fail; schema failures keep invalid outputSchema classification, value failures retain error locations/formatting, and schemas rejected by the old path remain rejected. This is not permission to normalize schemas, accept invalid schema branches, or silently narrow the accepted schema domain. Engine-specific SyntaxError text can differ between Node and Bun; compare within the same engine and preserve semantic class. Eleven matching probe rows are observations, not proof of universal equivalence.

**P1 / P2 trace.** Installed pi-subagents0.60.0 src/runs/shared/structured-output.ts126–140 only admits a non-null non-array object, then writes it;143–158 obtains typebox Compile, catches compile(schema) errors as invalid outputSchema, then Check and Errors run. readStructuredOutput171–175 distinguishes returned invalid validation from thrown failures. Typebox1.3.7 compile/validator.mjs constructs Build→Evaluate; schema/engine/properties.mjs13–15 constructs every nested property branch, including absent optional properties. schema/engine/pattern.mjs7–9 creates RegExp during Build. Value.Check delegates to schema/check.mjs; CheckProperties short-circuits absent properties and CheckSchema short-circuits a mismatched type. Thus the broad entry is shape-only; the compiler itself does NOT tolerate the malformed regex at validation time. The earlier phrase "upstream permissive" applies to entry admission, not compiler acceptance.

**Probe evidence.** On product subject7d56a6fb, a bounded15-row Node probe using actual pinned Compile and Value Check/Errors yields11 equal/4 unequal; compiler acceleration=true. A second Bun --no-install probe executes actual upstream createStructuredOutputRuntime and validateStructuredOutputValue for all four rows. All four enter runtime construction; the original validator returns invalid outputSchema for each. No product/dependency/source edits or dispatch changes. Artifacts _ops/c07-identity-workspace/m2b-2/{interpretation-probe.mjs,interpretation-results.json,interpretation-probe.log,real-consumer-probe.mjs,real-consumer-results.json,real-consumer-probe.log,manifest.json}; manifest binds six source hashes and all six evidence files.

| Counterexample / input | Compile runtime guard | Direct interpreted candidate | Attribution / required behavior |
| --- | --- | --- | --- |
| object.properties.optional={type:string,pattern:"["}; value={} | invalid outputSchema before value check | valid (Check=true) | True guard weakening: interpreter never visits absent optional branch; broad object admission is existing upstream behavior. Must refuse as invalid outputSchema, not accept |
| {type:string,pattern:"["}; value=1 | invalid outputSchema | invalid value, root: must be string | Both fail but classification differs: type short-circuit hides schema defect. Preserve schema-error priority |
| {type:string,pattern:"["}; value="x" | returned invalid outputSchema | thrown SyntaxError during Check | Failure timing/category differs; readStructuredOutput would emit Failed to validate rather than Structured output validation failed. Catching this alone cannot fix absent-branch acceptance |
| {type:object,patternProperties:{"[":{type:string}}}; value={} | returned invalid outputSchema | thrown SyntaxError during Check | Interpreter does visit patternProperties but prepares regex at value-check time; preserve schema-error category before value validation |

**P3 / design decision.** Direct substitution fails even the runtime-guard standard, not merely exact per-value error text. Keep M2b-2 product stopped. A valid design needs an authoritative non-codegen schema preparation operation that visits precisely the same schema graph and rejects the same schema defects before value checking, then uses Value.Check/Errors. Current public Type.IsSchema is only Guard.IsObject (type/types/schema.mjs13–15), so it is not that operation. Retaining Build/Compile is not a sealed solution: Build emits code and Evaluate calls Environment.Evaluate. This packet does not introduce a handwritten regex walk, synthetic-values exploration, a second schema parser or a catch-and-relabel shim. At10x schema depth/branching, synthetic probing would both miss unvisited branches and grow without a completeness bound; it is rejected as an authority replacement.

**Pending design question / exclusions.** Supervisor to review this standard and four-row attribution before any product paths activate. Prefer an upstream-authoritative no-codegen preparation capability if one can be demonstrated; current inspection has not established one. Otherwise preserve the blocker and present the exact product/dependency tradeoff for Owner rather than ship changed acceptance. No feature removal, schema rewriting, dependency upgrade/fork publication, M0 changes, parser shadowing or scanner relaxation. No runner vendoring or dispatch; M2b-3 still depends on accepted M2b-2 and1006 item11. M2b-1 remains frozen under independent gate.


## M2b-2 Owner decision packet — structured-output in sealed mode

Status: supervisor reviewed ff1fd5a4 and escalated this product boundary. Current engineering options cannot preserve the runtime guard under the existing no-codegen/no-shadow-parser constraints. M2b-2 product stays stopped; none of the choices below has Owner approval yet.

**Goal / invariant / trace.** Preserve an honest supported feature set and sealed code closure. The existing object-only schema admission leads to Compile construction on every validation; construction eagerly rejects defective schema branches before checking values. Four reproduced cases above show one accepted-value widening and three classification/timing changes with direct Value.Check. This is not Compile accepting a bad regex. Source and probe authority: structured-output.ts126–158; TypeBox1.3.7 schema/engine/{properties,pattern,schema}.mjs; _ops/c07-identity-workspace/m2b-2/manifest.json and both results JSON files.

| Choice | Concrete product and implementation effect | Risk / acceptance boundary |
| --- | --- | --- |
| **α — explicitly exclude structured-output capability from sealed entry (Fable recommendation)** | Sealed runner rejects requests for this feature explicitly; removes the validation/codegen feature graph from that entry. Ordinary text subagent results remain subject to their existing contract. Do not silently omit validation or label unchecked output validated. | Real product loss: schema-validated runner output unavailable in sealed mode. Must trace all foreground/background/collection callers and prove absence from the built sealed graph, plus precise request refusal. No feature removal before Owner decision. Smallest engineering scope. |
| **β — maintained vendored no-codegen preparation delta** | Deliberately authorize a second maintained implementation of Compile's schema-graph defect traversal at the **same construction phase**, followed by interpreted value checks. Freeze pi-subagents0.60.0 and exact TypeBox resolution/registry scope; retain provenance and explicit delta. | Second-implementation drift risk. The widening counterexample is mandatory refusal coverage; the other three are classification/timing equivalence checks. Extend corpus to refs/formats/optional branches/schema errors and compare against actual upstream consumers. Four passing cases cannot prove full equivalence; this choice explicitly changes the present no-second-parser boundary. |
| **γ — wait for authoritative non-codegen preparation API** | Keep this feature blocked until upstream or an explicitly scoped native1006 integration supplies an authoritative API preserving construction-time refusal semantics; consume the public API after verification. | External schedule, no delivery promise. The upstream TypeBox/pi-subagents owner must actually provide/integrate the capability; merely releasing fork1006 cannot create it. Present with native1006 item11, but no implementation/publication authority is implied. |

**Question.** Select α / β / γ for C07 first sealed release. Fable recommends α. Codex recommends the same only with explicit feature refusal, never unchecked-success behavior. Existing full daemon+Pi requirement remains; α is a specific additional feature reduction requiring Owner approval, not already granted by prior runner/i18n approval or a photon analogy.

**Dependencies / exclusions / 10x.** M2b-3 dispatch remains gated on this decision and its proof, five custody execution points, native1006 item11 and scripted-workflow disposition. α first fails on requests using the excluded capability and must report that exact refusal. β's maintenance/test surface grows with schema vocabulary, references and registry changes; a finite corpus is not a universal proof. γ is schedule-bound. No scanner relaxation, silent schema rewriting, upstream package mutation, push/merge/publish or native implementation is part of this decision packet.

**Additional read-only observation.** Explorer found foreground execution.ts1459–1483, background subagent-runner.ts1860–1879 and dynamic-fanout.ts291–296 consumers. Its manifest inspection also found client typebox1.3.7 versus pi-subagents dependency1.1.38 and separate custom-format registry behavior. The existing manifest-bound probe proves its specific resolution only; any β acceptance must first freeze actual consumer resolution and registry custody, not generalize one package instance to all lanes. No extra product experiment or dependency change performed for this packet.


## M2b-1 accepted; M2b-3 design only

Supervisor targeted gate PASS7d56a6fb, original /private/tmp/m2b1-gate/gate-verdict.md (old scratchpad path obsolete). Read back:83/83 targeted across6files; api10goldens/strict/diff/attribution pass; five native resource hashes independently recomputed. S2 attempts24=clipboard12+jiti12, i18n0, escaped/caches empty, providerRequests0. This is assets/preverify acceptance, not native API/path-policy/containment or dispatch acceptance.

Supervisor authorizes the package.json build-chain single line connecting build-pi-export-assets.mjs: mechanical “package.json unchanged” meant no dependency/lock drift, which holds. Build connection is the sole layout/inventory emit mechanism; no new dependency permission. Product remains1ec9ffce, gate subject7d56a6fb.

M2b-3 design appended to implementation-entry with current insertion seams, five real-process test shapes, terminal-print then runner internal integration and all-edge activation last. Plan/config checks are not live-root custody; session exclusion, persistent root ownership and commit/crash semantics remain stop conditions. Existing SDK helper not-enabled remains. α/β/γ already presented to Owner, no selection received; P4 E-chain and native1006 still await Owner. No product paths activated, no runtime/test/build/install/full/pack rerun.


M2b-3 bounded supervisor review accepted5e7c3ba7 as design final. Additional conditions recorded in implementation-entry: independent sealed print theme/locales/skills dependency probe; complete five-edge cutover gate with real fanout/rollback/charge-once/transitions and jiti12→0; downstream notice into spec and maxDepth>0 refusal into P4 contract at cutover. No new work dispatched, product/spec/P4 unchanged. Owner approval after the preceding final was bounded design review, not α/β/γ selection or native publication. Design stage ends pending four external decisions.


Owner selected alpha after an unambiguous question: sealed mode explicitly refuses structured-output requests, ordinary text subagents retained. Scripted workflow, native1006 and P4 remain excluded. First pass is read-only entry/graph mapping (explorer sealed_structured_output_boundary), root sole writer; no product changes yet. Shared factory graph and recovery/delegation inputs mean tool_call-only refusal is insufficient and would not remove compiler code. capabilities.json absent in this worktree is context report-only; context-map/root/packages instructions read.


### Alpha exact factory graph activation

Actual esbuild graph probe at _ops/c07-identity-workspace/m2b-2/factory-graph/runtime-inputs.json measures226 executable inputs (3734773 bytes), exactly M0 inventory minus src/types/pi-runtime-compat.d.ts. Register227 plus provenance rather than activating234 union. Supervisor resolves fixed RPC Compile as independent residual; only user-schema compiler subtree must disappear. Dedicated sealed artifact/private import is build selection, not a runtime dual-factory branch. 10x pressure remains upstream vendor delta review size and recursive custody, not a claim of full graph closure. Registry/public/dispatch actions unchanged.


## Owner updates and alpha verification stop

Owner selected scripted workflow: retain in the first sealed release, enable only after its closure is implemented and verified. This rejects the feature-loss option; it does not relax Worker/eval/vm scanner rules. Owner also authorized local Salesko P4 implementation based on375b5316 (including95aa478d), keeping original branches. No push/merge/deploy. This supersedes the prior pending E-base decision; M0 schema remains the single interface author, P4 single writer and gates still apply.

Alpha registration a392b34a completed. Actual227 factory sources plus package/LICENSE (229 originals) are copied byte-identically as unintegrated WIP; no host/build/dispatch/admission behavior changed. A test driver was prepared but its four attempts never reached refusal assertions: initial esbuild resolution, ESM require-condition mismatch, then unresolved native peer in externalized test bundle. These are INVALID pre-fix evidence, not eight product failures. After three fixture repair rounds, pause further driver modification/reverification and report per repository stop rule. Logs and verification-status.json live under _ops/c07-identity-workspace/m2b-2/alpha-vendor. Candidate bounded repair is explicit original-peer ESM resolution in test, not a product fallback. No full/pack run.

Per-consumer authority correction: shipped dist external typebox/typebox-compile resolves client1.3.7; original package source declares/resolves1.1.38. Preserve shipped S1 behavior. Dedicated sealed build must register its exact1.3.7 edge and retain fixed RPC Compile residual. Source-oracle1.1.38 is not a reason to change the shipped consumer. Current dist cannot resolve bare acorn at its anchor; this is report-only for scripted-workflow closure, not proof of the whole workflow path failing. New worker dispatch was unavailable due session thread limit; root owns current unintegrated WIP.


## Alpha implementation freeze107f4fb2 — gate pending

Owner selected explicit sealed structured-output refusal and the dedicated factory build boundary. Scripted workflow remains required for the first release after its own closure; recursive dispatch is still not-enabled. Earlier driver-stop and pending-choice paragraphs above are historical and superseded here.

**P1 map.** Both S1 and sealed hosts now use the same vendored227-source factory/capability graph (229 original files with package/license), with per-file upstream/current hashes and explicit deltas. pi-subagents0.60.0 moves from runtime to exact dev source-oracle edge; all706 locked package tuples are byte-equivalent as parsed. Runtime direct dependencies20→19. Shipped TypeBox remains1.3.7; upstream1.1.38 is only a separate declared-source baseline. The new private imports mapping points to dist types/JS, adds no public export. Source metadata/license are shipped in dist/assets/provenance/pi-subagents/0.60.0 and THIRD-PARTY.

**P2 trace.** Actual effective chain/static-parallel/dynamic-collect/workflow inherited and retained/slash/refinement requests reach build-selected assertStructuredOutputSupported. Ordinary seam is a no-op; sealed seam explicitly refuses with sealed_structured_output_unsupported before structured runtime artifacts/child spawn. Direct foreground/background requests have entry checks; retained resume resolves its effective schema before capacity acquisition. Recovery readers/validation/runtime creation also refuse, never return unvalidated success. Definition-only object-shape parsing remains unchanged. Refine show/rollback and ordinary text are retained. The dedicated sealed build excludes the original user-schema compiler module, proved by esbuild metafile and emitted sourcemap; fixed RPC Compile remains a separate known codegen edge.

**P3 choice.** A dedicated clean:false build selects the backend; no runtime branch joins two factories. Main build has no sealed alias. S2 product build author uses Bun.build exact onResolve /^#byok-pi-runtime-host$/ to the private sealed artifact, preserving the real root dispatcher. Supervisor explicitly approved this fixture/P4 mechanism. SDK does not ship a product-side redirect plugin. Salesko P4 must own this build selection. Original scanner rules unchanged; no global codegen-free claim. At10x upstream surface, vendored delta drift is the first maintenance risk; exact raw hashes, bounded module exclusion and ordinary controls retain one source author instead of divergent factories.

Evidence under _ops/c07-identity-workspace/m2b-2/alpha-vendor: baseline8995a4b1 pre-fix-complete-driver.log9 refusal RED+12controls GREEN; first four driver errors remain invalid evidence, authorized manifest-ESM repair resolved them. focused-r2.log51/51 (23 admission,5 build/provenance,23 closure) and typecheck-r2.log0. One new test had an extra parenthesis and another omitted real executor deps; corrected fixtures, no assertions loosened. First client build passed, sealed step preserved60 existing dist digests and todo step34. lock-equality.json706 exact tuples. frozen build/focused logs bind107f4fb2 and follow separately.

S2 s2-r1.log: real root dispatcher plus exact build redirect; ordinary/prepared both reach get_state,8 runtime paths within release,cache/escaped empty,providerRequests0; sole refusal remains registry tripwire24=clipboard12+jiti12. Original attempts-zero assertion remains RED. Private host source map excludes the ordinary host. This is partial containment, not complete C07. No full/pack executed by root; supervisor must run one complete frozen gate+real release-pack including installed sealed-entry Node usage refusal. No push/merge/publish/native1006/dispatch/install.

Frozen product107f4fb2: frozen-build.log exit0, frozen-focused.log51/51, strict.log OK, git diff --check0, attribution0. Exact product source frozen before those checks; no full/pack duplicated.


## Alpha r1 gate diagnosis and repair registration

ad6b2fe5 gate FAIL: /private/tmp/m2b2-gate/gate-verdict.md, test-full.log, baseline-probe.log, release-pack.log, install-test.log. Baseline host25/25, head8 new host failures; existing S2tripwire24 is separate. Source caller imports pi-rpc-host.ts→pi-extension-factories→vendored subagents→bare native peer, bypassing subagentsBuild(false), so module initialization fails before usage/parser assertions. Dist get_state fixture succeeds. New pack probe uses node --eval, so host correctly rejects nonempty execArgv before missing digest; installed file-based probe independently gives the exact expected78/message. Supervisor approved test/script-only correction, not gatekeeper's advisory new runtime peer anchor. Existing assertions/expected text, client TypeBox1.3.7, product exports and dependencies stay fixed. No full/pack root rerun; supervisor owns final verification.


## Alpha r1 correction frozena9730e43 — independent re-gate pending

Registration8be2d7c2 precedes correctiona9730e43 (three test/script files+29/-7). Callable/usage fixtures now import the actual private dist host; internal parser/session test entry uses subagentsBuild(false) with packages external from a client-local scratch anchor. No public exports, products, build configuration, dependencies or lock edits. Packed sealed probe writes the identical import/call text into an isolated .mjs and invokes Node without interpreter flags. No product guard/execArgv or expected error changed.

Evidence _ops/c07-identity-workspace/m2b-2/alpha-r1-fix/: targeted-r1.log25/25 (two affected files), typecheck.log0 (client+vendor), assertions.json proves every original expect()/assert. line byte-identical toad6b2fe5, typebox-resolution.json shows actual test-entry resolution1.3.7. node --check pi-launcher-smoke.mjs and git diff --check pass. Attribution0. Root did not run full/pack or claim the new smoke passed; supervisor runs the one complete re-gate at the new frozen subject. First gate's sequential client failure did not run the other13 test packages; those must be run explicitly despite retained S2 refusal. Expected residual remains S2tripwire24=clipboard12+jiti12.


## Alpha r2 fixture root cause and exact correction scope

Root-cause-prover confirmed TestServer drops queued seq2 when a read-ahead request afterSeq2 wins before client rewind. Evidence `/private/tmp/m2b2-redelivery-probe/trace-a.json` and ordering/fixshape tests; supervisor dispatch `/private/tmp/fable-supervisor-outbox.md`. Real SqliteMailboxStore.readAfter uses SELECT, not destructive dequeue. Two full failures cannot be dismissed as flaky; the proven defect belongs to the test fixture, not the production transport. The earlier driver-load hypothesis is not needed to explain the deterministic loss.

Use the exact probe correction: retain excluded entries, deliver selected entries once. Tests continue to schedule retries explicitly; this does not claim full mailbox/ACK emulation. New HTTP regression will choose losing cursor order explicitly, require later rewind delivery, and preserve raw event order. No existing timeout or assertion edits; no product, manifest, lock, build or API changes. `.ai/context/capabilities.json` is absent in this worktree; no scoped context added or harness repaired. Native1006 Owner question remains pending independently and does not prevent this authorized test correction.


## Alpha r2 correction frozen7357ebb4 — final independent gate pending

Registration4cd9da09, test-only correction7357ebb4 (two files+74/-4). New HTTP tests force read-ahead2 before rewind1, proving the old fixture loses excludedseq2;2/2pre-fixRED. Single-pass ordered partition keeps excluded entries, selected deliveries remain scripted one-shot. Guard and unchanged connection-manager-redelivery tests7/7PASS. No production bytes, original assertions/timeouts, manifest, lock or API changes.

One client full on frozen7357ebb4 completed122.79s:2763passed/11skipped/1failed across236files (233pass/2skip/1fail); sole failurepi-s2-bundle-resolution.test.ts:327 retains24 registry attempts(clipboard12+jiti12). Redelivery passed, no second full run. Evidence `_ops/c07-identity-workspace/m2b-2/alpha-r2-redelivery/freeze.json` binds subject, logs and the unchanged original redelivery file SHA. This closes root self-verification, not independent gate or complete S2. Final supervisor round3/3 must still run the agreed complete matrix/real pack; no push/PR/merge/publication performed by root.
