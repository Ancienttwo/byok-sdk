# Notes: C07 runner/i18n implementation entry

## Accepted base and scope

Product6696c211 passed the supervisor helper-lookup gate, not complete containment: client2673pass/11skip/one S2 tripwire36; ordinary/prepared get_state; release-pack0; API unchanged; attribution0. Gate original copied to `_ops/c07-identity-workspace/runner-i18n-entry/p3d-terminal-gate.md`. f9130440 is the accepted docs-only entry packet; no later product change.

Owner approved M0 two inert vectors + docs, conditional SDK M1–M3 and separate P4 implementation. Both product stages wait for supervisor M0 PASS and exact path registration. P4 product base must be previously pushed32cfcd49 after live check, or explicit followups disposition; docs-only375b5316 ancestry is not product authority. No P4 branch/product created.

## M0 draft delivery

Five successor docs and two JSON files only. Four kinds: pi-rpc/pi-prepared/pi-subagent-runner/pi-subagent-print. Five explicit edges include foreground and nested print routes; prepared has no descendant. Runner->print has depth cost0 because it executes the already-admitted async delegation. No in-process print equivalence assumed: global stdout/signal ownership conflicts with isolated concurrent process semantics.

Supervisor selected composition C for the next freeze: unchanged V1 template + templateDigest + Host descendantPolicy + typed perLaunch, with verified-parent exact optional-name selection. Existing byok.implementation-spawn/v1 remains unchanged; declaring exact names is not permission to edit its identity.launchEnvNamesDigest. Keep controlled directory roots and loader-values measurement; per-task session native option remains under the inherited session root. No empty-value shim, online daemon IPC or arbitrary code selector.

Record four finite upper bounds are explicit; vector3/7/2/11 is synthetic. Effective user configuration/grants must fit, no clamp. Explicit sessionCap is a product tightening over upstream unlimited; no production numerical limits chosen. Preserve root grant action with post-grant cap enforcement. Atomic fanout claims, session/parallel lifetimes and recursive parent authority still need the M0 custody gate.

Credential inheritance is explicit and keeps parent custody; no new lookup/provider authority. CLAIM C applies on every descendant: MCP gets projected env without provider credentials/Pi directory selectors. Secret values never enter fixtures/config/digest/logs; negative examples are clearly synthetic.

## Inventory and validation

Ignored evidence directory: `_ops/c07-identity-workspace/runner-i18n-m0/`.

- `vendor-inventory.json`:227 literal-relative source paths with bytes/hash,290 installed regular files counted,MIT license,package metadata,7 computed/dynamic edges. Not full runtime/transitive-license closure. Earlier explorer PARTIAL text is superseded only for exact inventory completion, not live custody proof.
- `locale-source-inventory.json`:9 exact local todo2.8.0 source files; English13 keys, others14,all strings. Record example hashes remain synthetic and distinct from source inventory hashes.
- `per-launch-env-source-inventory.json`:52 finite candidate names,48 buildPiArgs names +2 getSubagentDepthEnv outputs +2 proposed limit projections with consumer provenance. Correct package-root literal is PI_SUBAGENTS_PI_CODING_AGENT_PACKAGE_ROOT (utils.ts:12), not the constant identifier shorthand. Unknown executable selector PI_SUBAGENT_PI_BINARY remains forbidden.
- `build-draft.py`:local authoring of inert fixtures only, not a product serializer or second acceptance authority.
- `verify-vectors.mjs` / `four-kind-byte-check.log`:independent Node serialization/hash vs Python generation,5 record vectors and5 context encoding examples;4kind/5edge/depth arithmetic;227 source files + package/LICENSE +9 locale bytes rechecked.
-40 record/raw/SDK-consumer refusal designs +15 context refusal designs +5 arithmetic/transition examples. No product validator execution. Two required C negatives are exact-outside-allowlist and actual!=exact. Context examples contain no measured binding/real MCP config and do not prove executable readiness.

M0 remains NOT PASS until supervisor accepts the frozen composition schema/dual preimages, env delegation and recursive budget custody obligations. No install/full/pack/product test rerun; accepted6696c211 evidence is reused only for unchanged product facts. `git diff --check` and workflow strict apply to this draft. Missing .ai/context/capabilities.json remains existing report-only; no scoped context added.


## C composition follow-up to 44a2a7fa

Fable independently verified all5 prior Host hashes. Revised unpublished record field to descendantPolicy (finite allowlist + four required bounds). Added unchanged V1 templates with fractional mtimeMs, three-layer composition and9 negatives. Independent Python/Node bytes agree; unchanged V1 parser4/4 and existing shared env digest5/5. Existing V1 env-name rejection is not relaxed; new assertDescendantSpawn explicitly delegates optional presence and shares one extracted private physical reverify (future M1). Policy must compare against already verified parent declaration; candidate config digest is not policy authentication. Depth reset within cap is separately rejected via parent transition.

Evidence composition-byte-check.log and composition-existing-v1-check.log; no physical attestation/new-validator/live recursive claim. Old shape and prior ignored generators are historical authoring evidence, not compatibility product paths. New vectors are the sole candidate M0 interface once accepted. Product still6696c211; five docs+two inert JSON only.
