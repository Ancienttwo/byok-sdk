# Implementation Notes: wp5-s2-ci-flip

> **Contract**: `tasks/contracts/20260919-0244-wp5-s2-ci-flip.contract.md`
> **Last Updated**: 2026-09-19 02:44

## Facts established before execution

- 三个套件的 BUN_BIN 候选列表在 a6c5a297 完全一致（同序同集）：`process.env.BYOK_TEST_BUN_BIN` → `~/.local/bin/bun` → `/opt/homebrew/bin/bun` → `/usr/local/bin/bun`。仅代码风格不同（s2 用 `.find`，mcp/runtime 用 for-loop）。统一列表 = 该并集，原序保留，无本地发现路径回归。
- setup-bun@v2 装 bun 到 `~/.bun/bin/bun`，不在任何候选列表内 → CI 上三套件恒 skip。
- 已有 idiom：`BYOK_REQUIRE_DATAPLANE=1`（packages/cloud-dataplane/src/__tests__/support/dataplane.ts:10,34）import 期 throw。
- S2 监视器：pi-s2-bundle-resolution.test.ts:194-195（registryAttempts 记录 handler）、:321（零尝试断言）、:327（providerRequests=0）。
- CI：build-test（.github/workflows/ci.yml:35 起）是唯一跑全量 `bun run test` 的正式 job。

## Execution log

(append below)

## Implementation (2026-09-19 03:0x)

- helper: `packages/client/src/__tests__/support/test-bun-bin.ts` — `bunBinCandidates(env, homedir)`（并集原序）、`resolveBunBin(env, homedir, exists)`（REQUIRE 语义 + 可注入存在性检查供 pin 确定性）、`TEST_BUN_BIN_ENV` / `REQUIRE_BUN_ENV` 单一定义。
- 三个套件 `const BUN_BIN = resolveBunBin();` 替换 inline 解析；mcp/runtime 移除仅剩的 `existsSync` import；spawn/monitor/零尝试断言零改动（`git diff` 仅 BUN_BIN 块 + 新增负控）。
- pin 测试 `support/test-bun-bin.pin.test.ts`：候选并集探针序、REQUIRE=1 throw、无 flag undefined、env 优先、CI build-test 接线 pin（dataplane constraints idiom）。
- s2 负控：`registry monitor is live` — 不依赖 bun，同 recording-handler 形状，真实 fetch → registryAttempts 捕获 length 1。
- CI build-test：job env `BYOK_REQUIRE_BUN: '1'`（含 comment）+ Setup Bun 后新 step：`command -v bun` 空则 exit 1；打印 path/`bun --version`/`shasum -a 256`；`BYOK_TEST_BUN_BIN=<abs>` >> GITHUB_ENV。

## Verification evidence (tasks/runs/)

- `bun run build` → exit 0（sealed build preserved all 60 existing dist digests）。
- `bun run typecheck` → exit 0（12 packages Done）。
- `bun run check:api-surface` → exit 0（10 golden match）。
- `bun run check:version-authority` → exit 0。
- RED（缺工具 fail-closed）：`HOME=<empty> BYOK_REQUIRE_BUN=1 BYOK_TEST_BUN_BIN=/nonexistent/bun` 跑 pin 文件 + mcp 套件 → mcp 文件 import 期 throw（`resolveBunBin test-bun-bin.ts:59`），文件级失败，RED_EXIT=1 → `20260919-0244-wp5-s2-ci-flip.red-evidence.log`。
- skip 路径：同 env 去掉 flag → `7 passed | 1 skipped`，SKIP_EXIT=0 → `20260919-0244-wp5-s2-ci-flip.skip-path.log`。
- 定向（本地 bun 存在）：4 文件 13 tests → 12 passed / 0 skipped / 1 failed（仅 s2 大用例）→ `20260919-0244-wp5-s2-ci-flip.targeted.log`。

## Known local item (pre-existing, NOT introduced by this slice)

- `pi-s2-bundle-resolution.test.ts:327` clipboard tripwire：本机（darwin，有 display）子进程拉起时尝试 auto-install `@mariozechner/clipboard` → registryAttempts 非零。**基线同现**：换入 a6c5a297 原版 s2 文件单独跑 → 同一断言同因失败（BASE_S2_EXIT=1，12 次 GET，`/private/tmp/wp5-base-s2.log`）。根因与修复归 `origin/claude/wp5-s2-tripwire-zero`（PR #202，pi fork pin 0.85.1005→0.85.1006），其 probe evidence 证明 attempts 由 `hasDisplay` 门控 → headless ubuntu CI 恒零，CI authoritative。本刀不改产品依赖，不碰该断言。

## Full test / strict gate

(append after runs complete)

## Full test (2026-09-19 03:1x)

- `bun run test`（--sequential，遇 client 包 exit 1 即停）：client 包 243 文件 → `1 failed | 240 passed | 2 skipped`，`1 failed | 2878 passed | 11 skipped (2890)`。唯一失败 = 已知本地 s2 clipboard 项（基线同现，见上）。11 skipped 全在未触碰文件（env/platform 门控）。→ `20260919-0244-wp5-s2-ci-flip.full-test.log`
- 其余包 `bun run --sequential --filter '!@byok-sdk/client' --if-present test` → 全绿 0 失败（cloud 397 / cloud-dataplane 72+107 skipped（无本地 Postgres substrate） / conformance 161 / core 304 / examples 46 / implementation-identity 110 / keys 494+4 / protocol 387 / server 373+19 / testkit 4 / ui-runtime 20 / sdk 4）。→ `20260919-0244-wp5-s2-ci-flip.full-test-rest.log`
- 树合计：**5250 passed / 1 failed（已知本地项）/ 141 skipped**。dispatch 基线（4569/34）与本机不可直接对照：本机无 dataplane substrate（107 skipped 属该环境差），且基线捕获环境不明；failed=1 为既有本地项，非本刀引入。
- CI delta（本刀的实际目标面）：ubuntu runner 上 5 个 bun-gated 用例（s2 大用例 + mcp 1 + runtime 3）从静默 skip 变真实运行；新增 6 个全环境测试（pin 5 + s2 负控 1）。
- YAML 验证：python yaml.safe_load 解析 build-test job，env 与 step 顺序正确。

## Strict gate

(append after check-task-workflow)

## Strict gate (final)

- `repo-harness run check-task-workflow --strict`：首轮红（slice plan 缺 `## Evidence Contract` / `## Promotion Gate` → 补齐；再缺 `Why not checklist row` 字段 → 补齐）→ **OK，STRICT_EXIT=0**。证据：`tasks/runs/20260919-0244-wp5-s2-ci-flip.strict.log`。

## Commits

- `2961b3dd` chore(plan): register wp5 s2 ci-flip slice contract and artifacts
- `9fd4ad28` test(client): force the bun-dependent suites to run in CI (WP5 S2 formal gate)
- (final) chore(plan): tick WP5 + evidence logs + review/notes closeout

## Deviations from dispatch

1. 切片 plan 文件：dispatch 未列新 plan 路径，但 repo-harness 以 active-plan stem 耦合 contract（`plans/plan-<stem>.md` → `tasks/contracts/<stem>.contract.md`），program plan 的 stem 指向 docs-only program 契约，slice 契约无法绑定 → 按既有 per-slice 模式（N1/WP3/WP4）建 `plans/plan-20260919-0244-wp5-s2-ci-flip.md` 并 switch-plan。program 契约未改动。
2. 定向命令用 `bun run --filter @byok-sdk/client test -- <paths>`（ci.yml 内既有 idiom，走 client vitest config）而非 `bunx vitest run`；两种形式都验证过可运行（bunx 形式首次运行亦为 12 passed/1 failed 同结果）。
3. pin 测试新增了 dispatch 未点名的三个轻量 pin（候选并集探针序、env 优先级、CI 接线）：分别服务于 clause 1 的「并集不回归」核验、CI 机制保护、与 dataplane constraints 既有 idiom（dispatch 明确指向该 idiom）。
4. 「全量 bun run test 必须绿」在本机不可达：唯一红为既有本地项（基线同现，PR #202 归口）；其余 13 包全绿，已分别落证据。

## Acceptance-review fixes (F203-1 / F203-2, 2026-09-19 follow-up)

- F203-2 `resolveBunBin` strict mode：REQUIRE=1 且 `BYOK_TEST_BUN_BIN` 已设 ⇒ 显式路径是唯一候选，missing/not-a-file 直接 throw（消息点名显式路径），无 fallback 扫描；无 flag 时候选链原样保留。默认存在性探针由 existsSync 升级为 statSync isFile（目录不再算候选，fail-closed 方向）。新增两个 pin：显式缺失 + 默认候选存在 → throw 点名路径；显式有效 + 其他候选存在 → 恰返回显式路径且零 fallback 探测。实机反证：本机 `~/.local/bin/bun` 存在时 `BYOK_REQUIRE_BUN=1 BYOK_TEST_BUN_BIN=/nonexistent/bun` 仍 throw（旧实现会静默换用本机 bun）。
- F203-1 s2 负控重做：不再用 parent fetch 自证。hoist `startRegistryTripwire`（主用例与负控共用同一 recording handler，record=false 建模 sink off）；隔离 spawn 子进程，经 `npm_config_registry`/`BUN_CONFIG_DEFAULT_REGISTRY` 接线向 loopback 监视器发出一次真实 registry HTTP 尝试（与 clipboard auto-install 同类），断言真实监视器记录到精确形状 `[request.url]`。test-of-the-test：sink off（监视器应答但不记录）与 monitor unreachable（指向死端点）两种失能下记录为空 ⇒ 正控 `toEqual([violation])` 必然 FAIL；另做 mutation 验证（临时禁用记录 push → 控制红）。零尝试断言（:342/:348）原样未动，产品源码零改动。
- 运行时 receipt：负控用例以 `process.stdout.write` 打印 `[wp5-s2 receipt] resolvedBunBin=... negativeControlRecorded=[...]`（vitest 捕获 console.*，raw stdout 才能在绿跑中可见）。
- 验证：定向（support/ + s2 文件）→ 9 用例 8 过 1 失败（唯一红 = 既有 darwin clipboard tripwire :348 基线项，PR #202 归口）；`bun run typecheck` 全包 exit 0。
