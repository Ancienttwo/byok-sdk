The regression fails on the original README with two assertion failures and one keys-boundary pass. Worker must preserve the recorded guard bytes. Existing packed-package smoke refutes Issue178; no second repair is invented.

Operator setup correction: activate the existing canary Sprint and align worktree base/review/merge routing to the already-authorized codex/brc1415-canary branch. The previous default main had no canary Sprint, so offers were empty. No grant, target initialization SHA, Oracle binary, profile, budget or worker role changed. The real Engineer offer now admits only original Issue177.
