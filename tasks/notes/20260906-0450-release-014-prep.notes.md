# BYOK release014 preparation — single handoff

Status: REVIEWABLE VERSION/GRAPH CHECKPOINT; paused by release coordination. NOT final release subject.

## Ownership and exact identities
- Release coordinator: tmux %7; recovery owner %8; Salesko Multi-Agent owner %16.
- Worktree: /Users/kito/Projects/byok-sdk-wt-release-014-prep
- Branch: codex/release-014-prep
- Base: 21adf7144fbe712c176913d0e79d275350c3784d (local main, already includes #147).
- Version metadata/check subject: 0e3dd3b424b2434dd4930d45b3c74d89288657f8.
- Final subject MUST first integrate accepted acknowledged-interrupted/terminal resend/cloud settlement recovery and PR151. This checkpoint excludes both new lanes. Do not pack or publish it as the final train.
- Version choice: nine aligned SDK packages 0.14.0, independently versioned keys 0.4.0. Additive SDK API; keys catalog/store contract breaking change. User coordinator explicitly confirmed these versions reasonable.

## Changed files and reuse
No runtime implementation files changed. Version fields changed in packages/{core,protocol,server,cloud,client,cloud-dataplane,ui-runtime,testkit,sdk,keys}/package.json; matching workspace version records changed in bun.lock. README.md and docs/spec.md project manifest versions; README says prepared candidate, not published. CHANGELOG.md labels prepared/unpublished train and adds #147 summary. Reconcile final recovery/PR151 release notes during integration.

Portable metadata-only patch: /tmp/byok-release014-version-metadata.patch
SHA-256: c73b94c3d8e821c45caa651ae9221dcfb1712a4784b10644096c658044cec1e6
This patch excludes task workflow files. Review/apply it onto the accepted combined subject; do not blindly replace CHANGELOG or drop the integration owner's new changes. The source commit also contains isolated plan/contract/notes/review artifacts.

## P1/P2/P3
P1: package manifests own versions; lock workspace records project them; canonical release scripts own tarball graph and installed closure.
P2: manifest -> lock -> bun pm pack -> packed internal edges -> isolated npm import/CLI/migration smoke -> SHA-bound manifest. Stale lock can otherwise publish stale edges.
P3: retain metadata work, delay costly packing until recovery and PR151 have passed acceptance and exact subject is frozen. No new dependency, abstraction or runtime path.

## Checks already executed once — do not repeat for this checkpoint
Frozen install, build, typecheck, full root tests, API surface, version authority, release graph, strict task workflow and git diff --check all passed. Node 22.22.0, Bun 1.4.0. The checks started before the coordinated hold; only their results were collected after it. Full tests took 48.55s.

Evidence in worktree:
- tasks/notes/release-014-source-checks.json: exact source SHA, exit codes, log hashes.
- _ops/releases/0.14.0/logs/{build,typecheck,test,metadata}.log: actual local output.
- tasks/notes/release-014-registry-before.json: timestamped live registry metadata; all ten candidate versions were absent, latest was SDK0.13.0 / keys0.3.10. This is time-bound preparation evidence; publication requires fresh vacancy readback.
- tasks/contracts/20260906-0450-release-014-prep.contract.md and matching plan: coordinated hold recorded.

No tarballs or release-manifest.json were generated. No pack matrix, npm publish, push, PR, tag, deploy, live-secret modification or downstream pin was performed here. Source checks are not external semantic acceptance, registry proof, final integration checks or Salesko released-binary recovery acceptance. Existing #147 receipt applies to its recorded older source/target only.

## Reusable commands — future owner, after final accepted subject freeze
```sh
export PATH=/Users/kito/.npm/_npx/6d82ddbdd7da268b/node_modules/node/bin:$PATH
node --version
bun --version
git status --short
git rev-parse HEAD
bun install --frozen-lockfile
bun run check:release-graph
bun run check:version-authority
node scripts/release/pack-and-smoke.mjs --out-dir _ops/releases/0.14.0/artifacts
```
The pack script requires a clean committed worktree and empty output directory, runs its own build, packs ten packages, validates exact edges and SQL assets, does isolated npm install/closure/CLI checks, and writes artifacts/release-manifest.json with sourceGitSha and SHA256/SHA512 for every tarball. Do not run until final source freeze, and do not rerun source gates on this preliminary checkpoint. Reuse valid evidence only for matching source/fingerprint; acceptance for the changed final subject is coordinated by %7.

## Next ownership boundary
%7 integrates accepted recovery and PR151, ports/reconciles this metadata, then freezes one final subject before packed evidence. Current user authorization covers commit/push/PR in that coordinated train, not npm publish/tag/production deploy. This preparation lane remains paused; its worktree is preserved for inspection.
