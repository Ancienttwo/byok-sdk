# Implementation Notes: release-0-21-0

> **Status**: Active
> **Plan**: plans/plan-20260924-1805-release-0-21-0.md
> **Contract**: tasks/contracts/20260924-1805-release-0-21-0.contract.md
> **Review**: tasks/reviews/20260924-1805-release-0-21-0.review.md
> **Last Updated**: 2026-09-24 18:30
> **Lifecycle**: notes

## Design Decisions

- **Same two authorities, same three physical copies as 0.20.0.** `packages/core/package.json` and `packages/keys/package.json` moved first; the ten aligned manifests, the eleven `bun.lock` workspace records, and `README.md` + `docs/spec.md` were edited to agree. The `bun.lock` diff is exactly eleven lines, and `bun install --frozen-lockfile` accepted it without rewriting anything. The package set is the release-graph authority's (`scripts/release/check-package-graph.mjs`: seven dispatch packages + `testkit` + `implementation-identity` aligned, the `byok-sdk` umbrella, and independent `keys`), unchanged from 0.20.0.
- **keys takes a PATCH, by the policy.** `docs/spec.md#pre-10-package-version-policy` limits PATCH to "corrections with no new public behavior, API, persistence, or security authority". `git diff --stat v0.20.0 HEAD -- packages/keys packages/core packages/implementation-identity` was empty at the base `644f8473`, so keys' source and the code behind both of its workspace edges are unchanged. The same policy paragraph requires keys' packed and published `@byok-sdk/core` edge to equal the current dispatch release, which the published keys 0.6.1 (core 0.20.0) cannot satisfy.
- **The runbook pointer moves back to "prepared" form.** `deploy/runbooks/release-responsibility.md` pointed only at the published 0.20.0; it now points at the 0.21.0 handoff first, names 0.20.0 / keys 0.6.1 as `latest` with its publication record, and restates the version selection for this train.
- **The release notes state the v6 precondition as the spec does, not only as the PR body does.** PR #226's body gives "drain, then upgrade as a pair; unknown key or type freezes the cursor; the enqueue gate is the only guard". `docs/spec.md` and `docs/protocol.md` add two operator facts the notes carry too: required message dispositions must be settled and v5 preparations are recreated rather than read forward, and an old prepared offer already enqueued without `egressPolicy` is NOT repaired by a paired upgrade and needs operator handling.
- **The fresh-lane `allowTools: []` note names the escape hatch.** Omitting `allowTools` still selects Pi's default native registry on a fresh run (`permission-mapping.ts`, spec "Pi policy" paragraph), so the notes tell a Host that wanted the defaults to omit the list rather than send an empty one.
- **Handoff route unchanged from 0.20.0.** Web 2FA (`--execute`, no `--otp`) is primary, `--otp <code>` the alternative, both interactively in Terminal.app; the E404 recovery (readback alone, then the step-8 tag `git tag -a v0.21.0 -m "v0.21.0" -m "sourceGitSha <sha>"`, identical to `publish.mjs`'s `${tag}\n\nsourceGitSha ${headSha}`) is kept because it happened on 0.17.0, 0.19.0 and 0.20.0 (`docs/releases/v0.20.0-publication.md`).
- **CHANGELOG gets a new top section.** PR #226 added no Unreleased section, so the `0.21.0 / @byok-sdk/keys 0.6.2` section summarizes the release notes and is headed "unpublished until the registry confirms".

## Deviations From Plan Or Spec

- None recorded.

## Tradeoffs Considered

| Option | Decision | Reason |
|--------|----------|--------|
| keys `0.7.0` minor | Rejected | no keys API, behaviour, persistence or security authority moves; the policy's MINOR clause does not apply |
| Leave keys at `0.6.1` | Rejected | the spec requires keys' core edge to equal the current dispatch release |
| Regenerate `api-surface/` | Not done | `check:api-surface` passed unchanged (10 goldens match) |
| Materialize the architecture projection | Not done | see Open Questions: the check is not proof-only-materializable in this worktree without a CodeGraph index |

## Open Questions

- **Architecture projection is `human-action-required` in this worktree.** `repo-harness architecture-projection check --json` at `1924942d` reports `unresolved-major-change` / `verified-flow-proof-changed` on `capability.sdk.sdk-root` (modelDigest unchanged; sourceTreeDigest, flowProofDigest and projectionDigest moved) with `codeGraphStatus: unavailable` — this worktree has no `.codegraph/` index, unlike the P0 and 0.20.0-closeout worktrees. `apply --json` wrote nothing and failed with `architecture acceptance candidate identity conflict` (the preceding `check` had recorded a candidate for the same signal under a different request identity), and `reconcile --signal-id …` refused with `architecture reconciliation requires ready CodeGraph proof`. The self-created candidate file was removed so that a later `verify-sprint --prepare-acceptance` does not collide with it (`status` now reports 0 candidates). Building a CodeGraph index was not done without the user's go-ahead. The projection failure gate is `advisory` and no required check reads it; `check-task-workflow --strict` passes.

## Evidence Links

- Checks: `.ai/harness/checks/latest.json`
- Run snapshots: `.ai/harness/runs/`
- Registry at preparation time (2026-09-24): `npm view byok-sdk versions` ends at `0.20.0`; `npm view @byok-sdk/keys versions` ends at `0.6.1` (dist-tags `latest` 0.20.0 / 0.6.1). So `0.21.0` / `0.6.2` are free.
- Verification on `1924942d` (all four preparation commits), exit 0 unless stated: `bun run build` (`BUILD_EXIT=0`); `bun run typecheck` (`TYPECHECK_EXIT=0`); `bun run check:api-surface` ("10 package golden(s) match the built declarations"); `bun run check:version-authority` ("agree with byok-sdk@0.21.0 and @byok-sdk/keys@0.6.2"); `bun run check:release-graph` ("10 aligned manifests at 0.21.0, keys at 0.6.2"); `repo-harness run check-task-workflow --strict` ("[workflow] OK").
- `bun run test` (`TEST_EXIT=1`): the only failure was the known load-only flake `pi-mcp-launch-cwd.test.ts > leaves a bunfig.toml preload planted in the Agent home unexecuted` ("Test timed out in 10000ms"; client 1 failed / 3039 passed / 11 skipped). Host load average was ~6.5. Because `bun run test` is `--sequential` and stops at the first failing package, the remaining packages were run with the same command excluding client, all exit 0: cloud 407, cloud-dataplane 72 passed / 107 skipped, conformance 161, core 304, implementation-identity 110, keys 521 passed / 4 skipped, protocol 402, server 373 passed / 19 skipped, testkit 4, ui-runtime 20, sdk 4, examples 21 + 25. The flake file alone (`vitest run src/__tests__/pi-mcp-launch-cwd.test.ts`): first rerun 1 failed / 2 passed (same 10s timeout, 10.66s), second rerun 3 passed (9.12s). The test was not edited.
- `bun run check:release-pack` runs on the commit that carries these notes (clean worktree); its `sourceGitSha` must equal that commit. The result is in the orchestrator report.
- Known flakes not exercised here: the Windows-only `daemon-auth.test.ts` 750ms guard and `device-credential-store.native.test.ts` 20s timeout do not run on this darwin host.

## Promotion Filter

Promote a candidate to `tasks/lessons.md`, `docs/researches/`, or harness asset files only when all three hold: hard to reverse, surprising without local context, and a real trade-off existed. If any one is missing, keep it in this notes file instead.

## Promotion Candidates

- Promote to `tasks/lessons.md` only after a repeated correction or failure pattern.
- Promote to `docs/researches/` only when it is durable repo knowledge with evidence.
- Promote to harness asset files only after verification across more than one task or fixture.
