# Task Review: team-pi-relay

> **Status**: Accepted
> **Plan**: plans/plan-20260906-0311-team-pi-relay.md
> **Contract**: tasks/contracts/20260906-0311-team-pi-relay.contract.md
> **Notes File**: tasks/notes/20260906-0311-team-pi-relay.notes.md
> **Recommendation**: pass
> **Reviewed Target Revision**: 17f9db31b1b57f2b6ef2f11740d2872d5b54455f

## P1: map

Package-local Pi is exactly 0.85.1. Shared notification attempts and ephemeral
watermarks remain separate from TeamWorkspace's durable grant/read/ack authority.
Pi RPC is owned; Codex's exact existing thread is operator-owned. The new JSONL
GUI interface is separate from MCP and leaves TaskRunner's unattended policy intact.

## P2: actual trace

Codex gpt-6-astra posted seq 1 while Pi zai/glm-5.3 held a synthetic confirmation.
No notification was attempted before the exact GUI cancellation. Pi read seq 1,
posted seq 2, reread and acked its delivered seq 1. Codex read and acked seq 2.
There were exactly two queue attempts/receipts, budget exhaustion and Pi
agent_settled. Codex's two turns were read back as completed, then the disposable
thread was deleted. Owned processes and private scratch were removed.

## P3: decision and limits

Pi public coalesced UI lifecycle is the native input/provider admission authority.
The GUI ID map never infers expiry. The 30-second RPC and 120-second settlement
limits are explicit host lifetime budgets, including GUI waits. A readiness/send
race stops conservatively. Input preflight is fenced by a subsequent native state
receipt, with pendingInputs tracked during settlement. Native model identity is
read back exactly. No global install, GUI app construction, merge or release.

## Evidence

- `docs/researches/evidence/2026-09-06-pi-relay/results.json`: frozen source and
  built artifact hashes, exact native versions, ordered notification/UI metadata,
  actual receipt projection and cleanup evidence.
- `initial-oracle-results.json` preserves the first runner result: its incorrect
  assertion demanded Bob ack his own seq 2 and timed out after successful work.
- `same-run-workspace.json` plus `reevaluate.py` verifies that same native run
  against actual delivered-through authority. No additional provider call.
  The original timeout path did not capture the relay process exit code; native
  settlement, durable receipts, empty relay stderr and process cleanup are recorded.
- `smoke.py` now asserts Bob's delivered/acked seq 1 for future runs.
- Source aggregate: SHA256 of sorted compact JSON `results.sourceSha256` is
  `c1188e87293e61adeab3e14e32e6ad94863d088059ab0baf07103d2a8407aa41`.

## Verification

- Root build and typecheck passed on the frozen source; rebuilt CLI and extension
  hashes remain identical to the actual native run.
- Root test passed: 312 files, 3668 tests. A final-suite cold Wrangler packaging
  run hit its pre-existing 5000ms timeout; one unchanged root rerun passed. No
  cloud-dataplane source or timeout was changed.
- Focused Pi/relay/RPC tests: 52 passed, including real native no-model confirmation
  hold, timeout inventory, wrong/stale IDs, exact responses, and unattended cancel.
- API surface: nine package goldens match; only client adds deliberate hold/response API.
- Version authority and diff whitespace checks passed.
- Strict workflow and diff whitespace checks passed with final artifacts present.

## Independent review

Existing tmux Claude %6, session 77947b83-8e45-4dfd-9cbe-eedecf893af6, returned
functional/security boundary PASS and reproduced the native no-model tests.
Final frozen-source delta/evidence closure: **PASS**. Claude verified all 13 source
hashes, the source aggregate, built CLI and both extension hashes; compared the
initial and corrected ledger/controlEvents/sessions/sourceSha256 field by field;
and confirmed the corrected receipt oracle adds no observations or provider calls.
It accepted the actual confirmation hold, two-attempt round trip, self-trigger
exclusion and cleanup evidence. The missing exit-code observation remains the
explicit non-safety evidence limit above; no new model run is needed for this slice.
Only review/plan completion metadata changes after reviewed source 17f9db3.


## Approved main integration acceptance

User approved integration after the Pi source review. Main d138ce5 was merged
without production source conflicts; both CHANGELOG entries were retained and
the combined API golden matched generated declarations. Tested revision 99976fd:
all required checks passed, 314 test files and 3711 tests.
Only create-daemon.ts differs among the old native-smoke source fingerprints,
from main's independent event-spill configuration. The unchanged Pi admission
review/native evidence is reused within its original boundary; no second model
run or redundant review. integration.json records the new combined check subject.
Main was fast-forwarded in the isolated worktree because another task switched
the root to claude/timeline-spill. That checkout and context WIP were preserved.
No remote push or release occurred. Subsequent changes only record completion.
