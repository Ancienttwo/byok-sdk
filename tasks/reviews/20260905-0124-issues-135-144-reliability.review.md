# Task Review: issues-135-144-reliability

> **Status**: Pending
> **Plan**: plans/plan-20260905-0124-issues-135-144-reliability.md
> **Contract**: tasks/contracts/20260905-0124-issues-135-144-reliability.contract.md
> **Notes File**: tasks/notes/20260905-0124-issues-135-144-reliability.notes.md
> **Checks File**: .ai/harness/checks/latest.json
> **Last Updated**: 2026-09-05 04:37
> **Recommendation**: fail

## Verdict

- Owner-requested acceptance recheck: **FAIL** on `440325d1f3969e3fa04aad82e9708b0aecc3b93a`, baseline `aa44bb94afb4d2720e64b2567cb1ccf4594383de`.
- The previous gate PASS on `29ca654a45373fc63310ca3c4d77f02b7102c2c7` is superseded by two reproduced acceptance gaps below. The intervening commits change only plan/contract/review; source and existing full-test evidence are unchanged.
- No issue closure or downstream implementation follows this failed gate. The user's authorization to close #135-#144 was conditional on acceptance passing.

### P1 — #138 Agent message finalization cannot converge after a pre-write failure

- Location: `packages/cloud/src/inbound.ts:152-184` (`reserveAgentMessage`, pending rejection, external consumer, `finalizeAgentMessage`).
- Schedule: reserve one valid message; allow its external consumer to return success; throw before the finalization store writes its terminal; retry the byte-identical envelope.
- Observed with an in-memory fault probe: `{"firstError":"injected pre-write finalize fault","retry":"rejected","consumed":1,"finalizations":1,"terminalAfterFirst":false,"terminalAfterRetry":false}`.
- The retry exits at the pending reservation and never retries finalization. This is a remaining #138 coverage gap, not a newly introduced duplicate-consumer regression: the original issue explicitly requires `agent.message.publish` and missing-fact retry convergence.
- Required correction: define a durable reconciliation path for the pending external-effect outcome, preserving at-most-once consumer invocation unless an explicit idempotent consumer contract permits retry. Do not turn pending into accepted or re-invoke the external consumer blindly.

### P2 — #143 rejection reason is not frozen with the host decision

- Locations: `packages/cloud/src/cloud.ts:1090-1115`, `1204-1213`, `1290-1298`; `packages/cloud/src/approval-timeline.ts:19-39`.
- Schedule: create an awaiting-approval task; commit `reject(reason="first reason")` to the approval timeline; fail mailbox append; retry with `reason="second reason"`.
- Observed mailbox payload: `{"reason":"second reason","approvalId":"approval-reason"}`. The stable decision identity omits reason, the timeline does not persist it, and recovery takes the new caller option.
- Required correction: freeze the complete executable rejection payload in the first-write authority, recover from that value, and reject conflicting retries. Add the mailbox-failure/changed-reason falsifier.

## Owner-authorized Rework (replacement acceptance pending)

- #138 restores exact pending replay through the already-specified idempotent product consumer. Consumer exceptions preserve transport retry instead of manufacturing `held`; the durable admission remains recoverable after task cancellation or terminalization, with new reservations still lifecycle-gated. Consumer identity, payload, and immutable outcome remain product-owned.
- #143 persists nullable rejection reason in the first host decision, compares retries against that value, and uses it for executable command recovery. Historical host reject rows without reason fail closed because their original payload cannot be inferred.
- The new regressions are permanent, including the pre-write finalization failure and first-reason / changed-reason mailbox failure schedules. The prior rejection remains authoritative until the replacement gate and source-bound verification complete.

## Verification Evidence

- Recheck focused tests: client reliability 11/11, cloud inbound 9/9, server reliability 6/6, cloud approval 16/16, cloud egress/home isolation 16/16. These existing tests pass but do not cover the two new falsifiers.
- The prior AcceptanceReceipt passed canonical script verification before this rejection. The current `repo-harness run` wrapper fails on an unrelated `archctx-contracts` export mismatch; the canonical `scripts/acceptance-receipt.ts` entry point runs directly. No harness repair was attempted.
- Prior full-suite evidence below is reused because source content is unchanged; it is not a substitute for the failed semantic gate.

- Focused: client 11/11, cloud inbound 9/9, server reliability 6/6, approval 16/16, protocol freeze 7/7.
- Root build, typecheck, full test, API-surface, version-authority, deploy-SQL order, strict workflow, and `git diff --check`: passed.
- Full-suite representative totals: client 1614 passed/11 skipped; cloud 333 passed; cloud-dataplane 74 passed/103 skipped; conformance 158 passed; protocol 348 passed; server 345 passed/19 skipped.
- One unrelated `worker-packaging` timeout passed on immediate isolated rerun and the single root rerun passed.

## Cardinality and Authority

- Tenant remains `1:N` devices and `1:N` agents; a device may have `1:N` active AgentPlacements; each agent currently has at most one active placement.
- Agent-bound dedup, egress, content-read, Agent-home request/receipt, and public readback use exact `tenant + device + AgentRef` identity. Physical `conn.hello` has an explicit non-Agent dedup keyspace.
- Task attempt `taskId` remains the first-write reservation authority required by #141; the stored attempt carries exact device and AgentRef, so a conflicting same-task Agent cannot publish a second offer.
- No `UNIQUE(tenant_id, device_id)` or device-to-single-Agent derivation was introduced.

## Residual Evidence Ceiling

- The original full run skipped Postgres suites. This recheck used a disposable local PostgreSQL 18.4 instance: applied migrations 1-18, seeded an existing dedup row, applied 0019/0020, verified ledger no-op replay and preserved old row, then exercised actual Postgres stores for same-device Agent/revision dedup, egress receipts, tenant isolation, and message admission. All passed. This is focused migration/store evidence, not full Postgres/MinIO conformance or a production migration.
- Salesko does not configure BYOK `hostedJournal`, so downstream journal-before-ack restart E2E is not accepted here.

## Rollback

- Revert the integration commits after `aa44bb9`; no remote, registry, deployment, production migration, or tracker mutation occurred.

## Acceptance Receipt Projection

> **Disposition**: reject
> **Reviewer**: Codex
> **Source**: codex-review
> **Actor**: not-applicable
> **Reviewed Subject SHA256**: sha256:60bbf204dcfeb8d7a1fe8065aec8cabbd579908652a5fb3707564109aaab2646
> **Reviewed Subject Scope**: normalized-final-content
> **Reviewed Target Revision**: aa44bb94afb4d2720e64b2567cb1ccf4594383de
> **Verification Evidence SHA256**: sha256:e0abd2e604d1fc64a5c941732254518af29b3994def748308604b1e04906a41f
> **Issued At**: 2026-09-04T20:37:57.281Z

- Summary: Owner-requested recheck of 440325d rejects acceptance: Agent message finalization cannot recover a pending consumed effect (#138), and approval rejection reason is not frozen across mailbox-failure retry (#143).
- Findings: P1: #138: successful consumer plus pre-write finalize failure leaves pending reservation; identical retry is rejected without terminal convergence.; P2: #143: reject reason is absent from first-write decision authority; mailbox-failure retry can publish a changed reason under the same decision identity.
