# Task Review: wp5-s2-ci-flip

> **Status**: Complete
> **Contract**: `tasks/contracts/20260919-0244-wp5-s2-ci-flip.contract.md`
> **Plan**: `plans/plan-20260919-0244-wp5-s2-ci-flip.md`
> **Last Updated**: 2026-09-19 03:20
> **Subject**: 9fd4ad28（实现）@ branch `claude/wp5-s2-ci-flip`，base a6c5a297 = origin/main

## Acceptance Surface

四条款对照（plan WP5 行）：

1. **共享 helper 单一权威** — `packages/client/src/__tests__/support/test-bun-bin.ts`：`bunBinCandidates` 是三文件候选列表的精确并集（a6c5a297 三文件逐一对核：同集同序 `BYOK_TEST_BUN_BIN` → `~/.local/bin/bun` → `/opt/homebrew/bin/bun` → `/usr/local/bin/bun`，仅代码风格差异 `.find` vs for-loop）；`REQUIRE_BUN_ENV='BYOK_REQUIRE_BUN'` 单点定义；无 flag 时 undefined → 既有 `it.skipIf` 行为不变。sibling sweep：候选列表引用现仅存于 helper 与 pin 测试。
2. **Fail-closed pins** — `support/test-bun-bin.pin.test.ts` 5 用例：REQUIRE=1+候选全缺 → module-scope throw；无 flag 同输入 → undefined（skip 保留）；候选并集探针序 pin；`BYOK_TEST_BUN_BIN` 优先级 pin；CI build-test 接线 pin（dataplane constraints idiom）。
3. **监视器负控** — `pi-s2-bundle-resolution.test.ts` 新增 `registry monitor is live`：不依赖 BUN_BIN，同 recording-handler 形状，真实 fetch → `registryAttempts` 捕获 length 1（targeted log 内 2 tests | 1 failed 确认其运行且通过）。既有零尝试断言（:321/:327）原样未动。
4. **CI 正式 gate** — build-test job：`BYOK_REQUIRE_BUN: '1'`（job env + comment）+ Setup Bun 后 resolve step（`command -v bun` 空则 fail；打印 path/`bun --version`/`shasum -a 256`；`BYOK_TEST_BUN_BIN` >> GITHUB_ENV；comment 说明为何不硬编码 `~/.bun/bin/bun` 及 REQUIRE 的正式 gate 语义）。

## Verification Evidence

- build / typecheck / check:api-surface / check:version-authority → 全 exit 0。
- RED（缺工具 fail-closed）：`tasks/runs/20260919-0244-wp5-s2-ci-flip.red-evidence.log` — `BYOK_REQUIRE_BUN=1 BYOK_TEST_BUN_BIN=/nonexistent/bun HOME=<empty>` 下 mcp 套件 import 期 throw（test-bun-bin.ts:59），文件失败 exit 1；pin 文件 5 用例同 run 全过。
- skip 路径：同输入无 flag → `7 passed | 1 skipped`，exit 0（skip-path.log）。
- 定向：4 文件 13 用例 → 12 passed / 0 skipped / 1 failed（targeted.log）。
- 全量：client 包 2878 passed / 1 failed（已知本地项）/ 11 skipped；其余 13 包全绿；树合计 5250 passed / 1 failed / 141 skipped（full-test*.log）。
- strict：`repo-harness run check-task-workflow --strict` → OK exit 0（strict.log）。

## Known local item（不在本刀验收面）

`pi-s2-bundle-resolution.test.ts:327` clipboard tripwire 在 darwin（hasDisplay）本机失败。基线同现证明：换入 a6c5a297 原版文件单独跑同断言同因失败（exit 1）。根因与修复归 `origin/claude/wp5-s2-tripwire-zero`（PR #202，pi fork pin 0.85.1006）；其 probe evidence 证明 attempts 由 `hasDisplay` 门控 → headless ubuntu CI 恒零（CI authoritative）。本刀未触碰该断言与产品依赖。

## Findings

- 全量 `bun run test` 在本机不能 exit 0：唯一红 = 上述既有本地项（--sequential 在 client 包即停，需 `--filter '!@byok-sdk/client'` 补跑其余包，两者均已落 runs 证据）。CI（headless）不受此影响。
- dispatch 基线「4569 passed / 34 skipped」与本机数不可直接对照（本机 dataplane substrate 未起 → 107 skipped 属环境差；基线捕获环境不明）。

## Recommendation

PASS — 四条款闭合，fail-closed 与 skip 双向证据齐备，零尝试断言与产品源码零改动，strict 绿。建议 push 后以分支 CI 全绿作为行为 gate 的最终确认（ubuntu 为 s2 的 authoritative 环境）。
