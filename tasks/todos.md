# Deferred Goal Ledger

> **Status**: Backlog
> **Updated**: (archive-workflow)
> **Scope**: Medium/long-term goals deferred from active plan execution

Current plan tasks live in the active plan's `## Task Breakdown`.
Do not duplicate that execution checklist here. Record only work intentionally deferred beyond this slice, with the tradeoff and revisit trigger.

## Deferred Goals

| Goal | Why Deferred | Tradeoff | Revisit Trigger |
|------|--------------|----------|-----------------|
| **已消费（K4）**：platform-selecting default secret-store factory（原 `createDefaultLocalAgentSecretStore`） | K4 证明 release-channel service prefix 是 host branding policy，不是 generic keys policy。AiphaBee PR #7 保留 host factory，内部组合 package 的 macOS/Windows stores。 | `@byok-sdk/keys` 继续显式注入 `SecretStore`，不猜 platform/channel/vendor prefix；host 获得 drop-in behavior。 | **Closed — AiphaBee PR #7 merge `390307a`。** 只有出现跨 host 的共同 policy 才以新 contract 重开。 |
| **已消费（K4）**：scope data-directory manifest（原 `prepareLocalAccountDataScope`） | K4 证明目录命名、`scope.json` collision check 与 legacy claim 都属于 host account layout。AiphaBee PR #7 保留该 manifest，并把 package `provider_profile` 纳入 claim。 | package store 仍只接受显式 path；host 继续拥有多租户目录 authority，不把 AiphaBee layout 固化进 SDK。 | **Closed — AiphaBee PR #7 merge `390307a`。** 新 host 自行定义并测试 layout。 |
| Wire `@byok-sdk/keys` profile persistence to the `@byok-sdk/core` `TruthStore` contract (P5 in `ARCHITECTURE-PROPOSAL-byok-platform.md:698`) | 原技术前置 K4/K4.1 与 `TruthStore` 都已满足，但 sprint D-11 明确把 P5 留在 S7/RC 外：它会改变 keys dependency/security boundary，必须作为 post-RC 新 plan 独立审查。 | Profile metadata/hash/revision 在 RC 仍保持 local-only；keys dependency graph 不含 `@byok-sdk/core`/`@byok-sdk/protocol`，避免在 release closeout 偷渡新 authority。 | **Triggered, not scheduled.** S7 RC 合入并发布后，以独立 P5 plan 执行。 |
| **已消费（K4.1）**：generic `ProviderRegistry.testConnection()` vs host adapter | K4.1 的真实 caller 需要 narrative-domain validation，generic registry 不拥有该语义。AiphaBee adapter 直接组合 package clients 的 `testConnection()`，没有把单一 host convenience API 推进 package。 | 其他 host 仍可 resolve client 后调用其 `testConnection()`；少一层 convenience，换取 registry 不宣称它不拥有的 domain policy。 | **Closed — 选择 host adapter；AiphaBee PR #7 merge `390307a`。** 只有第二个独立 host 证明同一 registry-level contract 时重开。 |
| Close the long-poll auth/validation asymmetry: WS upgrade validates `protocolVersions`/`productId`/`deviceId` via the `conn.hello` gate (`ws-server.ts`), while the long-poll HTTP endpoints (`http.ts:206/255`) only run `authenticateBearer` | Discovered during S0's D-4 investigation (deep-reasoner track, 2026-08-07). Independent of GAP-002: the steer-capability fix moved to `task.claim.capabilities`, so S0 does not touch transport validation. Folding it into S0 would widen a bounded amendment. | Long-poll-only devices skip protocol-version and product equality checks that WS devices must pass; bearer-token claims still bind device identity, so the gap is version/product skew tolerance, not an auth bypass. | S1's tenant cut (T-005 already adds `conn.hello.productId` equality) — decide there whether long-poll needs an equivalent registration/validation surface, or an explicit documented waiver. |
| ~~Add `packages/core/README.md` and `packages/core/LICENSE` so publish metadata matches shipped files~~ | **Consumed by S7-c**：`packages/core` 与其余 public release packages 均带 package-local README/LICENSE，manifest `files` 明列这些 artifacts，root release graph 强制 LICENSE byte equality。 | `pnpm pack` tarball inventory 与 clean install/import 已纳入 `scripts/release/pack-and-smoke.mjs`，原先只有首次 publish 才暴露的不一致已变成可执行 gate。 | **Closed 2026-08-09 — S7-c npm release candidate.** |
| Tenant-scope the operator surface: `ByokServer.machines.list()` (`index.ts:213-214`, `hub.ts:1768`) enumerates all tenants' devices and `MachineInfo` (`types.ts:159-166`) carries no `tenantId` | **Ruled 2026-08-07 (S2): the embedded operator surface stays host-global by design.** In embedded mode the hosting SaaS *is* the control plane and owns every tenant, so a flat `machines.list()` is the correct authority shape — there is no second principal for it to over-expose to. Tenant-scoped operator surfaces belong to `@byok/cloud` (S3+), built on the tenant-first ports `@byok/core` landed in S2; `@byok/server` is not widened for them. Original deferral: found by the S1 acceptance gate (2026-08-07); not a wire exposure — no HTTP route serves machines. | The embedder cannot build a per-tenant dashboard or filter machines by tenant; a multi-tenant host sees one flat list and filters client-side. Accepted as the embedded-mode cost. | **Closed — no revisit.** Hosted multi-tenant operator views arrive with `@byok/cloud` on the core ports (S3+), not by changing `ByokServer.machines.list()`. Reopen only if embedded mode ever serves a principal that is not the host itself. |
| S3b first-commit fixes from the S3a gate (P2-1/P2-3/P2-4): correct the "verbatim, as the device sent it" comment at the receipt seam (`packages/cloud/src/cloud.ts:105`, `inbound.ts:39-42` — it is a canonical re-encode under the frozen v1 codec, and S3b's journal will share this idempotency seam), add an unwrapped-fetch guard (`expect(cloud.fetch).toBe(registry.fetch)`), and fix the GAP-015 landing label `S3` → `S3b` in `docs/architecture/sdk-architecture.md` | Found by the S3a acceptance gate (2026-08-07); none affect wire semantics, tenant isolation, or the statelessness red line, and S3a was already merge-clean | One-line-level corrections ride along instead of blocking a green slice | S3b's opening commit (the journal slice touches the same files and docs) |
| **已消费（S4A/S4B/S6/S7）**：消化 `tasks/reviews/byok-document-review-20260807.md` §3 的 P1-2～P1-14、P1-17 设计项 | S4A 消费 schema/migration/object lifecycle；S4B-c 消费 reservation/GC/dead-letter；S6-a/b/c 消费 proof/truth 的 symbol/behavior 项；S7 以 Node 20/22 × macOS/Linux/Windows packageability、SQLite runtime/driver 文档、public manifest/export 与 umbrella namespace 可执行检查收口 packaging/symbol release 面。 | 已发布 schema 不返工；release matrix 与 public symbol graph 已成为 CI gate。一般化的「文档引用任意源码符号存在」harness 自动化仍只记在下一行，不在此处冒充完成。 | **Closed 2026-08-09 — S7-c npm release `v0.1.0`.** |
| 落实 review §4 的 P2 文档治理十条，以及 §8 记账的三项 CI 自动化检查：事实快照 SHA 检查（P0-1）、`verify-protocol-fingerprint`（P0-4）、文档引用符号存在性检查（P1-17） | 验收裁定（§8.4）判定 P2 为文档治理方向、不本轮执行；三项 CI 检查都要求扩展 harness checks 才有落点，而本轮修订授权范围是文档内容，不含 harness 能力。 | 文档事实漂移仍靠人工审查发现——这次 review 本身就是证据；P2 的拆分与状态字段缺失让跨章节追踪继续靠通读。接受一轮的代价，换取不在文档修订中夹带 harness 改动。 | harness checks 的下一次扩展（三项检查一并加入 `repo-harness run check-*`），或 canonical 架构文档再次超出可维护长度时启动 P2-2 的拆分。 |
| Journal-cover the `noteSkippedSeq` skip path: `connection-manager.ts:695-702` advances the persisted cursor for unparseable-but-task-classed envelopes without passing through `onEnvelope`, so a hosted daemon acks (and the mailbox retires) unknown `task.*` types from a newer cloud without any durable trace | Found by the S3b acceptance gate (2026-08-08). Pre-existing behavior; `connection-manager.ts` was an explicit contract red line for S3b (zero-diff), and the skip path only fires for envelope types this build cannot execute anyway | The architecture claim "ack hangs behind the journal commit" covers every envelope that reaches `onEnvelope`; the skip path acks without local evidence, which is honest today (nothing executable was lost) but silently discards forward-compat envelopes in hosted mode | A future protocol version bump adding new `task.*` types — that bump should either journal the skip as an explicit `skipped` record or route it through the handler chain via a contract amendment. The S4A branch of this trigger is consumed: S4A-b documents the evidence gap in `deploy/runbooks/mailbox-retention.md` rather than changing `connection-manager.ts`, per `docs/researches/s4a-dataplane-design.md` §10 |
| `MailboxCursorState.updatedAt` reports the read clock when `cloud.sequence` has created a `device_stream` row and the mailbox has never `advanceCursor`d (`packages/cloud-postgres/src/stores/core/mailbox.ts:243`), rather than an instant anything actually wrote | Found by the S4A-b acceptance gate (2026-08-08). `0001` gives `acked_seq` no companion instant, and the `acked_at` `0002` adds is written only on ack — in that state there is no written instant to read. | No contract or suite asserts this field today, and `ackedSeq` being 0 alongside it makes the state self-evident. Reporting the read clock is more honest than fabricating a write moment, but the two compositions can genuinely disagree here. | Any slice that gives `updatedAt` meaning — cursor-age alerting, stale-device detection, cursor auditing. That slice should either make the field nullable or persist a real row-creation instant. |
| **已消费（ADR-024）**：裁定 R2 object 的 hash authority，并据此冻结 `object_manifest` 与 S4B finalize/GC 约束 | 2026-08-08 的 ADR-024 已裁定：通过认证的 daemon 声明是 canonical SHA-256 authority；cloud `HEAD` 只观测存在性、size/content-type，不读回重算，也不声称验证摘要。 | 同 tenant dedupe/accounting 依赖 daemon 声明；错误或恶意的配对 device 可在自己的 tenant 内错标同 size/type bytes，reconciler 无法发现。换得的是无第二次完整 read-back 的带宽/计算成本，且不为不存在的 R2 checksum 能力造兼容分支。 | **Closed — ADR-024 Accepted.** 仅当 R2 支持 PutObject SHA-256 `FULL_OBJECT`，或产品改为不信任 tenant 内配对 device 时，以新 ADR supersede；否则不重开。 |
| 裁定 tenant id 的字符集归属：`tenantId()`（`packages/core/src/tenant.ts:53-79`）不约束字符集，而 core 刻意不做正规化（避免与控制面争一源真相），所以 S4A-c 的防御落在 adapter 的 key 构造点（`packages/cloud-postgres/src/stores/r2-blobs.ts` 的 `assertKeySegmentTenant`） | S4A-c 的授权面是 object plane 与 deploy skeleton；`packages/core/` 是本刀的冻结面（机检），而收紧 core 的 mint 点是 tenant identity 的契约修订，需要控制面一起裁 | 每个把 tenant id 用作路径/key 段的新 composition 都要自己再竖一道守卫，漏一个就是跨租户 alias | 下一个把 tenant id 编进命名空间的 composition（D1 adapter、KV、对象前缀变更），或任何一次 tenant identity 的契约修订 |
| 收敛 composition 混装：`ByokCloudOptions` 对 `cloud` stores 与 `blobContentProxy` 独立接收，呼叫方可把 Postgres/R2 stores 与另一个 proxy 混装并挂上 `/content`（Codex STYLE，S4A-c 双轨验收） | 官方 factory 本身诚实——`createPostgresCloudStores` 不产 proxy，`createInMemoryByokCloud` 两半同源——而把「谁能配谁」提到型别层是 composition API 的形状改动，不是本刀的 P1 修复面 | 「R2 无字节路径」是 factory 层的事实而非型别层的事实：一个手工组装的 host 仍可挂上两条 `/content` route 去代理它其实拿不到的字节 | 出现第二个真实 proxy 实作，或 composition API 下一次修订 |
| salesko dogfood 集成证据落账（byok commit + tarball sha + 下游 commit + 走通面），作为 `presence.hints` 能力对外宣称的前置 | presence producer slice（`plans/plan-20260812-0201-presence-producer-capability-discovery.md`）只交付 daemon 侧实现与验证；下游真实消费证据不在其 allowed paths 内，也不受本仓 commit 控制。 | 上游能力已可用且有第一方测试背书，但 release notes 在拿到下游证据前不宣称该能力——换取「宣称必有消费方背书」这条纪律，代价是能力落地与对外宣称之间存在时间差。 | 1a tarball 产出后，salesko 承诺当天回证（见 `docs/researches/2026-08-12-salesko-consumption-evidence.md` §4） |
| Git 长任务账本，两级落点：(a) 「长任务 Git 工作流」技能包（声明式内容，指导 agent 用 Git CLI 管理多日任务状态，R1 通道的首个示范载荷，SDK 零代码）；(b) Git-backed `LocalTaskJournal` 后端（走现有 port 注入，不改 core） | 源头是 owner 在 RAFT 实际投研任务中观察到 agent **自发**用 Git CLI 当工作流账本（2026-08-12 澄清：agent/skill 行为层，非 RAFT 平台设施——与静态拆解未见 git 任务跟踪一致）。现有 `SqliteLocalTaskJournal`（WAL + fsync）已满足单机 crash-safety；git 的增量价值在跨会话长任务的历史可回溯/可搬运。 | (a) 依赖 R1 先落地；agent 自管的 git 账本宿主无结构化读取能力。(b) 高频 append 在 git 上有性能与单写者锁成本，定位为长任务 checkpoint 层或备选后端，非替换默认 journal。 | (a) R1 技能包通道落地时作为示范载荷。(b) 宿主需要**结构化读取**任务历史（resume-after-crash、换机续跑、审计导出）时升级；评估时对照 `gitWorkspace`（spec:56-86）避免两套 git 状态语义。 |
| 统一 append-only 审计账本 port（device + cloud 两侧），敏感操作（pairing/assertion 签发/revoke/skill-pack 安装）写入 hash-chain 账本 | 三个被研产品同向证据：RAFT `upgrade.log` JSONL closed-set 写入合约、hermes hub `append_audit_log` + cron executions ledger、buzz hash-chain moderation audit。我们的安全敏感面目前各自 ad hoc 记录，没有统一账本；但没有 compliance 拉力时先不铺 port。 | 缺统一账本期间，安全事件取证靠分散日志拼接；先记模式（closed-set errorCode 写入合约 + hash 链防篡改）不写代码，避免无消费方的抽象。 | 第一个企业级下游提出审计/合规要求，或 skill-pack 通道（R1）落地时顺带裁定其审计面归属。 |
| Scheduled dispatch：SaaS 侧定时向 daemon 派发任务（hermes cron 模式：确定性 schedule DSL + fresh session 无会话上下文 + deliver-to-origin + respawn 防护 + execution ledger） | 属新产品能力，无当前下游诉求；hermes 的职责切分（NL 由 LLM 上游解析、工具层只收确定性 DSL）符合我们 no-shadow-parser 原则，可整体沿用为设计基线（`~/Projects/hermes-agent/cron/`）。 | 不做期间下游只能在自己侧起 scheduler 再走普通 task dispatch——这本身就是合理的过渡形态，也是验证需求真实性的探针。 | 下游在自己侧实现 scheduler 后提出上移诉求，或第二个下游重复同一模式。 |
| Assertion 条件文法（R2）：`DeviceAssertionEnvelopeV1` 增加 capability conditions（操作类型 × 时间窗多维限权），参照 buzz NIP-OA conditions grammar（`docs/nips/NIP-OA.md`） | device assertion broker 尚在收尾，当前 audience scoping 单维已满足 salesko 场景；扩展是 additive-minor，过早引入文法解析面会扩大安全审查范围。 | 单 assertion 不能区分操作权限，需要区分时下游只能申请多个 assertion——可接受的过渡成本。 | 下游需要单 assertion 区分操作权限的第一个真实用例。**执行约束（buzz 教训）**：buzz 定义了该文法但从未对 event 求值（`nip_oa.rs` 仅语法+签名校验，全仓无 evaluation 调用点），安全声明与实现脱节——我们若做，文法与 `verifyDeviceAssertion` 求值点必须同一 slice 交付并有拒绝路径测试（见评估文档 §5.1）。 |
| 包管理迁移 pnpm → Bun（owner 提案 2026-08-13）：bun 作为 package manager + script/test runner；Node 保持运行时目标与 engines floor，npm 发布产物不变 | R1 contract 已冻结且 exit criteria/CI/release 脚本均绑定 pnpm 语义；混入 feature 刀违反合并单元纪律与追新偏好自带的「迁移必须独立排任务」规则。 | 迁移前继续付 pnpm/bun 双工具认知成本（repo-harness 本身已跑在 bun 上）。迁移刀需单独验证：better-sqlite3 原生模块在 bun install/test 下行为；`scripts/release/pack-and-smoke.mjs` 的 pnpm pack 语义；harness 模板与 CLAUDE.md Required Checks 全量改写。 | R1 Phase 1 合入后立即启动，作为下一个独立 plan；R1 的新测试面顺带成为迁移验证面。 |
| 会话级单飞行调度纪律（R3）：同一会话 scope 至多一个 prompt 在飞 + in-flight deadline 兜底回收 + 在飞期间去重，参照 buzz `EventQueue`（`crates/buzz-acp/src/queue.rs:230-410`） | 当前任务派发是单触发源，`TASK_TRANSITIONS` 管单任务生命周期已够；这层管的是跨任务调度纪律，无并发触发源时是死代码。 | 多触发源（webhook/A2A mention）并发打向同一 daemon 会话时存在放大循环风险——在那之前风险不存在。 | 第二个并发触发源接入同一会话 scope（同上评估文档 §6）。 |
| CI 加固：`packages/client` 的 `daemon-owner.ts:324` `acquireStoreMutex` 用确定性 mutex 端口，满仓并发（`pnpm -r run test`）下与真实进程或并行 worker 碰撞抛 `DaemonOwnerActiveError`（overnight 三刀期间反复偶发绊过完成门，隔离/重跑即过） | 是既有 load-sensitive flake，与本轮三刀（B-1/A-1/B-2）均无关，report-only 未在任一刀内修（跨刀 scope）。 | 不修期间完成门/CI 偶发假红，靠重跑绕过——每次重跑是成本且掩盖真实信号的风险。 | 作为独立 CI 加固刀：mutex 端口改为随机化+重试或碰撞可恢复，或测试在竞争下 skipIf。overnight 运行已确认它稳定复现于全仓并发。 |
| CI 加固：cloud-postgres dataplane 的 MinIO teardown 偶发 `Unexpected HTTP response: 503` + `socket hang up`（测试全过后 teardown 阶段 MinIO 返 503） | B-1 的 undici keep-alive 修复消除了纯 keep-alive socket reset，但 server 端主动 503 是另一失败模式，keep-alive 修复管不了；A-1 ship 时 4 次 dataplane 有 1 次撞上，重跑即过。 | 不修期间 dataplane 偶发假红，靠重跑绕过。 | 作为独立 CI 加固刀：MinIO 容器 teardown 前先 drain/close object 客户端，或对 teardown 阶段的 503 做有界重试；与 daemon-owner flake 可合并为一个「CI 稳定性」刀。 |
