# Task Contract: c07-runner-i18n

> **Status**: M0 accepted0df88a7f; active M1a runtime declaration cutover
> **Plan**: plans/plan-20260916-c07-runner-i18n.md
> **Task Profile**: implementation
> **Owner**: kito
> **Capability ID**: sdk-sdk-root
> **Last Updated**: 2026-09-16
> **Review File**: `tasks/reviews/20260916-c07-runner-i18n.review.md`
> **Notes File**: `tasks/notes/20260916-c07-runner-i18n.notes.md`

## Why

Current S2 tripwire remains36. Async runner is a code-loading subprocess, not data; caller config cannot create executable authority. The selected design needs a precise cross-repo implementation gate before new helper kind or dependencies.

## Goal

Prepare the implementation packet in `docs/researches/20260916-c07-runner-i18n-implementation-entry.md`. Owner replied「批准」to the three separately listed M0 / SDK M1–M3 / Salesko P4 questions. M0 design plus two inert vector files is active now. SDK/P4 product implementation is authorized conditionally, but M0 acceptance and exact path registration are mandatory before writes. Earlier Q1–Q3 approval covered design only; this reply expands scope.

## Scope

- Active: five docs and two inert test-only JSON vector files below; M0 schema/encoding/version checklist, Host projection interface, custody/env proof matrix, locale layout, vendor provenance and future tests.
- Inactive future implementation: M1 declarations/config; M2 runner/i18n; M3 functional/packed acceptance. Product path inventory is in design, not allowed_paths. Native1006 and Salesko P4 have separate contracts.
- Excluded in M0: production source/executable tests/manifests/lock/goldens, install/dependency resolution, scanner/feature changes, full/pack, push/merge/publish, real deployment.

## Stop Conditions

- Stop before any inactive path or product stage; no inferred permission from the broader parent contract.
- Do not mint descendant binding by rewriting parent launchArgv or derive env expectation from final env.
- Unresolved runner→Pi custody/field/schema encoding stops M1; no temporary compatibility shape.

## Falsifier

A config/binding whose child kind was not declared by Host and resolved by daemon must not become a runnable descendant. A trace proving an undeclared executable edge or dynamic interpreter selection invalidates implementation readiness.

## Workflow Inventory

- Plan: `plans/plan-20260916-c07-runner-i18n.md`
- Design: `docs/researches/20260916-c07-runner-i18n-implementation-entry.md`
- Notes/review: as above.
- Do not change active-plan/worktree pointers or derived tasks/current.md.
- Existing product authority: parent20260916-1003; accepted subject6696c211, not modified here.

## Change Assessment

```json
{"protocol":1,"oracles":[]}
```

## Acceptance Policy

```json
{"protocol":1,"reviewer":"Claude","user_waiver":"allowed"}
```

## Allowed Paths

```yaml
allowed_paths:
  - plans/plan-20260916-c07-runner-i18n.md
  - tasks/contracts/20260916-c07-runner-i18n.contract.md
  - tasks/notes/20260916-c07-runner-i18n.notes.md
  - tasks/reviews/20260916-c07-runner-i18n.review.md
  - docs/researches/20260916-c07-runner-i18n-implementation-entry.md
  - tests/fixtures/c07-runtime-record/canonical-revision.v1.json
  - tests/fixtures/c07-runtime-record/rejections.v1.json
  - packages/implementation-identity/src/identity.ts
  - packages/implementation-identity/src/environment.ts
  - packages/implementation-identity/src/spawn-binding.ts
  - packages/client/src/daemon/tool-implementation-identity.ts
  - packages/client/src/sdk-reserved-helper-host.ts
  - packages/client/src/__tests__/sdk-reserved-helper-host.test.ts
  - packages/implementation-identity/src/__tests__/runtime-resolution.test.ts
  - packages/client/src/index.ts
  - packages/client/src/adapters/pi/runtime-launch.ts
  - packages/client/src/adapters/pi/input-preparation-runtime.ts
  - packages/client/src/adapters/pi/input-preparation.ts
  - packages/client/src/__tests__/fixtures/runtime-resolution.ts
  - packages/client/src/__tests__/tool-implementation-identity.test.ts
  - packages/client/src/__tests__/runtime-launch-description.test.ts
  - packages/client/src/__tests__/pi-runtime-launch-binding.test.ts
  - packages/client/src/__tests__/pi-runtime-host-binding.test.ts
  - packages/client/src/__tests__/input-preparation-runtime.test.ts
  - packages/client/src/__tests__/pi-input-preparation.test.ts
  - packages/client/src/__tests__/pi-prepared-launcher.test.ts
  - packages/client/src/__tests__/pi-runtime-launch-cwd.test.ts
  - packages/client/src/__tests__/pi-s2-bundle-resolution.test.ts
  - packages/client/package.json
  - bun.lock
  - api-surface/client.d.ts
  - api-surface/implementation-identity.d.ts
  - CHANGELOG.md
```

## Evidence Requirements

```yaml
evidence_requirements:
  benchmark: not_applicable
```

## Exit Criteria (Machine Verifiable)

```yaml
exit_criteria:
  files_exist:
    - plans/plan-20260916-c07-runner-i18n.md
    - tasks/contracts/20260916-c07-runner-i18n.contract.md
    - tasks/notes/20260916-c07-runner-i18n.notes.md
    - tasks/reviews/20260916-c07-runner-i18n.review.md
    - docs/researches/20260916-c07-runner-i18n-implementation-entry.md
  tests_pass: []
  commands_succeed:
    - git diff --check
    - repo-harness run check-task-workflow --strict
```

## Future implementation acceptance (not evidence yet)

M0: joint SDK/P4 literal schemas, byte vectors, resource bounds, exact vendor inventory and recursive custody trace accepted. M1: strict declaration/config/unknown-version rejection; one prefix per binding; compiler identity explicitly required. M2: real async/cancel/ceiling and non-English locale behavior, nine assets checked before load, no source duplication or runtime TS loader. M3: actual packed Node/S2 entry/recursive paths, unchanged zero-attempt guard, scanner actual bundle, API/graph and single frozen full/pack. Clipboard/native blocker is retained as RED until1006, never converted to skip.

## Rollback Point

Docs-only delta from6696c211; no runtime/registry changes to roll back. Product publication needs its own exact frozen-candidate approval.

## Three independent activation decisions

M0 test-only expansion (now activated by the direct Owner reply): exactly `tests/fixtures/c07-runtime-record/canonical-revision.v1.json` and `tests/fixtures/c07-runtime-record/rejections.v1.json`, inert vectors plus existing five docs, no product/parser changes. The same reply approved conditional M1–M3 SDK and separate Salesko P4 products. Both remain gated on reviewed M0 and exact vendor/path registration; no new approval request for already approved scope. Even M1–M3 completion leaves clipboard12 and S2 RED until1006; no containment approval inferred. Compiler default argument removal occurs in M1, M0 only freezes that obligation.

## Owner activation — 2026-09-16

Direct reply「批准」to all three numbered items recorded in handoff§102. M0 only is executing now; M1–M3/P4 wait for the reviewed M0 schema/recursive trace and exact product paths. Push/merge/publish/fork1006/real installation remain excluded. No product writes under this activation commit.


## M0 C composition acceptance scope

Unchanged ImplementationSpawnBindingV1 template + templateDigest + exact Host descendantPolicy projection + typed perLaunch. Existing V1 parser/assert behavior retained; new descendant assertion explicitly uses exact-name subset/equality and unchanged loader-values, sharing one internal physical reverify in future M1. Host sorted integer codec and SDK template original-order JSON byte codec are distinct; fixtures include fractional V1 stat values. Parent immutable declaration, not candidate config policy, is the comparison authority. Four mandatory negatives plus policy escalation/root drift/depth reset remain test-design vectors, not executed product tests. This section activates no product path.

## M1a activation after M0 PASS (supersedes earlier M0-only restrictions)

Supervisor accepted0df88a7f and released M1/P4 contracts. Active SDK M1a is the exact product/test/manifest/lock/golden paths above; earlier inactive statements describe historical M0. Root is the sole writer. This first slice introduces strict runtime-only Host wrappers and the four-kind locator union, keeps policy/edges in the measured daemon result, and removes compiler default discovery. MCP retains its bare V1 response and existing validator/measurement bytes. One private physical measurement implementation serves both explicit resolve APIs; the original generic identity resolver becomes MCP-only so no runtime caller can silently discard policy. Runtime callers use a new measured declaration result, not a second Host resolve method. No old runtime bare-record fallback.

New exact @juicesharp/rpiv-i18n2.8.0 dependency and its rpiv-config edge enter lock here; preserve existing resolved consumer versions/integrities. This slice does not import the optional peer or change extension behavior. Runtime self launch retains the frozen declaration for later plan assembly, but runner/print dispatch and descendant execution are not enabled. No stub custody or guessed production limits. The four-kind locator is a declaration vocabulary, not proof every kind is executable today.

P1: Host authors wrapper/policy/edges, shared measures original V1 bytes, client owns launch selection/config, keys custody unchanged. P2: authority.resolve(request.subject) -> strict subject-specific response -> unchanged physical measurement -> immutable runtime identity+policy+edges -> client self-launch decision. P3: split explicit runtime versus MCP consumption to prevent discarded permission data, share physical measurement rather than duplicate it; at10x, per-file asset hashing remains first cost, with no skipped reverify.

Future M1b/M2 paths remain inactive until registered. Atomic fanout, session/parallel lifetime, zero-cost charge-once handoff, concurrent cancellation/retry permit release, parent-instance/root-budget custody need proven enforcing calls before enabling any new recursive path. P4 separately activates its own contract on pushed32cfcd49 or Owner-decided followups; no Salesko writes here. Native1006/push/merge/publish/install/deploy excluded.

Acceptance after freeze: build/typecheck/client full plus keys/identity, API/version/graph/strict/diff, real release-pack, attribution0; supervisor owns the sole full/pack run to avoid contention. Targeted local tests precede freeze. Keep S2 tripwire RED36, not skip/relax. Parser/refusal tests use accepted M0 vectors and zero-measurement negatives. Record dependency semantic-edge proof and existing MCP byte parity. Product commit separate from registration/CHANGELOG.

M1a exact follow-up paths: shared environment/spawn-binding move existing fixed keys inherited-name constants (definitions only, same re-exports/projection bytes) so runtime policy can reuse them without a circular import or second list. Runtime fixed-prefix table moves from client launch policy into shared declarative runtime vocabulary, client consumes same function; no prefix semantic change. Reserved helper host explicitly rejects runner/print as not enabled; test pins rejection and no dispatch. bun.lock was already registered in ca5ba62f allowed_paths.

## Supervisor atomic-slice correction
The attempted exact i18n dependency activates upstream todo's top-level optional import immediately, producing nine missing-locale fallback warnings before helper usage validation (10 failures, other207 targeted assertions pass). Supervisor directed M1a to restore package.json/bun.lock byte-identically; M1b owns dependency + private todo integration + frozen locale layout + child preverify as one atomic slice. No stderr suppression, extra dist/bin/locales layout or relaxed assertions. The manifest/lock paths remain registered but have no frozen M1a delta. The three links left by this attempted Bun install were removed by exact recorded paths; package cache bytes were retained.
