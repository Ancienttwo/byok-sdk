# Task Contract: Issue178 existing-coverage disposition

> **Status**: Active
> **Plan**: plans/plan-20260910-2154-brc178-completion.md
> **Task Profile**: ledger-closeout
> **Owner**: ancienttwo
> **Capability ID**: capability.sdk.sdk-root
> **Review File**: tasks/reviews/20260910-2154-brc178-completion.review.md
> **Notes File**: tasks/notes/20260910-2154-brc178-completion.notes.md

## Why

Issue178 reports a test gap already covered by the exact baseline source and independently passing CI smoke jobs. Formal closure must preserve that evidence and the original failed aggregate accurately.

## Goal

Commit the exact typed not_planned decision and its falsifier, obtain canonical-root semantic acceptance over both artifacts, then close Issue178 through the authorized campaign API.

## Scope

- In scope: two evidence artifacts with the current campaign, Issue, source observation and non-executed task revision.
- Out of scope: source changes, duplicate tests, package publication, new defects, and historical receipt rewriting.

## Allowed Paths

```yaml
allowed_paths:
  - tasks/evidence/brc178-completion.decision.json
  - tasks/evidence/brc178-completion.falsifier.md
```

## Exit Criteria (Machine Verifiable)

```yaml
exit_criteria:
  files_exist:
    - tasks/evidence/brc178-completion.decision.json
    - tasks/evidence/brc178-completion.falsifier.md
```

## Verification Plan

```json
{
  "protocol": 1,
  "checks": [
    {
      "id": "evidence-digests",
      "kind": "command",
      "command": "python3 -c 'import json,hashlib,pathlib; p=pathlib.Path('\"'\"'.'\"'\"'); d=json.loads((p/'\"'\"'tasks/evidence/brc178-completion.decision.json'\"'\"').read_text()); assert d['\"'\"'falsifier'\"'\"']['\"'\"'artifact_sha256'\"'\"']=='\"'\"'sha256:'\"'\"'+hashlib.sha256((p/d['\"'\"'falsifier'\"'\"']['\"'\"'artifact_path'\"'\"']).read_bytes()).hexdigest(); expected={'\"'\"'scripts/release/pack-and-smoke.mjs'\"'\"':'\"'\"'76b7c99b85c8b92515001623ceb7846e9302355f464caec9d704c15086a9d311'\"'\"','\"'\"'packages/sdk/src/index.ts'\"'\"':'\"'\"'9330d09d7135b6da47a6b95142b2b37e188527351cfe225ab89c0fa024516915'\"'\"','\"'\"'.github/workflows/ci.yml'\"'\"':'\"'\"'300505464484cdc69ac0bae8820f54bb6d8ad9de556773bad64a1d1e1efbf196'\"'\"'}; assert all(hashlib.sha256((p/k).read_bytes()).hexdigest()==v for k,v in expected.items()); print('\"'\"'falsifier and baseline source digests match'\"'\"')'",
      "cwd": ".",
      "phase": "verification",
      "cost": "normal",
      "evidence_policy": "current_exact",
      "necessity": "Binds the falsifier bytes and unchanged source inputs to the cited historical CI evidence.",
      "inputs": {
        "env": [
          "PATH"
        ]
      }
    },
    {
      "id": "workflow",
      "kind": "command",
      "command": "repo-harness run check-task-workflow --strict",
      "cwd": ".",
      "phase": "verification",
      "cost": "normal",
      "evidence_policy": "current_exact",
      "necessity": "Checks the independent evidence-disposition workflow metadata.",
      "inputs": {
        "env": [
          "PATH"
        ]
      }
    }
  ]
}
```

## Evidence Requirements

```yaml
evidence_requirements:
  benchmark: not_applicable
```

## Acceptance Policy

```json
{"protocol":2,"reviewer":"Codex","source":"codex-plugin","user_waiver":"allowed"}
```

## Stop Conditions

Stop on a mismatched source digest, changed Issue observation, executed or bound task, or missing canonical-root acceptance. Preserve prior failures. Do not use a source-only pass as the campaign receipt.

## Acceptance Notes (Human Review)

This artifact-only disposition adds no executable behavior. Cited source/CI observations retain their original subject identities. Check the decision with validateCampaignNotPlannedDecision from the approved harness before committing; the actual closeout repeats that validation. Prepare and review at the canonical canary root after integrating the evidence commit, with REPO_HARNESS_DIFF_BASE set to the exact parent commit before these artifacts. Both files must be in the receipt reviewed_paths. Complete the actual codex-plugin review, record its returned recommendation/findings, verify the receipt, and only then invoke close-not-planned. No waiver is asserted.

## Rollback Point

Revert the two evidence files before public closeout if invalid. Preserve immutable records after closeout and route any later correction through an explicit new disposition.
