# Task Contract: r2-key-prefix

> **Status**: Active
> **Plan**: plans/plan-20260812-0400-r2-key-prefix.md
> **Task Profile**: code-change
> **Workflow Profile**: strict
> **Owner**: kito
> **Capability ID**: root
> **Last Updated**: 2026-08-12 04:02
> **Review File**: `tasks/reviews/20260812-0400-r2-key-prefix.review.md`
> **Notes File**: `tasks/notes/20260812-0400-r2-key-prefix.notes.md`
> **Exemplar**: `docs/reference-configs/contract-brief-example.md`

## Why

host 被迫为每个产品开专用 bucket(handoff 条目 6):`tenantObjectKey` 硬编码布局、`R2BlobStoreOptions` 无 prefix。additive option 消除此摩擦;做错的风险是默认布局被意外改变(既有对象失联)或引入 dual-read 兼容路径(纪律禁止)。

## Goal

`R2BlobStoreOptions.keyPrefix?`(默认缺省 = 现布局逐字节不变)接入 core 的 key 构造;前缀构造期 fail-closed 校验(白名单字符集、无首尾斜杠/空段);文档写明 immutable 语义(改前缀 = 既有对象失联,切换须独立一次性 migration,禁 dual-read)。除 core/cloud-postgres 外零 diff。

## Scope

- In scope: packages/core/src/(key 构造 + 校验 + 测试)、packages/cloud-postgres/src/(options + 接线 + 测试)、packages/cloud-postgres/README.md 或等效文档。
- Out of scope: 任何迁移工具、dual-read、bucket 管理、其他包。
- Taste constraints: 校验只在构造期,无运行时 setter。

## Stop Conditions

- Stop and hand back to the parent if the change would require editing a path outside Allowed Paths.
- Stop if an Exit Criteria command cannot be run in this environment.
- Stop if Goal, Scope, or Exit Criteria are internally contradictory.

## Falsifier

先写默认布局快照断言(现 key 逐字节)跑绿,再动实现——实现后该断言仍须绿(默认零变化前提);带前缀断言先红后绿。

## Root Cause Evidence

n/a (not bugfix)

## Workflow Inventory

- Source plan: `plans/plan-20260812-0400-r2-key-prefix.md`
- Deferred-goal ledger: `tasks/todos.md`
- Review file: `tasks/reviews/20260812-0400-r2-key-prefix.review.md`
- Notes file: `tasks/notes/20260812-0400-r2-key-prefix.notes.md`
- Checks file: `.ai/harness/checks/latest.json`
- Run snapshots: `.ai/harness/runs/`
- Scope gate: edit only paths listed under `allowed_paths`; update this contract before widening scope.
- Completion gate: run `verify-sprint --prepare-acceptance`, record one typed AcceptanceReceipt under the frozen policy below, then run `verify-sprint`; review Markdown is projection only.

## Acceptance Policy

```json
{"protocol":1,"reviewer":"Claude","user_waiver":"allowed"}
```

## Allowed Paths

```yaml
allowed_paths:
  - packages/core/src/
  - packages/cloud-postgres/
  - plans/plan-20260812-0400-r2-key-prefix.md
  - tasks/todos.md
  - tasks/contracts/20260812-0400-r2-key-prefix.contract.md
  - tasks/reviews/20260812-0400-r2-key-prefix.review.md
  - tasks/notes/20260812-0400-r2-key-prefix.notes.md
```

## Evidence Requirements

```yaml
evidence_requirements:
  benchmark: not_applicable
```

## Delegation Contract

```yaml
delegation:
  budget:
    tokens: null
    runner_invocations: null
    wall_time_minutes: null
  permission_scope:
    mode: inherit_allowed_paths
    writable_paths: []
    network: inherited
  roles:
    parent:
      mode: narrate_and_gatekeep
      purpose: approval_checkpoint_owner
    explorer:
      mode: read_only
      purpose: codebase_research
    worker:
      mode: edit_within_allowed_paths
      purpose: implementation
    verifier:
      mode: read_only
      purpose: exit_criteria_review
  runner:
    preferred:
      - subagent
      - codex-exec
      - main-thread
    fallback: main-thread
    brief_is_authoritative: true
```

## Exit Criteria (Machine Verifiable)

```yaml
exit_criteria:
  files_exist:
    - packages/cloud-postgres/src/__tests__/r2-key-prefix.test.ts
  artifacts_exist:
    - .ai/harness/checks/latest.json
    - tasks/notes/20260812-0400-r2-key-prefix.notes.md
  commands_succeed:
    - pnpm --filter @byok-sdk/core run test
    - pnpm --filter @byok-sdk/core run typecheck
    - pnpm --filter @byok-sdk/cloud-postgres run build
    - pnpm --filter @byok-sdk/cloud-postgres run typecheck
    - pnpm --filter @byok-sdk/cloud-postgres run test
    - git diff --quiet main -- packages/protocol packages/client packages/server packages/cloud deploy scripts
    - repo-harness run check-task-workflow --strict
```

## Acceptance Notes (Human Review)

- Functional behavior: 默认配置全部既有 key 逐字节不变;`keyPrefix:'acme/prod'` → `acme/prod/tenants/...`。
- Edge cases: 首/尾斜杠、空段、大写、空格、空串前缀 → 构造期抛错。
- Regression risks: 现有 R2/blob 测试全绿。

## Rollback Point

- Commit / checkpoint: `3d66543c`
- Revert strategy: revert;零迁移。
