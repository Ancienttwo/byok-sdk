# Task Contract: official-pi-087

> **Status**: Active
> **Plan**: plans/plan-20260925-0300-official-pi-087.md
> **Task Profile**: code-change
> <!-- legal values: code-change | docs-only | ledger-closeout | migration | eval-only | delegated-run | bugfix (omit for legacy passthrough); see docs/reference-configs/sprint-contracts.md -->
> **Owner**: kito
> **Capability ID**: root
> **Last Updated**: 2026-09-25 03:42
> **Review File**: `tasks/reviews/20260925-0300-official-pi-087.review.md`
> **Notes File**: `tasks/notes/20260925-0300-official-pi-087.notes.md`
> **Exemplar**: `docs/reference-configs/contract-brief-example.md`

## Why

退役自维护 Pi fork，同时保留 prepared 请求、预算、工具、凭证与安装身份的边界。任何漏掉的 framing 或发送绕行都会使 D 失去意义。

## Goal

已完成 M1/M2/M3 并通过独立 gate；M4 也已通过独立 gate。本轮按 w2:pB 派工收尾：两个 MEDIUM、npm 双实例边界与 todo、M5 脱敏证据、rebase、全链验证及最终 projection/release-pack。此前范围：官方 0.87.1 依赖与 breaking changes 适配，公开 API prepared lane、A1″/A2′、最终字节门、两层 retry 关闭、SDK RPC frame bound 与空工具集，以及官方 closure 身份与打包。只本地 commit，不 push。M5 已按单独授权完成三次普通推理，不再调用 provider。

## Scope

- In scope: M4 gate MEDIUM 测试证据修正、npm 双实例 spec/notes/todo、M5 脱敏结果及 SHA256 入仓；先 rebase 最新 origin/main，冲突即停；再全链与最终 projection/release-pack。M1–M4 已通过独立 gate。
- Out of scope: 追加 provider 调用、push/发布、Salesko 实施、fork 修改。
- Taste constraints: <!-- advisory only, no run gate; default style/taste lives in AGENTS.md and the minimal-change policy, use this to record a per-task override -->

## Stop Conditions

- Stop and hand back to the parent if the change would require editing a path outside Allowed Paths.
- Stop if an Exit Criteria command cannot be run in this environment.
- Stop if Goal, Scope, or Exit Criteria are internally contradictory.

## Falsifier

任何公开 API 无法保留的准备/消费保证、需要 private import/patch/复制 serializer 或 Session core 的证据，均立即停下报告。先以已安装官方精确版本做离线最小探针。

## Root Cause Evidence

Required when Task Profile is `bugfix`; leave as-is otherwise.

- root_cause: one sentence naming file:line/condition (testable, not "a state issue").
- repro: the command or UI path that reproduces the symptom.
- regression_guard: path to a test that fails on the unfixed code and passes after the fix (must also appear as a `package_test` check in Verification Plan).
- pre_fix_failure_artifact: path to a captured run of regression_guard on the UNFIXED code. Capture with `bun test <regression_guard> > <artifact> 2>&1; echo "PRE_FIX_EXIT=$?" >> <artifact>` (no pipes — pipes swallow the exit status). The gate requires a non-zero `PRE_FIX_EXIT=` line plus the regression_guard path string in the artifact (see the Root Cause Evidence Gate section in docs/reference-configs/sprint-contracts.md).

## Workflow Inventory

- Source plan: `plans/plan-20260925-0300-official-pi-087.md`
- Deferred-goal ledger: `tasks/todos.md`
- Review file: `tasks/reviews/20260925-0300-official-pi-087.review.md`
- Notes file: `tasks/notes/20260925-0300-official-pi-087.notes.md`
- Checks file: `.ai/harness/checks/latest.json`
- Run snapshots: `.ai/harness/runs/`
- Scope gate: edit only paths listed under `allowed_paths`; update this contract before widening scope.
- Completion gate: run `verify-sprint --prepare-acceptance`, record one typed AcceptanceReceipt under the frozen policy below, then run `verify-sprint`; review Markdown is projection only.

## Change Assessment

```json
{"protocol":1,"oracles":[{"id":"root-required-checks","kind":"deterministic_test","paths":["*"]},{"id":"check-release-pack","kind":"runtime_readback","paths":["*"]}]}
```

## Acceptance Policy

```json
{"protocol":2,"reviewer":"Codex","source":"codex-plugin","user_waiver":"allowed"}
```

## Allowed Paths

```yaml
allowed_paths:
  - tests/fixtures/c07-runtime-record/official-pi-087
  - packages/
  - scripts/
  - package.json
  - bun.lock
  - api-surface/
  - docs/spec.md
  - docs/protocol.md
  - docs/architecture/
  - docs/architecture/.projection-manifest.json
  - docs/architecture/modules/sdk/sdk-root.md
  - docs/researches/official-pi-migration/
  - docs/releases/
  - README.md
  - CHANGELOG.md
  - plans/plan-20260925-0300-official-pi-087.md
  - tasks/contracts/20260925-0300-official-pi-087.contract.md
  - tasks/notes/20260925-0300-official-pi-087.notes.md
  - tasks/reviews/20260925-0300-official-pi-087.review.md
  - tasks/todos.md
  - tasks/runs/20260925-official-pi-c-reprobe.json
  - tasks/runs/20260925-official-pi-c-reprobe.json.sha256
  - plans/plan-20260925-0331-pi-087-official-migration.md
  - tasks/notes/20260925-0331-pi-087-official-migration.notes.md
  - tasks/contracts/20260925-0331-pi-087-official-migration.contract.md
  - docs/researches/official-pi-migration/consolidation-ruling.md
  # post-merge closeout on the shared 0.22.0 closeout branch (2026-09-25)
  - deploy/runbooks/release-responsibility.md
  - tasks/current.md
  - tasks/reviews/20260925-1007-release-0-22-0.review.md
  - plans/archive/
  - tasks/archive/
```

## Evidence Requirements

```yaml
evidence_requirements:
  # Set benchmark to required when this contract consumes the harness profile benchmark matrix.
  benchmark: not_applicable
```

## Delegation Contract

```yaml
delegation:
  budget:
    tokens: null
    runner_invocations: null
    wall_time_minutes: null
  permission_scope:
    mode: inherit_allowed_paths
    writable_paths: []
    network: inherited
  roles:
    parent:
      mode: narrate_and_gatekeep
      purpose: approval_checkpoint_owner
    explorer:
      mode: read_only
      purpose: codebase_research
    worker:
      mode: edit_within_allowed_paths
      purpose: implementation
    verifier:
      mode: read_only
      purpose: exit_criteria_review
  runner:
    preferred:
      - subagent
    fallback: null
    brief_is_authoritative: true
```

## Exit Criteria (Machine Verifiable)

This block contains only non-executable artifact requirements. Define every
executable check once in the canonical Verification Plan below. Each check must
state its phase, cost, evidence policy, necessity, and input environment; a
missing or malformed plan fails closed. Populate artifact requirements only
for deliverables this task actually owns; do not create a spec, notes or report
merely to fill this template.

```yaml
exit_criteria:
  files_exist: []
  artifacts_exist: []
```

## Verification Plan

```json
{
  "protocol": 1,
  "checks": [
    {
      "id": "build",
      "kind": "command",
      "cwd": ".",
      "phase": "verification",
      "cost": "normal",
      "evidence_policy": "current_exact",
      "necessity": "Approved plan repository verification boundary; Official closure guards must remain fail closed.",
      "inputs": {
        "env": []
      },
      "command": "bun run build"
    },
    {
      "id": "typecheck",
      "kind": "command",
      "cwd": ".",
      "phase": "verification",
      "cost": "normal",
      "evidence_policy": "current_exact",
      "necessity": "Approved plan repository verification boundary; Official closure guards must remain fail closed.",
      "inputs": {
        "env": []
      },
      "command": "bun run typecheck"
    },
    {
      "id": "test",
      "kind": "command",
      "cwd": ".",
      "phase": "verification",
      "cost": "normal",
      "evidence_policy": "current_exact",
      "necessity": "Approved full suite; new empty TMPDIR isolates the diagnosed machine-state failure without changing deadlines.",
      "inputs": {
        "env": ["TMPDIR"]
      },
      "command": "bun run test"
    },
    {
      "id": "test-scripts",
      "kind": "command",
      "cwd": ".",
      "phase": "verification",
      "cost": "normal",
      "evidence_policy": "current_exact",
      "necessity": "Approved plan repository verification boundary; Official closure guards must remain fail closed.",
      "inputs": {
        "env": []
      },
      "command": "bun run test:scripts"
    },
    {
      "id": "check-api-surface",
      "kind": "command",
      "cwd": ".",
      "phase": "verification",
      "cost": "normal",
      "evidence_policy": "current_exact",
      "necessity": "Approved plan repository verification boundary; Official closure guards must remain fail closed.",
      "inputs": {
        "env": []
      },
      "command": "bun run check:api-surface"
    },
    {
      "id": "check-version-authority",
      "kind": "command",
      "cwd": ".",
      "phase": "verification",
      "cost": "normal",
      "evidence_policy": "current_exact",
      "necessity": "Approved plan repository verification boundary; Official closure guards must remain fail closed.",
      "inputs": {
        "env": []
      },
      "command": "bun run check:version-authority"
    },
    {
      "id": "check-release-graph",
      "kind": "command",
      "cwd": ".",
      "phase": "verification",
      "cost": "normal",
      "evidence_policy": "current_exact",
      "necessity": "Approved plan repository verification boundary; Official closure guards must remain fail closed.",
      "inputs": {
        "env": []
      },
      "command": "bun run check:release-graph"
    },
    {
      "id": "check-release-pack",
      "kind": "command",
      "cwd": ".",
      "phase": "verification",
      "cost": "normal",
      "evidence_policy": "current_exact",
      "necessity": "Isolated packed install and real CLI smoke; new empty TMPDIR is required by the consolidation ruling.",
      "inputs": {
        "env": ["TMPDIR"]
      },
      "command": "bun run check:release-pack"
    },
    {
      "id": "strict-workflow",
      "kind": "command",
      "cwd": ".",
      "phase": "verification",
      "cost": "normal",
      "evidence_policy": "current_exact",
      "necessity": "Required workflow validation.",
      "inputs": {
        "env": []
      },
      "command": "repo-harness run check-task-workflow --strict"
    }
  ]
}
```

Author the actual checks using [Testing Policy and Artifact Standards](../../docs/reference-configs/sprint-contracts.md#testing-policy-and-artifact-standards).
The empty array is not permission to omit required repository checks: retain it
only when no executable criterion applies and explain why in Acceptance Notes.
Prefer existing covering tests; creating a task-named test or adding typecheck
is not a template requirement. For each selected check declare `id`, `kind`,
`cwd`, `phase`, `cost`, `evidence_policy`, `necessity`, `inputs.env`, and its
`command` or `path`. Declare the same execution once, including checks nested
inside aggregate scripts. Use `baseline_with_delta` only with an immutable
baseline and named current delta checks; never infer it from paths or command text.

## Acceptance Notes (Human Review)

- Changed behavior/boundary, existing covering tests and remaining gap:
- New test case/file rationale, or why existing coverage is sufficient:
- Selected check IDs and why their coverage is sufficient; omitted coverage:
- Full/expensive check justification and expected cost, if applicable:
- Execution/baseline references, subject, current delta and disposition:
- Residual risks and incomplete observations:

## Rollback Point

- Commit / checkpoint: 3dd7ba6f
- Revert strategy: revert only local task commits; no remote state changes.

### Independent gate repair round 1

Owner-scoped delta verification: test:scripts, every scripts/release/*.test.mjs, architecture-projection check, typecheck, strict workflow; additionally execute the new v6 cloud refusal case. Existing expensive build/full-suite/release-pack evidence remains on the recorded implementation; this slice changes fixtures, test coverage, comments and deterministic projections only. Continuation gate.state.refusal propagation is explicitly deferred to M4.

### M4 verification boundary

Owner requires relevant five-edge/custody/workflow checks plus build, typecheck, full test with a new empty TMPDIR, test:scripts, and strict workflow. M4 does not rerun release-pack or mutate projection on each commit. A separate isolated npm install probes the exact official runtime's physical duplicate instances with the same offline scripted-workflow fixture; installed-file attestation is reused unchanged. No provider/registry publish.

### Final closeout verification boundary

Owner requires sequential closeout: correct the two M4 MEDIUM findings and document all four duplicated siblings; persist M5 sanitized evidence even though C remains 1024; rebase latest origin/main and stop on any conflict. Only after integration freeze run build, typecheck, new-TMPDIR full test, test:scripts, all release tests, api-surface, version-authority, release-graph, strict workflow. Then materialize architecture once, commit it, require projection check exit 0, and run release-pack from a clean worktree with sourceGitSha equal to final HEAD. No push/publish or additional provider calls.
