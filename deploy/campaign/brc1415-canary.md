# BRC14/15 BYOK SDK canary

Owner selected public Ancienttwo/byok-sdk after accepting existing Connector availability. Preserve intentional local/Web GPT account separation and the current UI model. Use only codex/brc1415-canary, one group, at most two Issues, max parallel two, two authoring rounds, manual merge. No main mutation, release or npm publication.

Overall forty-call/ninety-minute budget retains expiry 2026-09-08T16:34:27.212Z. Prior AiphaBee attempts consumed two reserved calls (one prompt). This target receives one shadow revision observation; conditional active grant is limited to the remaining thirty-seven calls and same expiry. No old observation is reclassified as valid revision evidence.


Raw revision observation on shadow target 5b4808e51f9cf8a5b0abd24a038243e369b879b9 was verified using the exact commit and ref resources. The revised overall allocation has consumed five reserved calls (three submitted prompts, two pre-send failures). Active/manual grant is capped at thirty-five calls and the same expiry; its own new target revision must pass admission before materialization or workers.


## Approved collection and budget correction

The complete provider collection contains 178 Issue/PR records across two pages, totaling 1,025,686 bytes including repository identity. Three operator diagnostic reads verified the existing adapter succeeds with sufficient finite limits; this is not campaign admission evidence. The original max_issues=20 and max_total_bytes=262144 could not cover this repository. Use max_pages=4, max_issues=400 and max_total_bytes=8388608; retain max_body_bytes=16384 and the 30-second deadline. Pagination, payload rejection and complete-snapshot requirements remain unchanged. At ten times the observed collection this policy deliberately refuses until an operator sets a new bounded scope.

Budget accounting includes five prior reservations, four active reservations and three diagnostic reads: twelve calls consumed against the original forty-call envelope, leaving at most twenty-eight with the original expiry 2026-09-08T16:34:27.212Z. Any new grant must bound runner invocations and agent turns consistently with provider calls, rather than the former four-invocation limit. The exhausted run and its stop receipt remain immutable. No replacement grant has been issued.

Issues #177 and #178 exist but lack the configured brc1415-canary eligibility label. A complete collection remains mandatory; selecting only those numbers cannot prove absence of duplicate campaign slots. The current product cannot transplant authoring into a new campaign or revive an exhausted run. Preserve the two Issues pending the Owner decision on formal continuation versus a fresh campaign.
