/**
 * Type surface for `pi-subagent-runner-bridge.js`: the un-pruned vendored
 * background-runner entry (`runs/background/subagent-runner.ts`), mirroring
 * its declared signature exactly.
 */

export declare function runSubagentRunnerEntry(argv?: readonly string[]): void;
