{
  "name": "@byok-sdk/example-salesko-connector-broker",
  "version": "0.0.1",
  "private": true,
  "description": "Reference Salesko-style local connector broker: OS credential custody, read-only Gmail provider port, domain policy, and stdio MCP",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=22.19.0"
  },
  "bin": {
    "salesko-connector-mcp": "./dist/bin/salesko-connector-mcp.js"
  },
  "scripts": {
    "build": "tsup && tsc -p tsconfig.build.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@byok-sdk/keys": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "tsup": "^8.5.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
