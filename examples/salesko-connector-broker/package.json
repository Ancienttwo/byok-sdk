{
  "name": "@byok-sdk/example-salesko-connector-broker",
  "version": "0.0.1",
  "private": true,
  "description": "Private Salesko-style connector broker with desktop Google OAuth, read-only Gmail metadata, local policy, and stdio MCP",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=22.22.0"
  },
  "bin": {
    "salesko-connector-mcp": "./dist/bin/salesko-connector-mcp.js"
  },
  "scripts": {
    "build": "tsup && tsc -p tsconfig.build.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@byok-sdk/keys": "workspace:*",
    "email-addresses": "5.0.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "tsup": "^8.5.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
