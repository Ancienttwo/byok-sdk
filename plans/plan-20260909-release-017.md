# Release 0.17.0 / keys 0.4.3

## Authority and P1/P2/P3

User approved the proposed SDK MINOR publication acceptance. Prepare, verify,
merge accepted source, publish frozen CI tarballs and read back registry/tag/release.
No downstream dependency change, product deployment or real database migration.
Ten manifests own versions; bun.lock and API goldens are projections. CI artifact
manifest binds source SHA and tarball hashes. Registry readback verifies the
installed dependency closure; keys patch changes its exact core dependency.
Schema-v3 migration refusal and durable receipt limits must be public release notes.
At 10x package count partial publication/registry propagation is the first risk;
retain immutable artifacts and never republish an already uploaded package.

## Task Breakdown

- [x] Verify candidate ancestry, current registry and release policy
- [x] Freeze version/docs metadata and run source gates
- [x] Obtain exact-source CI and frozen release pack acceptance
- [x] Merge source (PR #179; main f00646d7f82e518f02293923687bdf81f64076d5)
- [ ] Execute publication and verify registry identities/integrity
- [ ] Verify annotated tag/GitHub release and record downstream handoff

## Local source acceptance

Build, typecheck, API goldens, version authority, release graph, workflow and root
script checks passed. Full workspace tests: 3942 passed / 135 skipped. Ignored logs:
`_ops/release017-*.log`. npm profile readback returned E401; restore registry login
before execute publication. No credentials are retained in this record.

## Release acceptance handoff

See `docs/releases/v0.17.0-handoff.md`. Branch and main CI both passed 23/23;
main artifact dry-run and public consumer acceptance passed. Publication remains
blocked by npm E401 invalid login. No package upload or release tag was attempted.
