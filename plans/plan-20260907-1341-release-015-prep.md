# Plan: recovery release preparation

> **Status**: In Progress
> **Task Contract**: tasks/contracts/20260907-1341-release-015-prep.contract.md
> **Implementation Notes**: tasks/notes/20260907-1341-release-015-prep.notes.md

## Authority and scope
User approved PR156 merge and corrected release preparation. PR156 is merged at 28381941c199085d9a677ab6425df8e048219ea6. Prepare and push a reviewable corrected candidate; do not publish npm packages, tag a release, deploy, or change downstream pins.

## P1 Architecture Map
Ten public manifests own versions; bun.lock projects workspace versions. Nine packages share the SDK train; keys independently versions its exact packed core dependency. Existing release scripts and CI own packing and isolated installation verification.

## P2 Concrete Trace
Manifest -> lock workspace entry -> bun pm pack exact internal dependency -> isolated npm installation -> hashed release-manifest bound to sourceGitSha. Reusing keys0.4.0 would reuse an immutable artifact with an old core edge.

## P3 Design Decision
Prepare SDK0.15.0 and keys0.4.1 after merged recovery fix. PR156 adds the public TaskRunner.hasPendingRecoveredAgentMessage method; docs/spec.md requires MINOR for additive public API, even when introduced by a bug fix. No runtime changes, dependencies or abstractions. At 10x package count partial publication is the first coordination risk; retain exact manifest authority. New workflow files record the approval and verification boundary.

## Task Breakdown
- [x] R1 Verify merged base and registry versions, freeze metadata scope.
- [ ] R2 Validate source gates and commit final candidate metadata.
- [ ] R3 Push candidate and verify exact push-CI release artifact; record handoff without publication.

## Source gate blocker
Final metadata build, typecheck, API surface, version authority, release graph and strict workflow passed. Root test stopped in client: 1727 passed, 11 skipped, 1 failed. The existing cancellation-tombstone compiled recovery test read local journal truth_state pending where confirmed was asserted (execution-recovery-kill.test.ts:620). No production/test source was changed for release preparation. Do not push for costly release CI or publish until the separate recovery verification issue is resolved. R2 remains incomplete; R3 was not started.

## Approved recovery assertion slice
User approved proving and fixing the cancellation test confirmation race. P1: cloud attempt state and daemon SQLite terminal truth are separate authorities. P2: cloud accepts terminal -> HTTP acknowledgement -> onOutboundAccepted -> await journal.confirmTerminal; reading cloud state does not await the final local write. P3: gate only the compiled test fixture before confirmTerminal, prove the existing assertion fails, then release the gate and wait on existing waitForJournalTruth. Preserve all terminal bytes, cancellation and one-runtime assertions; no production changes or new dependencies. At 10x scheduling contention this unawaited boundary races more often; semantic synchronization fixes it without extending sleeps/timeouts.
- [x] F1 Deterministically reproduce pending journal at the cloud-accepted boundary.
- [x] F2 Release the fixture gate and await durable confirmation; run focused and required source checks.
