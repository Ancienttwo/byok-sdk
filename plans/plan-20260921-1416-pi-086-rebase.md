# Plan: Re-land the Pi fork and its prepared seam on upstream 0.86.1

> **Status**: Executing
> **Created**: 20260921-1416
> **Slug**: pi-086-rebase
> **Artifact Level**: work-package
> **Promotion Reason**: moves the pinned Pi runtime across an upstream breaking release (0.85.1 → 0.86.1), re-implements the fork's prepared-input seam on the new `SystemMessage`/`TranscriptContext` model, and bumps the SDK's prepared wire contract (compiler/envelope 2 → 3); crosses the fork repository, a published artifact line and `@byok-sdk/protocol` + `@byok-sdk/client`
> **Verification Boundary**: fork — `npm run check` + the fork's test suites + the S0 falsifier promoted to a standing test; SDK — root required checks + `bun run check:release-pack` + the prepared-lane test files + one offline compile/consume regression against the newly pinned fork build
> **Rollback Surface**: the SDK pin returns to `pi-coding-agent@0.85.1008` / `pi-agent-core@0.85.1008` / `pi-ai@0.85.1007` by reverting the single SDK PR; the 0.85 fork line stays published and untouched; 0.86 fork builds stay published but unreferenced
> **Spec**: `docs/spec.md`
> **Research**: `docs/researches/runtime-input-preparation-contract.md` (§17–§18d); architecture consultation + S0 falsifier result of 2026-09-21 (summarised under Due diligence; the falsifier test is `packages/coding-agent/test/s0-prepared-falsifier.test.ts` on fork branch `claude/pi-086-s0-falsifier`, commit `0a802b8a4`)
> **Task Contract**: `tasks/contracts/20260921-1416-pi-086-rebase.contract.md`
> **Task Review**: `tasks/reviews/20260921-1416-pi-086-rebase.review.md`
> **Implementation Notes**: `tasks/notes/20260921-1416-pi-086-rebase.notes.md`

## Agentic Routing
- Selected route: parent-owned P1/P2/P3; execution delegated to workers slice by slice, each fork slice and the SDK slice accepted through `gatekeeper`; fork publication is an Owner action.
- Routing reason: Owner request 2026-09-21 to move the Pi dependency to upstream 0.86.1. Upstream has no prepared-input seam, so the fork cannot be retired; it has to be re-landed.
- Due diligence:
  - P1 map: the fork (`@byok-sdk/pi-ai`, `pi-agent-core`, `pi-coding-agent`; source only on this machine, sole remote = upstream `earendil-works/pi`, never pushed to) is `v0.85.1` + 26 own commits in four themes: (a) mechanical — RPC frame bound + `./rpc-types` export, model-catalog codegen fix, `find` parameter narrowing, MCP SDK production dependency, fork release tooling; (b) single-line `origin === undefined` host-canonical guards in usage/compaction/answer readers; (c) the lazy native-clipboard patch; (d) the prepared seam — `system-prompt-renderer.ts`, `prepareOpenAICompletionsRequest` / `consumePreparedOpenAICompletionsRequest`, `nativeModel()`, `PREPARED_RESIDUAL_TABLE`, `createPreparedAgentSession`, drift refusals. The SDK consumes the seam in `packages/client/src/adapters/pi/input-preparation.ts` and `packages/client/src/bin/pi-prepared-host.ts`, states its wire in `packages/protocol/src/input-preparation.ts`, and pins the fork by exact alias version. Out of scope: keys launcher, consent gate, counter, authority resolver, remote-lane transport, Salesko.
  - P2 trace (0.86.1, proven by S0): explicit prompt inputs → upstream `buildSystemPrompt`/`buildSystemPromptSections` → `Context{systemPrompt,messages,tools}` → `normalizeContext` (first message becomes a `SystemMessage`, literal `timestamp: 0`) → openai-completions `convertMessages` flattens the FIRST system message through `getSystemMessageText` into one string → request body D. S0 showed D computed this way is byte-identical to the first request of a real 0.86.1 `AgentSession` (both context shapes × no tools / `read+bash`; 1952 / 3668 bytes), that a wrong cwd differs only at `$.messages[0].content`, that D is stable under a 1h clock advance, that the only impure section builder is `docs` (`system-prompt.ts:153` → `config.ts` `getPackageDir()`), and that a live session always constructs a `CacheWarmer` (`sdk.ts:307`, default mode `streaming`) which stayed inactive only because the test model had no `promptCache`. Pressure points: the fork's own renderer (now divergent from upstream's section/XML rendering), the `Context`-typed drift comparison, and `CacheWarmer`.
  - P3 decision rationale: the seam's invariant is "the bytes the Host counted are the bytes the provider receives, or the session refuses". 0.86 changed how the system prompt is rendered, not what reaches the wire: `sections`, `toolsAdded` and `timestamp` never enter D. So the snapshot keeps carrying explicit prompt inputs (no transcript on the wire), and the fork stops owning a renderer — it calls upstream's builders with one added pure parameter. The rendered text does change (non-preamble sections are XML-wrapped), so every stored artifact of contract v2 is unusable against the new runtime; that is stated as a version bump with a named readiness refusal, not bridged. At 10x the first thing to break is per-provider body construction; the redo therefore converges on upstream's `onPayload` hook plus a pure exported request builder so the fork shrinks toward something upstreamable.

## Workflow Inventory
Complete this inventory before implementation. If any line is unknown, keep the plan in Draft and fill it before projection.

- Active plan: `plans/plan-20260921-1416-pi-086-rebase.md`
- Sprint contract: `tasks/contracts/20260921-1416-pi-086-rebase.contract.md`
- Sprint review: `tasks/reviews/20260921-1416-pi-086-rebase.review.md`
- Implementation notes: `tasks/notes/20260921-1416-pi-086-rebase.notes.md`
- Deferred-goal ledger: `tasks/todos.md`
- Current checks: `.ai/harness/checks/latest.json`
- Run snapshots: `.ai/harness/runs/`
- Scope authority: `tasks/contracts/20260921-1416-pi-086-rebase.contract.md` `allowed_paths`
- Concurrency rule: `.ai/harness/active-plan` selects the active plan for this worktree when present; `.ai/harness/active-worktree` records the owning worktree. If another worktree already owns active work, open or switch to the matching worktree instead of serializing unrelated plans.
- Execution isolation: approved contract-level work projects through `repo-harness run plan-to-todo --plan plans/plan-20260921-1416-pi-086-rebase.md` and may start `repo-harness run contract-worktree start --plan plans/plan-20260921-1416-pi-086-rebase.md`.

## Approach
### Strategy
A new fork line is built forward from the upstream tag; the 0.85 line is not merged or rebased (a dry-run merge conflicts in 22 files and would drag the old seam along). Slices run strictly in order, one writer at a time, on fork branch `claude/pi-086` cut from `v0.86.1`:

- **S1 Mechanical replay.** Re-apply themes (a) and (b) as fresh commits against 0.86.1 files: RPC frame bound + `./rpc-types`/root export + depth-1 id recovery; catalog codegen type-only import fix; `find` parameter narrowing; `@modelcontextprotocol/sdk` production dependency (only if S3 shows it is still needed — replayed provisionally, judged in S3); fork release tooling (`scripts/fork/*`, stage/publish, `assertStagedAliasCoherence`) with the upstream base moved to 0.86.1; the host-canonical single-line guards at their new locations (`evals/pi-harness.ts` is now `harness.ts`). Each replayed fix carries its original test. The prepared seam, the renderer split and the clipboard patch are NOT replayed. Exit: `npm run check` and the fork test suites green on the branch.
- **S2 Clipboard patch verdict.** Upstream deleted `clipboard-native.ts` in favour of pi-tui's helper. Decide with evidence whether the SDK helper argv still loads a native module eagerly: a sealed-host smoke under the SDK's argv. Outcome is either "obsolete, dropped" or a minimal new patch.
- **S3 Toolchain verification.** `tsc` + runtime on the S1 branch for: `@google/genai` 2.21, `openai` 6.40 (the seam sends a raw string body through `client.post` — prove it still goes out byte-exact), and whether the MCP SDK dependency fix is still required. Also settle the three items the consultation left unverified: `transform-messages.ts` determinism, raw-body `client.post`, and bun's behaviour on an unresolvable bare specifier.
- **S4 Seam redo.** (1) Add a pure `docsPaths` parameter to `BuildSystemPromptOptions`; the fork calls upstream's builders and carries no renderer of its own. (2) Snapshot inputs gain `toolGuidelines` and `sections`; `forceSystemPrompt` is refused. (3) Compile = `normalizeContext` + the openai-completions request builder, exported pure as `buildRequestPayload(model, TranscriptContext, options)`; consume moves onto the `onPayload` shape. (4) Drift comparison stays at the same point (after `normalizeContext`, before the stream function) and compares provider-visible identity: rendered text of the first system message, tool declarations from `getCurrentTools`, and the remaining messages; precondition — exactly one system message, in first position, else refuse. (5) A prepared session never constructs a `CacheWarmer` (`createAgentSession` passes `cacheWarmer: undefined` under `preparedInput`) and admission refuses one. (6) `nativeModel` admits the `promptCache` key only when absent-valued; `preparedToolProjection` includes `constrainedSampling` (built-in tools enable it by default from 0.86 and it changes the tools bytes of D). (7) Residual table: the top-level body key set of `buildParams` is unchanged 0.85.1 → 0.86.1, so no new classifications; the redo re-proves that with a test rather than asserting it. (8) Compiler and envelope contract versions 2 → 3; the v2 reader is deleted. (9) The S0 falsifier becomes a standing test, extended to what S0 did not cover: turn ≥ 2, tool-result turns, mid-conversation sections patch, non-empty contextFiles/skills/customPrompt, a model with `promptCache`.
- **S5 SDK wire v3 + pin.** Protocol snapshot gains `toolGuidelines`/`sections`; client compiler call, prepared host and store carry them; contract versions move to 3 with no v2 acceptance; a stored v2 record is refused at READINESS with the named reason `runtime_contract_superseded` (not left to fail as drift at consume); fork pin → the published 0.86 build; identity pins/gates, third-party manifest and the doc lines that state the pin move with it; a dated section in `docs/researches/runtime-input-preparation-contract.md`.

Fork versions follow `<upstream>.<forkBuild>` → `0.86.1xxx`; whenever `pi-ai` moves, every package that pins it by exact alias is re-published in the same build.

### Trade-offs
| Option | Pros | Cons | Decision |
|--------|------|------|----------|
| Build forward from `v0.86.1`, redo the seam on upstream's model (this plan) | Smallest fork; renderer ownership returns to upstream; converges on `onPayload` | Wire contract bump; stored v2 artifacts become unusable | **Chosen** |
| Merge/rebase the 0.85 fork line onto 0.86.1 | Keeps history | 22 conflicting files, all in the rewritten seam hosts; preserves a renderer that no longer matches the live session | Rejected |
| Keep the frozen renderer, accept divergence from the live session | No seam work | Breaks the invariant: counted bytes ≠ sent bytes (or permanent drift refusal) | Rejected |
| Put the transcript (`SystemMessage` with sections) on the wire | Mirrors upstream types | S0 proved it unnecessary; couples the SDK wire to an upstream internal type | Rejected |
| Accept v2 records alongside v3 | No Host-visible refusal | Dual reader for bytes that can never match; steady-state compatibility path | Rejected |
| Stay on 0.85.1 until the official-Pi migration | No work now | Owner asked for 0.86.1; the official-Pi capability proof should target the 0.86 model anyway | Rejected |

## Detailed Design
### File Changes
Fork repository (`claude/pi-086`, not part of this repo's `allowed_paths`): S1–S4 as described; the per-slice file lists are fixed in each dispatch packet and recorded in the notes file.

This repository (S5; the exact carrier list is re-derived by an explorer pass at the S4 gate, because S4 fixes the final shape of the two new snapshot fields):
| File | Action | Description |
|------|--------|-------------|
| `packages/protocol/src/input-preparation.ts` (+ test) | edit | snapshot `toolGuidelines`/`sections` with closed shapes; compiler/envelope contract version 3; readiness reason `runtime_contract_superseded` |
| `packages/client/src/adapters/pi/input-preparation.ts` (+ test) | edit | compile against the 0.86 seam; pass the two new inputs verbatim |
| `packages/client/src/bin/pi-prepared-host.ts` (+ test) | edit | consume against the 0.86 seam; no `CacheWarmer`; config format version if its shape changes |
| `packages/client/src/daemon/control-protocol.ts`, `daemon/input-preparation-store.ts` (+ tests) | edit | hand parser + store round-trip for the new fields; v2 record → `runtime_contract_superseded` |
| `packages/client/package.json`, `bun.lock`, identity pins/gates, `packages/client/vendor/` manifest | edit | fork pin → 0.86 build |
| `scripts/release/*` that assert the fork build | edit | follow the pin |
| `docs/researches/runtime-input-preparation-contract.md` | edit | new dated section (§19) |
| `tasks/todos.md` | edit | ledger: upstream the `docsPaths` parameter and `buildRequestPayload`; Salesko follow-up (facts `promptEnvironment` vs the new inputs) |

### Code Snippets
None frozen here. The `docsPaths` parameter shape and the `buildRequestPayload` signature are fixed at the S4 dispatch, after S3 evidence.

### Data Flow
Host snapshot `{…, toolGuidelines, sections}` → protocol v3 → daemon parse → store → SDK compiler → fork: upstream `buildSystemPrompt(…, docsPaths)` → `normalizeContext` → `buildRequestPayload` → D + projection/residual classification → envelope v3 → prepared host → prepared session (no `CacheWarmer`) → drift check on provider-visible identity → `onPayload` sends exactly D.

## Risk Assessment
| Risk | Likelihood | Impact | Mitigation |
|------|------------|--------|------------|
| Byte equality fails beyond the first request (turn ≥ 2, tool results, sections patch) | Medium | High | S4 extends the falsifier to those cases BEFORE the seam is declared done; the prepared lane only ever sends the first request, so a failure narrows admission instead of breaking it |
| `constrainedSampling` or another 0.86 default silently changes D | Medium | High | tool projection includes it; residual-table re-proof test over the reachable configuration space, as done for build 7 |
| A prepared session warms the cache (extra real request, `cache_warm` usage) | Certain if unhandled | High | structural: not constructed + admission refusal + a test counting outbound requests = 1 on a `promptCache` model |
| `openai` 6.40 re-serialises a raw string body | Low | High | S3 proves byte-exact egress before S4 starts; if it fails, stop and re-consult |
| Partial fork release (build 7 repeat) | Medium | Medium | `assertStagedAliasCoherence` is replayed in S1; all three packages publish together |
| Fork source exists on one machine only | Certain | High | Owner decision pending on a fork-owned remote; never push to upstream |
| Stored v2 records in downstream hosts | Certain | Medium | named readiness refusal; release notes; Salesko follow-up is a ledger row |
| Version authority / package version bump | Medium | Medium | no bump in this plan; if `check:version-authority` demands one, stop and ask the Owner |

## Task Contracts
- Contract file: `tasks/contracts/20260921-1416-pi-086-rebase.contract.md`
- Review file: `tasks/reviews/20260921-1416-pi-086-rebase.review.md`
- Implementation notes file: `tasks/notes/20260921-1416-pi-086-rebase.notes.md`
- Template: `.claude/templates/contract.template.md`
- Verification command: `repo-harness run verify-contract --contract tasks/contracts/20260921-1416-pi-086-rebase.contract.md --strict`
- Active plan rule: `.ai/harness/active-plan` is authoritative for this worktree when present; `.ai/harness/active-worktree` records the owning worktree. Do not infer active execution from the latest non-archived plan.

## Handoff

- Checks file: `.ai/harness/checks/latest.json`
- Session handoff: `.ai/harness/handoff/current.md`

## Promotion Gate

- **Merge/PR unit**: one SDK PR (`claude/pi-086-rebase`) for S5; the fork work is its own branch and its own published build(s).
- **Rollback surface**: revert the SDK PR (pin back to fork build 8). Records written under contract v3 are refused by a reverted SDK; no data migration either way.
- **Verification boundary**: `bun run build`, `bun run typecheck`, `bun run test`, `bun run check:api-surface`, `bun run check:version-authority`, `bun run check:release-pack`, `repo-harness run check-task-workflow --strict`.
- **Review/acceptance boundary**: `gatekeeper` after S1, after S4 (fork diff), and on the SDK diff.
- **High-risk surface**: prepared request bytes; shared wire schema; pinned runtime identity; published artifacts.
- **Why not checklist row**: two repositories, a breaking upstream release, a wire-contract version bump and a published artifact.

## Evidence Contract

- **State/progress path**: this plan's Task Breakdown + the notes file (fork branch tips and per-slice dispatch file lists are recorded there).
- **Verification evidence**: pasted runner tails, verbatim; the standing falsifier's output; registry `dist.integrity` vs the stage manifest for the 0.86 fork build; outbound-request count for the `promptCache` case.
- **Evaluator rubric**: D from explicit inputs is byte-identical to the live 0.86.1 session's first request across the extended matrix; the fork contains no system-prompt renderer of its own; a prepared session issues exactly one request; no v2 reader exists; a v2 record is refused at readiness with `runtime_contract_superseded`.
- **Stop condition**: the SDK regression passes against the PUBLISHED 0.86 fork build with required checks green — or a hard stop if S3 shows raw-body egress is not byte-exact, or S4's extended falsifier breaks equality on the first request.
- **Rollback surface**: as above.

## Annotations
<!-- [NOTE]: prefixed inline. Claude processes all and revises. -->

[NOTE] 2026-09-21, correcting S1's scope. The Strategy above lists theme (b), the host-canonical guards, as mechanical. It is not: on the 0.85 line the only admission checks for a `HostCanonicalAssistantMessage` live in two seam files (`prepareOpenAICompletionsRequest`'s native validator and `core/input-preparation.ts`), both introduced by the excluded seam commit `a4ebf781c`, and nothing on the fork tip constructs the variant outside them. Replaying `3879d05b3`/`cb4a6de14` in S1 would widen the published `Message` union with no producer and no validator — a fail-open type on a published package. Ruling: theme (b) moves to S4 and lands as one unit with its validators; its two seam-independent behavioural tests (`agent-session-stats`, `session-manager/host-canonical-history`) come with it. S1 also left three seam-owned pieces out of the fork tooling, to be re-attached in S4: the `preparedContract` block (config key, manifest field, its test), the `prepared-session-input` staged smoke, and the `checkConsumerDeclarations` loop.

## Task Breakdown
- [x] S0 Falsifier on `v0.86.1`: D from explicit prompt inputs equals the live session's first request — HYPOTHESIS HOLDS, 10/10, fork branch `claude/pi-086-s0-falsifier` @ `0a802b8a4` (2026-09-21)
- [x] S1 Fork: mechanical replay onto `v0.86.1` (`claude/pi-086`), `npm run check` + suites green, gate — 7 commits, tip `cd8e99782`, independently gated PASS on 2026-09-21; theme (b) and three seam-owned tooling pieces deferred to S4 per the note above; staged version would be `0.86.1001`, nothing staged or published
- [ ] S2 Fork: clipboard patch verdict with sealed-host smoke evidence
- [ ] S3 Fork: toolchain verification (genai 2.21, openai 6.40 raw-body egress, MCP SDK dependency, the three unverified items)
- [ ] S4 Fork: seam redo (host-canonical history variant + its validators as one unit, tooling re-attachments per the 2026-09-21 note, docsPaths, snapshot inputs, pure request builder + `onPayload` consume, drift identity, no CacheWarmer, `promptCache`/`constrainedSampling`, residual re-proof, contract v3, extended falsifier), gate
- [ ] S4b Owner: publish the 0.86 fork build from Terminal.app; verify registry integrity against the stage manifest
- [ ] S5 SDK: wire v3 + carriers + `runtime_contract_superseded` + pin + doc section + ledger entries
- [ ] S6 Required checks incl. `check:release-pack`, gate, PR
